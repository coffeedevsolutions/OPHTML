# facts: project/faq

No new verification. Every row below restates a fact a parent facts file
already verified, or a finding `docs/findings.md` already confirmed. The
"verified by" column names the parent file and fact id (or finding id)
the page's sentence rests on.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| faq.questions | The page answers twenty questions, in brief order: Why is flex-direction required; Why px only; Why only eight named colours; fontgen refuses on macOS; Text is boxes on the console; Solid fills vanish on the console; PS2UI_ERR_ARENA at boot; PS2UI_ERR_SIZE from tex_set; The previewer does not show hidden rows; Can I draw over a game; How do I scroll; How do I make a dialog; ps2ui check prints a negative budget; What the payload column means; Which number is the arena; Can I use a weight axis; Does &nbsp; work; Why is my slot text cut; Two screens use the same slot name; Which test target do I run | `_prompts/project/faq.md` Page structure | the twenty H3 headings on the page, in this order | verified |
| faq.flex-direction | A container laying out two or more children must declare `flex-direction`; a one-child or empty container is never asked | `_facts/authoring/css.md` `css.flex-direction.required` | restates `css.flex-direction.required` | verified |
| faq.px-only | `px` is the only unit most length properties take; `width`/`height`/`min-*`/`max-*`/`flex-basis` also take `%` and `auto`; `em`/`rem`/`vw`/`vh`/`ch` are refused by name | `_facts/authoring/css.md` `css.units` | restates `css.units` | verified |
| faq.eight-colours | The named colour set is exactly eight names, plus `transparent`; any other name is a compile error naming the token | `_facts/authoring/css.md` `css.colors`, `css.colors.unknown` | restates `css.colors`, `css.colors.unknown` | verified |
| faq.fontgen-macos | `ps2ui fontgen` checks `features.check("raqm")` before opening a font and refuses without it; the remedy tries fribidi first, then a Pillow rebuild against libraqm | `_facts/cli/ps2ui-fontgen.md` `fontgen.raqm`, `fontgen.raqm.remedy`; `_facts/getting-started/installation.md` `install.raqm.remedy` | restates `fontgen.raqm`, `fontgen.raqm.remedy`, `install.raqm.remedy` | code-only (this session's Pillow has Raqm, so the live refusal is unmocked; the parent rows verify the mocked text a test printed) |
| faq.text-boxes | `ps2ui_render` sets `PrimAlphaEnable` ON every call because gsKit feeds that field to `TEX0.TCC` as well as `PRIM.ABE`; TCC off makes a glyph atlas read as opaque RGB, so a glyph fills its whole quad | `_facts/runtime/frame-loop.md` `loop.primalpha`; `docs/findings.md` F-004 | restates `loop.primalpha`; cites finding F-004 (confirmed) | verified |
| faq.fills-vanish | gsKit's default blend computes coverage as `128 - As`, not `As`; an opaque fill authored at `As = 128` composites to zero coverage under that default and disappears; `ps2ui_render` writes its own ALPHA register every call for this reason | `_facts/runtime/frame-loop.md` `loop.blend.equation`; `docs/findings.md` F-002 | restates `loop.blend.equation`; cites finding F-002 (confirmed) | verified |
| faq.err-arena | `PS2UI_ERR_ARENA`: `arena` is NULL or `arena_size` is below `ps2ui_arena_size()`; size the arena from the bake transcript or the function, not a hardcoded figure | `_facts/runtime/errors-and-constants.md` `errors.arena`; `_facts/runtime/frame-loop.md` `arena.contract`, `arena.number.per-blob` | restates `errors.arena`, `arena.contract`, `arena.number.per-blob` | verified |
| faq.err-size | `PS2UI_ERR_SIZE` from `ps2ui_tex_set`: `len` differs from the entry's reservation; pass the `payload` figure, not the page-rounded `in pages` figure; the return is a bare code | `_facts/runtime/streaming-art.md` `tex.errors`, `tex.len-is-payload` | restates `tex.errors`, `tex.len-is-payload` | verified |
| faq.previewer-hidden | `preview.render` and `ps2ui serve` take no visibility parameter, so the page always draws the baked state; a node hidden with `ps2ui_visible_set` still looks focusable in the previewer | `_facts/cli/previewer.md` `serve.limits` | restates `serve.limits` | verified |
| faq.draw-over-game | There is no compositing API; an overlay is a second `screen_set`/`render` pair in the same frame, since `ps2ui_render` never clears; the Python previewer needs a workaround script to show the composite | `_facts/authoring/screens-and-overlays.md` `compose.contract`, `compose.no-api`, `compose.previewer.workaround` | restates `compose.contract`, `compose.no-api`, `compose.previewer.workaround` | verified |
| faq.scroll | CSS `overflow` has no scrolling value; a scrolling window of rows is `data-repeat` plus the runtime `ps2ui_list` API, whose window slides the minimum distance and never wraps | `_facts/authoring/css.md` `css.properties.overflow`; `_facts/authoring/lists.md` `list.window.minimum-scroll`, `list.window.no-wrap` | restates `css.properties.overflow`, `list.window.minimum-scroll`, `list.window.no-wrap` | verified |
| faq.dialog | A dialog is a second baked screen; `screen_set` to it before the second `render` in the same frame; the last `screen_set` owns the D-pad; dismissing is one `screen_set` back to the base, which restores its remembered focus | `_facts/authoring/screens-and-overlays.md` `compose.contract`; `_facts/runtime/frame-loop.md` `loop.two-renders` | restates `compose.contract`, `loop.two-renders` | verified |
| faq.negative-budget | Past a canvas width, three framebuffers plus the texture budget exceed 4 MiB and the default budget goes negative; `ps2ui-check`'s VRAM label then names the unusable default and prints two note lines; a declared `vramBudget` suppresses both | `_facts/cli/ps2ui-check.md` `check.vram.default`; `_facts/authoring/vram-budget.md` `vram.impossible-default`, `vram.impossible-default.check` | restates `check.vram.default`, `vram.impossible-default`, `vram.impossible-default.check` | verified |
| faq.payload-column | `payload` is the raw byte count (`len(data)` baked, the reservation streamed); it is the exact `len` `ps2ui_tex_set` wants; the page-rounded `in pages` figure is `PS2UI_ERR_SIZE` if passed instead | `_facts/authoring/vram-budget.md` `vram.breakdown.columns`, `vram.payload-vs-pages` | restates `vram.breakdown.columns`, `vram.payload-vs-pages` | verified |
| faq.which-arena-number | `ps2ui-check` prints two arena figures because `GSTEXTURE` holds pointers, wider on a 64-bit host than on the EE; the EE figure is what `ps2ui-bake` also prints and what ships | `_facts/cli/ps2ui-check.md` `check.arena-note`, `check.arena-note.bake` | restates `check.arena-note`, `check.arena-note.bake` | verified |
| faq.weight-axis | There is no weight axis: two faces only, and `font-weight` at or above 600 selects bold, below it selects regular | `_facts/authoring/text-and-fonts.md` `fonts.weight-rule` | restates `fonts.weight-rule` | verified |
| faq.nbsp | `&nbsp;` decodes to U+00A0, then the whitespace-collapse pass matches it as ordinary whitespace and turns it into U+0020; a no-break space cannot be authored | `_facts/authoring/html.md` `html.whitespace.nbsp` | restates `html.whitespace.nbsp` | verified |
| faq.slot-cut | `ps2ui_slot_set` copies at most `data-slot-capacity` bytes (default 63), then drops a trailing partial UTF-8 sequence; capacity counts bytes, not characters | `_facts/authoring/dynamic-text.md` `slot.capacity.default`, `slot.capacity.bytes` | restates `slot.capacity.default`, `slot.capacity.bytes` | verified |
| faq.slot-name-collision | Slot names resolve over the whole blob, not per screen; the baker refuses a name repeated on two screens at bake time, naming both | `_facts/authoring/dynamic-text.md` `slot.rules.unique-in-blob`; `_facts/authoring/screens-and-overlays.md` `screens.scope` | restates `slot.rules.unique-in-blob`, `screens.scope` | verified |
| faq.test-target | `make -C runtime test` chains `syntax-check`, `timing-check` and `test-narrow` before `test_runtime`; `make -C runtime syntax-check CC=clang` checks the same sources under a second compiler; there is no `test-compat` target | `_facts/runtime/integrating.md` `integrate.test-targets`, `integrate.test.output` | restates `integrate.test-targets`, `integrate.test.output` | verified |
