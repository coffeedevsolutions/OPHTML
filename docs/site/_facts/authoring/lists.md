# facts: authoring/lists

Session commands were run from the repository root. `<scratch>` is
`/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/authoring-lists`.

Commands behind the rows below:

- `ps2ui fontgen fonts/vendor/DejaVuSans.ttf fonts/vendor/DejaVuSans-Bold.ttf` then
  `ps2ui build` in `<scratch>/browser`, a copy of the tutorial project from
  `docs/tutorial-uc3.md` steps 1 to 5. Printed
  `ps2ui-layout: 14 paint commands, 6 focusables -> build/library.json` and
  `ps2ui-bake: 1 screen(s), 24 records, 2 textures (32 KiB baked), 1 CLUTs -> build/ui.uib`.
- Six error compiles under `<scratch>/err`, each
  `ps2ui-layout <f>.html base.css --fonts fonts/fonts.json -o <f>.json`.
- `node --test test/layout.test.js` in `packages/layout`: `# tests 71`, `# pass 71`, `# fail 0`.
- `make -C runtime test`: `PASS: 5 checks, 0 failure(s)` (test_narrow) and
  `PASS: 410 checks, 0 failure(s)` (test_runtime). "ok N" below is the test_runtime numbering as printed.
- `<scratch>/refill.c`, a host program that loads `<scratch>/browser/build/ui.uib`, binds
  `ps2ui_list_init(&list, "row-", 6)` and runs the refill loop. Compiled with
  `cc -std=c99 -Wall -Wextra -Werror -O2 -Iruntime -Iruntime/stub -Iruntime/vendor/gsKit -Iruntime/vendor/host-shim runtime/ps2ui.c runtime/stub/gskit_stub.c refill.c runtime/build/gsKit_texture_size.o -o refill`, exit 0.

Parent facts reused without restatement: `html.repeat.root`, `html.repeat.substitution`,
`html.example.tutorial`, `html.attributes`, `api.functions.list_init`,
`api.functions.list_set_count`, `api.functions.list_move`, `api.functions.list_select`,
`api.functions.list_item_at`, `api.functions.list_selected_row`,
`api.functions.list_apply_visibility`, `api.structs.list-public`, `api.scope.rules.list`,
`api.ordering.set-count-after-init`, `api.ordering.apply-visibility`,
`api.constants.list-name-max`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| repeat.rules | `data-repeat="N"` stamps N copies of the element in place. N is a literal decimal in 1..256. `{i}` and `{n}` substitute inside the subtree. A repeat inside a repeat is refused, and `data-repeat` on the root element is refused | packages/layout/src/repeat.js:21, :24-26, :59-65, :77-98 | the five compiles and the warning compile listed in the rows below; layout.test.js ok 57-64 all pass | verified |
| repeat.rules.count | The count is bounded by `MAX_REPEAT` 256. `data-repeat="0"` and `data-repeat="300"` both print `layout: <div> line 2: data-repeat="<N>" is out of range 1..256. Every copy costs commands, and a focusable one costs a focus node, so this is a budget you want to feel.` and exit 1 | packages/layout/src/repeat.js:21, :84-90 | compiled `<scratch>/err/r0.html` and `<scratch>/err/r300.html`; both printed that line, exit 1; layout.test.js:883 `repeat: an out-of-range count names the budget` passes as ok 60 | verified |
| repeat.rules.literal | The count must match `/^\d+$/`. Anything else prints `layout: <div> line 2: data-repeat="<raw>" is not a whole number. The count is baked, so it cannot come from data.` and exits 1 | packages/layout/src/repeat.js:77-82 | compiled `<scratch>/err/r2.5.html`: `error: layout: <div> line 2: data-repeat="2.5" is not a whole number. The count is baked, so it cannot come from data.`, exit 1; layout.test.js:876 passes as ok 59 | verified |
| repeat.rules.substitution | `{i}` is the 0-based copy index and `{n}` the 1-based one, substituted in every attribute value and every text node of the subtree. `data-repeat` itself is dropped from the copy | packages/layout/src/repeat.js:24-26, :28-44, :34 | parent fact `html.repeat.substitution`; re-verified this session by the tutorial compile, whose IR carries focus nodes `row-0` to `row-5` and slots `name-0`..`name-5`, `size-0`..`size-5` (parent fact `html.example.tutorial`) | verified |
| repeat.rules.nesting | A `data-repeat` element whose subtree holds another `data-repeat` prints `layout: <div> line 2: data-repeat inside data-repeat. Only one index is in scope, so the inner {i} would be ambiguous.` and exits 1 | packages/layout/src/repeat.js:93-98 | compiled `<scratch>/err/nested.html`: that line, exit 1; layout.test.js:894 passes as ok 61 | verified |
| repeat.rules.root | `data-repeat` on the root element prints `layout: <div> line 1: data-repeat on the root element has nothing to repeat into; put it on a child` and exits 1 | packages/layout/src/repeat.js:60-65 | parent fact `html.repeat.root`; re-verified by `<scratch>/err/root.html`, exit 1 | verified |
| repeat.warn.no-index | A count above 1 with no `{i}` or `{n}` anywhere in the subtree's attributes or text warns once and compiles. The serialiser walks descendant attributes, so `{i}` on a nested `data-slot` alone silences it | packages/layout/src/repeat.js:103-109, :127-131 | compiled `<scratch>/err/noindex.html`: `warning: layout: <div> line 2: data-repeat="3" but no {i} or {n} anywhere inside, so every copy is identical. Add {i} to the ids and data-slot names, or the copies cannot be told apart.` then `ps2ui-layout: 7 paint commands, 0 focusables -> noindex.json`, exit 0; layout.test.js:904 and :920 pass as ok 62 and ok 63 | verified |
| repeat.warn.duplicate-slot | A `data-slot` with no `{i}` under a repeat above 1 is a hard error from the display-list builder, not a rename: `layout: duplicate data-slot name "title"`, exit 1 | packages/layout/src/repeat.js:16-19 (comment); packages/layout/test/layout.test.js:930-937 | compiled `<scratch>/err/dupslot.html`: `error: layout: duplicate data-slot name "title"`, exit 1 | verified |
| repeat.consumed | Expansion runs on the element tree before any style is computed, and `data-repeat` never reaches the cascade | packages/layout/src/index.js:149-151; packages/layout/src/repeat.js:34 | layout.test.js:939 `repeat: data-repeat never reaches the cascade as an attribute` passes as ok 64 | verified |
| repeat.identical | A repeated row compiles to exactly the commands a typed-out row compiles to; nothing downstream can tell a copy from an original | packages/layout/src/index.js:149-151 | layout.test.js:856 `repeat: copies lay out as if they had been typed out` asserts `deepEqual(templated.commands, written.commands)` and passes as ok 58 | verified |
| list.model | A `ps2ui_list` is index arithmetic over already-baked rows. It touches no blob table and needs no format version. `prefix`, `rows`, `count`, `top` and `sel` are public fields | runtime/ps2ui.h:757-781 | parent fact `api.structs.list-public`; ok 138 `list starts empty` reads `list.rows`, `list.count` and `list.top` directly | verified |
| list.api.semantics | The seven list functions divide as: `list_init` binds, `list_set_count` tells it how many items exist, `list_move` and `list_select` move the selection, `list_item_at` and `list_selected_row` report, `list_apply_visibility` hides the rows past the end. The rows below give one per function | runtime/ps2ui.h:786, :794, :802, :806, :811, :815, :822 | ok 138-156 and ok 291-300 | verified |
| list.api.semantics.init | `ps2ui_list_init(list, prefix, rows)` binds a list to `rows` baked rows whose focus names are `prefix` followed by the decimal row index. It stores the prefix pointer without copying, and sets `count`, `top` and `sel` to 0 | runtime/ps2ui.h:783-786; runtime/ps2ui.c:1685-1693 | ok 138 `list starts empty`; ok 139 `empty list has no item in row 0`; ok 140 `empty list has no selected row`. The no-copy is code-only: `list->prefix = prefix` at ps2ui.c:1689 and no test outlives the string | verified |
| list.api.semantics.set_count | `ps2ui_list_set_count(ctx, list, count)` sets the item count. A count of 0 resets `sel` and `top` to 0 and returns. Otherwise `sel` clamps to `count-1`, the window scrolls into view, and focus moves to the clamped row. Passing `NULL` for `ctx` moves the indices and leaves focus alone | runtime/ps2ui.h:788-794; runtime/ps2ui.c:1695-1703 | ok 154 `shrinking the list pulls the selection and window back in`; ok 155 `emptying the list resets it`; ok 299 `shrinking a list never leaves focus on a hidden row`; ok 300 `and focus follows the clamped selection`. Every check from ok 142 to ok 155 passes `NULL` for `ctx` | verified |
| list.api.semantics.move | `ps2ui_list_move(ctx, list, delta)` adds `delta` to `sel` in `long`, clamps to 0..count-1, then does what `list_select` does. It returns 1 when `sel` or `top` changed, 0 otherwise, and 0 for an empty list. A page is `delta = list->rows` | runtime/ps2ui.h:796-802; runtime/ps2ui.c:1750-1758 | ok 141 `moving in an empty list does nothing`; ok 148 `moving past the end clamps`; ok 150 `moving down at the end is a no-op`; ok 151 `moving past the start clamps`; ok 152 `moving up at the start is a no-op`; ok 153 `a page down moves one screenful`; ok 292 `list_move focuses the row the selection lands on` | verified |
| list.api.semantics.select | `ps2ui_list_select(ctx, list, item)` selects an absolute item index. An index at or past `count` clamps to `count-1`. It scrolls the window, syncs focus, and returns 1 when `sel` or `top` changed. It returns 0 for an empty list | runtime/ps2ui.h:804-806; runtime/ps2ui.c:1738-1748 | ok 293 `list_select focuses by absolute index`; ok 154 (a select past the end followed by a shrink) | verified |
| list.api.semantics.item_at | `ps2ui_list_item_at(list, row)` returns `top + row`, or -1 when `row` is at or past `rows`, or when `top + row` is at or past `count`. It is the refill loop's test for a row to blank | runtime/ps2ui.h:808-811; runtime/ps2ui.c:1705-1712 | ok 139; ok 142 `rows past the end of a short list report -1`; ok 147 `row 0 now shows item 1`; ok 149 `the last item is on the last row`; `<scratch>/refill` printed `row 2 item_at -1` through `row 5 item_at -1` at count 2 | verified |
| list.api.semantics.selected_row | `ps2ui_list_selected_row(list)` returns `sel - top` when that lands in 0..rows-1, else -1. An empty list and a list with 0 rows both return -1 | runtime/ps2ui.h:813-815; runtime/ps2ui.c:1714-1727 | ok 140 `empty list has no selected row`; ok 144 `selection reaches the last visible row without scrolling`; ok 146 `the selection stays on the bottom row while scrolling` | verified |
| list.api.semantics.apply_visibility | `ps2ui_list_apply_visibility(ctx, list)` walks rows 0..rows-1 and calls `ps2ui_visible_set` on each row name with `item_at(row) >= 0`. Rows past the end are hidden, the rest are shown. It returns nothing and does nothing when `ctx` or `list` is NULL | runtime/ps2ui.h:817-822; runtime/ps2ui.c:1760-1770 | ok 297 `apply_visibility hides only the rows past the end`; ok 298 `a short list draws less than a full one`; `<scratch>/refill` printed `visible 1` for rows 0 and 1 and `visible 0` for rows 2 to 5 at count 2 | verified |
| list.window.minimum-scroll | The window slides the minimum distance that puts `sel` back on screen, never centring. `sel < top` pulls `top` down to `sel`; `sel >= top + rows` pushes `top` to `sel - rows + 1` | runtime/ps2ui.c:1663-1681 | ok 144 `selection reaches the last visible row without scrolling`; ok 145 `the fifth step scrolls by exactly one row`; `<scratch>/refill` on a 6-row, 9-item list printed `top=0 sel=5` after five moves and `top=3 sel=8` after a page down | verified |
| list.window.no-tail-gap | When `count > rows`, `top` is capped at `count - rows`, so the last screenful never shows blank rows above real items. When `count <= rows`, `top` is forced to 0 | runtime/ps2ui.c:1673-1680 | ok 148 `moving past the end clamps` leaves `top == 6` on a 10-item, 4-row list; `<scratch>/refill` printed `top=3` on a 9-item, 6-row list at the end | verified |
| list.window.no-wrap | A list clamps at both ends and never wraps. Moving down on the last item and up on the first both return 0 | runtime/ps2ui.h:796-801; runtime/ps2ui.c:1752-1756 | ok 150 `moving down at the end is a no-op`; ok 152 `moving up at the start is a no-op` | verified |
| list.focus.sync | After any index change, `list_sync_focus` builds the row name from `prefix` plus the selected row and calls `ps2ui_focus_set`. Focus therefore always sits on the row the selection occupies | runtime/ps2ui.c:1647-1661, :1729-1736 | ok 292 `list_move focuses the row the selection lands on`; ok 293 `list_select focuses by absolute index`; `<scratch>/refill` printed `focus=row-5` at `sel=5` and `focus=row-1` after the count shrank to 2 | verified |
| list.focus.prefix-mismatch | A prefix that matches no baked focus name fails silently. `list_sync_focus` returns `ps2ui_focus_set`'s 0, and both callers discard it, so the indices move and `list_move` still returns 1 while focus stays where it was | runtime/ps2ui.c:1702, :1729-1736, :1746 | ok 156 `a prefix that matches no node still tracks indices`: a `"save-"` list driven against the memcard blob, whose saves screen has no `save-N` nodes, returns 1 from `list_move` with `rows.sel == 1` | verified |
| list.rows.name-buffer | Row names are built into a `char[PS2UI_LIST_NAME_MAX]` stack buffer, 64 bytes, and `list_row_name` truncates both the prefix and the digits at that bound. The index is emitted from at most six digits | runtime/ps2ui.h:301; runtime/ps2ui.c:1647-1661 | parent fact `api.constants.list-name-max`; code-only here too: no test uses a prefix near 64 bytes | code-only |
| list.rows.mismatch | Nothing checks `list->rows` against the number of baked rows. A `rows` larger than the blob has makes `apply_visibility` call `visible_set` on names that do not resolve, which returns 0 and is discarded | runtime/ps2ui.c:1760-1770 | code-only: the runtime has no blob-side row count to compare against, and no test binds an oversized `rows` | code-only |
| list.refill.loop | The app fills row `r` from item `item_at(r)` with `ps2ui_slot_set` after every move, and blanks the row when `item_at` is -1. `ps2ui` owns `top`, `sel` and focus; the app owns the data | runtime/ps2ui.h:768-770; docs/tutorial-uc3.md:383-398 | `<scratch>/refill` ran that loop over the tutorial blob: at `top=3` the six rows read `Ico`, `Katamari Damacy`, `Okami`, `Rez`, `Shadow of the Colossus`, `Vib-Ribbon` | verified |
| list.demo.blob | The tutorial project compiles to 14 paint commands, 6 focusables, 24 records, 2 textures and 13 slots, and its blob asks for a 1516-byte EE arena | docs/site/assets/authoring/lists/demo/ui/library.html; docs/site/assets/authoring/lists/demo/ui/library.css | `ps2ui build` in `<scratch>/browser` printed `ps2ui-layout: 14 paint commands, 6 focusables -> build/library.json`, `ps2ui-bake: 1 screen(s), 24 records, 2 textures (32 KiB baked), 1 CLUTs -> build/ui.uib` and `ps2ui-bake: arena 1516 bytes` | verified |
