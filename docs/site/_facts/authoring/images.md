# facts: authoring/images

Every command was run from the repository root in this session unless a row
says otherwise. `<scratch>` is
`/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/authoring/images`.
Three scratch projects live there: `mini` (one cover, one CSS width), `idx`
(a 32x24 indexed PNG laid out at 64x48) and `sizes` (one PNG at two sizes).
`channel6-ui` is a copy of `examples/channel6/ui` with the `palettize`
attribute removed from the six cover elements, so that `--palettize-images`
is the only difference between the two bakes in `images.cost.psmt8`.
Nothing was written into `examples/channel6/build/`.

`python3 -m unittest discover -s tests -k TestImages -v` in `packages/baker`
printed `Ran 13 tests ... OK`. `node --test packages/layout/test/layout.test.js`
printed 71 pass, 0 fail.

Parent facts reused without restatement: `css.images.no-stretch`,
`css.units`, `css.properties`, `cli.bake.options`, `cli.bake.transcript`,
`cli.bake.summary`, `cli.bake.exit-conditions`,
`cli.bake.exit-conditions.no-write`, `cli.bake.vram.budget-default`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| images.formats | PNG is the only build-time source format. `readPngSize` checks the 8-byte signature, requires `IHDR` as the first chunk, and reads width and height as two big-endian u32s at bytes 16 and 20. The baker emits one of two GS formats: PSMCT32 at 32 bits per texel by default, PSMT8 at 8 bits per texel plus a 256-entry PSMCT32 CLUT under `palettize` | packages/layout/src/image.js:14-41; packages/baker/ps2ui_bake/quads.py:374-385 | compiled `<img src="assets/cover.jpg">` from `<scratch>/mini`: `error: image: "..." is not a PNG — only PNG is supported at build time; convert other formats before compiling`, exit 1; test_baker.py:1916 test_rgba_image_bakes_psmct32_texquad and :1931 test_palettized_image_bakes_psmt8_with_clut both pass in the 13-test run | verified |
| images.src.resolution | A relative `src` resolves against the directory of the HTML file, not the working directory. An absolute `src` is used as written. With no asset base the compile refuses rather than guessing | packages/layout/src/box.js:265-284; packages/layout/src/index.js:332-338 | `ps2ui-layout examples/channel6/ui/games.html examples/channel6/ui/channel6.css --fonts fonts/fonts.json -o <scratch>/games-root.json` from the repository root: every image command carries `/home/user/OPHTML/examples/channel6/ui/assets/cover-*.png`, though the root holds no `assets/` directory; layout.test.js:662 `image: missing src and missing files are compile errors` covers the no-asset-base branch | verified |
| images.sizing | An `<img>` is a replaced element. With no CSS size it measures at its intrinsic pixels plus padding and border. With one axis set the other follows the intrinsic aspect ratio, rounded half up. With both axes set the CSS wins | packages/layout/src/flex.js:236-268 | layout.test.js:572 `image: <img> measures intrinsically and emits an image command` (8x6) and :651 `image: one specified axis keeps the intrinsic aspect ratio` (32x24 from 8x6); compiling `<scratch>/mini` with `.cover { width: 96px }` over a 160x96 source gave `"w": 96, "h": 58` | verified |
| images.sizing.no-stretch | An `<img>` is never stretched by `align-items: stretch` or `align-self: stretch`; the stretch branch is skipped for a box carrying an image | packages/layout/src/flex.js:613-620 | parent fact `css.images.no-stretch`, reused; layout.test.js:572 places an image in a `flex-direction: row` container under the default `align-items: stretch` and it stays 8x6 | verified |
| images.prescale | The baker decodes each image and resizes it to the laid-out size with Pillow's LANCZOS filter before packing texels, so the texture's dimensions are the quad's dimensions. The GS does not scale at draw time here | packages/baker/ps2ui_bake/quads.py:371-373, :437-440 | test_baker.py:2090 test_image_prescaled_to_layout_size asserts a 16x12 source becomes a 32x24 texture at a 32x24 layout; baking `<scratch>/games-plain.json` printed `tex[ 6] PSMCT32    96x58` for a 160x96 cover laid out at 96x58 | verified |
| images.texture.key | The texture cache is keyed by `(src, w, h, palettize, strict)`. Two elements naming the same file at the same size and the same palettize state share one texture; the same file at two sizes is two textures | packages/baker/ps2ui_bake/quads.py:303-317 | baked `<scratch>/sizes`, three `<img>` elements on one file at 64x48, 64x48 and 32x24: `2 textures`, `tex[ 0] PSMCT32    64x48` and `tex[ 1] PSMCT32    32x24`; test_baker.py:2098 test_same_image_same_size_shares_texture | verified |
| images.palettize.rules | `palettize` on an `<img>` bakes that image as PSMT8 with its own 256-entry CLUT. `--palettize-images` does the same for every image in the bake. The flattener passes `attr or self.palettize_all` as the palettize flag and `strict=attr`, so only the attribute is a claim about one asset | packages/layout/src/box.js:287-291; packages/baker/ps2ui_bake/quads.py:437-439; packages/baker/ps2ui_bake/cli.py:152-155, :228 | test_baker.py:2082 test_palettize_all_overrides_per_image; the 13-test run passes; baking `<scratch>/games-plain.json` with `--palettize-images` turned all six `96x58` covers from PSMCT32 to PSMT8 | verified |
| images.palettize.indexed | An already-indexed (P-mode) PNG whose file size equals its laid-out size is baked verbatim: its own index values and its own palette survive. A size mismatch under the `palettize` attribute is an error and no blob is written. The same mismatch under `--palettize-images` is a warning, and the image is requantized | packages/baker/ps2ui_bake/quads.py:331-371 | from `<scratch>/idx`, a 32x24 indexed PNG at `width: 64px; height: 48px`: with `palettize`, `ps2ui-bake: image: '...' is an indexed PNG at 32x24 but is laid out at 64x48. ...`, exit 1, no `ui.uib` written; without the attribute and with `--palettize-images`, `warning: '...' is an indexed PNG at 32x24 laid out at 64x48; --palettize-images requantized it, ...`, exit 0, `tex[ 0] PSMT8      64x48`; test_baker.py:2028 test_indexed_png_refuses_to_be_resized, :2041 test_palettize_all_requantizes_an_indexed_png_rather_than_failing, :2059 test_palettize_all_still_bakes_a_matching_indexed_png_verbatim, :2072 test_indexed_png_without_palettize_is_still_rgba | verified |
| images.palettize.clut | Every CLUT is padded to 256 entries. A P-mode PNG that defines fewer entries keeps its defined ones and the rest are zero. A non-indexed source is quantized with Pillow's FASTOCTREE to at most 256 colours, which keeps alpha in the palette entries | packages/baker/ps2ui_bake/quads.py:290-301, :374-381 | test_baker.py:1995 test_indexed_png_with_a_short_palette_is_padded_to_256 asserts a 4-entry palette still bakes `256 * 4` bytes with the unused entries zero; test_baker.py:1969 test_indexed_png_keeps_its_own_indices_and_palette asserts the same padding | verified |
| images.palettize.clut-per-image | Each palettized image appends its own CLUT; palettized images do not share one. The six covers in the `--palettize-images` bake produced seven CLUTs, one shared glyph-coverage CLUT plus six | packages/baker/ps2ui_bake/quads.py:362-381 | baking `<scratch>/games-plain.json` with `--palettize-images`: `ps2ui-bake: 1 screen(s), 533 records, 15 textures (145 KiB baked), 7 CLUTs`, with `clut[0]` through `clut[6]` in the breakdown; the same bake without the flag printed `1 CLUTs` | verified |
| images.cost.psmt8 | PSMT8 costs a quarter of PSMCT32 per texel, plus 1024 B of CLUT payload per palettized image. One 96x58 cover: `22272 B payload` as PSMCT32, `5568 B payload` as PSMT8. The budget model charges 8 KiB pages, so the same cover moves from `32768 B in pages` to `8192 B`, and its CLUT is charged one page | packages/baker/ps2ui_bake/vram.py:44-56, :155, :230-243 | two bakes of `<scratch>/games-plain.json` in this session, without and with `--palettize-images`: the `tex[ 6]` rows above, and `textures 352256 B of 753664 B budget (46%)` against `textures 253952 B of 753664 B budget (33%)`; test_baker.py:2121 test_vram_savings_reported_for_psmt8 asserts the PSMT8 total is under half the PSMCT32 total | verified |
| images.alpha | An RGBA PNG keeps its alpha. The baker crosses it into the GS 0..128 domain exactly once, in the texels for PSMCT32 and in the CLUT entries for PSMT8 | packages/baker/ps2ui_bake/quads.py:290-301, :382-385 | test_baker.py:1916 asserts `max(tex.data[3::4]) <= 0x80` for PSMCT32 and :1931 asserts the same over the CLUT for PSMT8; both pass in the 13-test run | verified |
| images.zero-size-dropped | An image whose laid-out width or height computes to 0 emits no texture and no draw record. The flattener returns before building one | packages/baker/ps2ui_bake/quads.py:421-426 | compiled `<scratch>/mini` with `.cover { width: 0px }` and baked it: `runtime tables: 0 textures, 0 CLUTs, 0 slots, 1 screens` and `1 screen(s), 1 records, 0 textures (0 KiB baked), 0 CLUTs`, the one record being the panel rect | verified |
| images.errors.layout | The layout stage refuses four image cases, each exit 1: no `src` and no `data-tex-slot`; a file it cannot read (`cannot read "<path>": ENOENT`); a file that is not a PNG; a PNG whose first chunk is not `IHDR` (`malformed PNG (IHDR not first chunk)`) or whose IHDR width or height is 0 (`zero-sized PNG`) | packages/layout/src/image.js:24-40; packages/layout/src/box.js:266-270 | all four triggered from `<scratch>/mini` in this session: a missing file, `cover.jpg`, a copy with bytes 12..16 overwritten with `IEND`, and a copy with the IHDR width set to 0; layout.test.js:662 covers the first three | verified |
| images.errors.bake | A file that passes the PNG header check and still will not decode raises `image: cannot decode <src>: <reason>`, which the CLI reports as a bake failure | packages/baker/ps2ui_bake/quads.py:318-322 | read in source; no test drives it, because a file that reaches the baker has already passed `readPngSize`, and constructing one that satisfies the signature and IHDR yet defeats Pillow is not a case the tree exercises | code-only |
| images.streamed | `<img data-tex-slot="NAME">` reserves a slot the app fills at runtime. It takes no `src`, refuses `palettize`, and needs an explicit `width` and `height` because there is no file to measure. The reservation is PSMCT32 only | packages/layout/src/box.js:226-262; packages/layout/src/flex.js:247-253; packages/baker/ps2ui_bake/quads.py:390-397 | layout.test.js:586 `streamed: data-tex-slot emits a named slot with no src`, :601 `streamed: needs an explicit width and height`, :610 `streamed: src and data-tex-slot are mutually exclusive`, :645 `streamed: palettize is refused, the art does not exist yet`; the file passes 71/71 | verified |
| images.palettize.non-img | `palettize` is read only inside the `<img>` branch of the box builder, so the attribute on any other element is inert. It is not reported as unknown either, because the HTML parser does not validate attribute names against a tag | packages/layout/src/box.js:223, :279-283 | read in source; no test asserts the absence of an effect, and the compiler emits nothing to observe | code-only |
