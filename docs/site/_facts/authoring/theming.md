# facts: authoring/theming

Every command was run from the repository root in this session unless a row
says otherwise. `<scratch>` is
`/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/authoring/theming`.
The compile command is
`ps2ui-layout theme.html <name>.css --fonts fonts/fonts.json -o out-<name>.json`
run from `<scratch>`, where `fonts/` is a copy of the repository's `fonts/`.
`theme.html` is `<ui><div class="row">Library</div></ui>`.

Suites run here: `make -C runtime test` printed `1..410` and
`PASS: 410 checks, 0 failure(s)`. `node --test packages/layout/test/parse.test.js`
printed 39 pass, 0 fail. `node --test packages/layout/test/layout.test.js`
printed 71 pass, 0 fail. `cd packages/baker && python3 -m unittest discover -s tests -k TestTintTable`
printed `Ran 14 tests ... OK`.

The opl-env IR was copied to `<scratch>/opl/` and rebaked there; nothing under
`examples/*/build/` was written.

Parent facts reused without restatement: `css.colors`, `css.colors.unknown`,
`css.border`, `css.opacity`, `css.inherited`, `css.at-rules`,
`cli.bake.transcript.tints`, `check.tints`, `check.tints.names`,
`check.catalogue.32`, `check.catalogue.35`, `api.functions.theme_set`,
`api.ordering.theme-any`, `api.scope.rules.clut-theme`, `uib.header.features`,
`uib.header.features.set`, `uib.records.tint`, `uib.invariants.counts`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| theme.syntax | Three constructs, and nothing else. `:root { --name: <color> }` defines a name. `@theme <ident> { --name: <color> }` gives every name a second value. `var(--name)` reads one at a use site. `:root` is intercepted before the selector parser, so it is a declaration site and never a selector | packages/layout/src/css.js:377, packages/layout/src/css.js:259, packages/layout/src/css.js:494-501 | compiled `<scratch>/theme.css` (the four-line sheet): exit 0, `2 paint commands`; `out-theme.json` carries `themes: ["root","light"]`, `fillVar: "--panel"` and `colorVar: "--ink"` | verified |
| theme.syntax.colours-only | A custom property's value must be a colour. Any other value is an error: `css: line <n>: <name>: "<value>" is not a color, and ps2ui custom properties are colors only -- geometry is baked, so a themeable length would be a different and much larger design` | packages/layout/src/css.js:385-391 | compiled `:root { --gap: 4px }`: that message, exit 1; parse.test.js:249 `var: a non-color custom property is refused at the definition` | verified |
| theme.syntax.root-ordinary | An ordinary property inside `:root` is a warning and is dropped: `css: line <n>: ":root { <prop>: ... }" is ignored -- :root defines custom properties on this target, not inherited style`. There is no inherited-root cascade on this target | packages/layout/src/css.js:379-383 | compiled `:root { color: #ffffff; --panel: #12182a }`: that warning, exit 0, 2 paint commands; parse.test.js:274 | verified |
| theme.syntax.outside-root | A custom property declared on any other selector is a warning and does not define: `css: line <n>: custom property "<name>" outside :root is ignored -- ps2ui resolves names globally, so an element-scoped value would silently mean the :root one` | packages/layout/src/css.js:407-413 | compiled `.row { --panel: #12182a; padding: 32px }`: that warning, exit 0, `1 paint commands`; parse.test.js:256 | verified |
| theme.syntax.at-theme-head | The head must match `@theme <ident>` exactly, the identifier starting with a letter. Anything else is an error: `css: line <n>: "<head>" -- @theme takes one identifier, e.g. "@theme light". A theme is selected by index at runtime and the name exists so a human can say which index they meant, so an unparseable one is refused rather than numbered`. An unterminated block is `css: line <n>: unterminated @theme block` | packages/layout/src/css.js:335, packages/layout/src/css.js:263, packages/layout/src/css.js:268-272 | compiled `@theme 1 { --panel: #ffffff }`: that message, exit 1; compiled a block with no closing brace: `unterminated @theme block`, exit 1 | verified |
| theme.syntax.at-theme-body | A theme block holds custom properties only. Any other property is `css: line <n>: @theme <name> { <prop>: ... } -- a theme supplies custom properties and nothing else. Sizes, fonts and layout are baked geometry; a themeable length would be a different and much larger design`. Setting one name twice in one block is `css: line <n>: @theme <name> sets <prop> twice` | packages/layout/src/css.js:521-526, packages/layout/src/css.js:447-448 | compiled `@theme light { padding: 4px }` and `@theme light { --panel: #ffffff; --panel: #eeeeee }`: both messages, exit 1 each; parse.test.js:339 | verified |
| theme.syntax.at-theme-unknown-name | A theme that sets a name `:root` does not define is an error: `css: line <n>: @theme <name> defines <prop>, which :root does not. A name only reaches a use site through :root, so this value could never be drawn` | packages/layout/src/css.js:527-531 | compiled `:root { --panel: ... }` with `@theme light { --ink: #ffffff }`: that message, exit 1; parse.test.js:323 | verified |
| theme.syntax.at-theme-duplicate | Two blocks with one name is an error: `css: line <n>: @theme <name> is declared twice`. The name `root` is reserved, and reusing it appends ` -- "root" is the theme :root defines, and it is always index 0` | packages/layout/src/css.js:506-512 | compiled two `@theme light` blocks: `error: css: line 3: @theme light is declared twice`, exit 1; parse.test.js:332; the `root` suffix is a source branch no compile here triggers | verified (duplicate); code-only (the `root` suffix) |
| theme.syntax.omitted-name | A theme that omits a name keeps the `:root` value and warns: `css: line <n>: @theme <name> does not set <prop> (defined at line <m>), so it keeps the :root value -- a theme that covers some of the palette recolours some of the screen`. The vector is still full width | packages/layout/src/css.js:544-555 | compiled a two-name `:root` with a one-name `@theme light`: that warning, exit 0; the same file under `--strict` printed `ps2ui-layout: --strict: 2 warning(s)`, exit 1; parse.test.js:313 | verified |
| theme.var.whole-value | `var(--name)` must be the entire declaration value. `VAR_USE` is anchored, so a value with anything beside the `var()` never matches and falls through to `parseColor`, which refuses it by name | packages/layout/src/css.js:579, packages/layout/src/css.js:501-515 | compiled `background: var(--panel) solid`: `error: css: line 2: background: bad color "var(--panel) solid" (flat colors only — gradients are a texture you bake yourself)`, exit 1; compiled `background: linear-gradient(var(--panel))`: the same shape | verified |
| theme.var.border-exception | The `border` shorthand is the one place a `var()` sits beside other tokens. The shorthand splits outside parentheses and recognises a `var(` token, so `border: <n>px solid var(--name)` sets a themed border colour | packages/layout/src/css.js:853-861 | compiled `.row { border: 2px solid var(--edge) }` in a two-theme sheet: exit 0, the rect carries `borderColorVar: "--edge"` and `borderColorThemes: [[124,155,224,255],[47,95,192,255]]`; parse.test.js:206 `var: one name across three properties is one role`, :378 | verified |
| theme.var.no-fallback | A fallback is refused: `css: line <n>: <prop>: var(<name>, ...) has a fallback. A role is one colour per theme; a fallback is a second value for the same name and there is nothing here to choose between them`. A malformed remainder is `css: line <n>: <prop>: malformed var(<name><rest>)` | packages/layout/src/css.js:601-610 | compiled `background: var(--panel, #ffffff)`: that message, exit 1; parse.test.js:240; the malformed branch is a source path no compile here reaches | verified (fallback); code-only (malformed) |
| theme.var.undefined | An undefined name is an error, never a silent literal: `css: line <n>: <prop>: <name> is not defined in :root. Undefined names are refused rather than falling back to a literal, because a theme that cannot reach a colour is exactly the defect this mechanism exists to prevent` | packages/layout/src/css.js:611-617 | compiled `.row { color: var(--nope) }`: that message, exit 1; parse.test.js:232 | verified |
| theme.index | Theme 0 is what `:root` defines and is always present. `@theme` blocks take indices 1 upward in source order. The IR's `themes` array carries the names, index-ordered, `root` first, and its length is the width of every `*Themes` vector | packages/layout/src/css.js:418; packages/layout/src/index.js:220, packages/layout/src/index.js:258 | `out-theme.json` printed `themes: ["root","light"]` with `fillThemes: [[18,24,42,255],[255,255,255,255]]`; parse.test.js:287, :346 | verified |
| theme.names.build-time | Names never reach the console. The blob stores `n_theme` and a theme-major tint table; no `var()` name and no theme name is written | packages/baker/ps2ui_bake/uib.py:377-378, packages/baker/ps2ui_bake/uib.py:450-452; packages/baker/ps2ui_bake/check.py:646-658 | a byte scan of `examples/opl-env/build/ui.uib` for `light`, `root` and `--accent` printed `False False False` | verified |
| theme.tint-table.keying | An entry is keyed on the pair (var name, whole theme vector). Two use sites of one name collapse to one entry. Two literals that agree collapse to one entry but never into a named one. Two names holding one colour stay two entries. `opacity` folds into alpha before the key is formed, so one name at two opacities is two entries | packages/baker/ps2ui_bake/uib.py:221-268 (`_tint`), packages/baker/ps2ui_bake/uib.py:197-219 (`_vector`); packages/layout/src/paint.js:30-33 (`foldAlpha`) | `ps2ui-bake <scratch>/op.json --tints` on a sheet with `background: var(--panel)` and `background: var(--panel); opacity: 0.5` printed 3 entries: `0 (51,102,153,128) --panel`, `2 (51,102,153,64) --panel`; test_baker.py:2696 `test_a_name_is_a_role_and_a_literal_is_not`, :2925 `test_two_names_on_one_value_are_two_entries`, :2987 `test_one_name_is_not_one_entry_when_opacity_folds_in`, :2944 `test_a_slot_keys_on_its_name_too` | verified |
| theme.tint-table.slots | Colour lives in two tables. Slot base and focus colours intern through the same `_tint`, with their own `color_base_var` and `color_focus_var` names | packages/baker/ps2ui_bake/uib.py:279-287 | `ps2ui-check --tints examples/opl-env/build/ui.uib` printed `cmd, slot` in the `where` column for entries 1, 4 and 10; test_baker.py:2743; runtime test ok 397 | verified |
| theme.tint-table.shared-role | One name used at many sites is one entry. `var(--accent)` appears nine times in `examples/opl-env/ui/opl.css` and is entry 8 of a 28-entry table | examples/opl-env/ui/opl.css; packages/baker/ps2ui_bake/uib.py:221-268 | `grep -c "var(--accent)" examples/opl-env/ui/opl.css` printed 9; the rebake of the opl-env IR printed `8  ( 62, 78,112,128)  ( 24, 48, 96,128)  --accent` and `# 28 tint entries over 2 theme(s): root, light` | verified |
| theme.tint-table.literal | A literal has no name and gets a full-width vector of itself. It is one entry, fixed in every theme, and no theme can move it | packages/layout/src/css.js:586-600, packages/layout/src/css.js:625-628; packages/baker/ps2ui_bake/uib.py:197-219 | the opl-env rebake printed `9  (128,128,128,128)  (128,128,128,128)  (literal, unthemed)   FIXED in every theme`; parse.test.js:196, :301 (`@theme: a literal gets a full-width vector of itself`) | verified |
| theme.tint-table.ceiling | Interning raises `more than 65536 distinct tints` once an index passes 0xFFFF; `n_tint` is a uint16 | packages/baker/ps2ui_bake/uib.py:262-266 | no blob in this tree comes near the ceiling, and no test drives it | code-only |
| theme.literal-warning | In a sheet that declares a theme, a colour literal on `color`, `background`, `background-color`, `border-color` or `border` warns once per authored declaration: `css: line <n>: <prop>: "<token>" is a literal in a sheet that declares a theme, so no theme can move it -- name it in :root, or leave it if staying fixed is deliberate`. `none` and `transparent` are exempt. A sheet with no `@theme` never warns | packages/layout/src/css.js:422-423, packages/layout/src/css.js:375-401 | compiled a themed sheet with `color: #8b94a7`: that warning, exit 0; the same sheet with the `@theme` block removed warned not at all; parse.test.js:352, :368 | verified |
| theme.diagnostics | The full set this page tabulates: the seven errors (non-colour custom property, unparseable `@theme` head, unterminated `@theme` block, non-custom property in a theme, unknown name in a theme, name set twice in a theme, duplicate theme name; plus `var()` fallback and undefined name) and the four warnings (ordinary property in `:root`, custom property outside `:root`, a theme omitting a name, a literal in a themed sheet) | packages/layout/src/css.js:379-405, packages/layout/src/css.js:421-470, packages/layout/src/css.js:516-532, packages/layout/src/css.js:375-401 | each message above was produced by a compile in this session; the exact strings are quoted in the rows for `theme.syntax.*`, `theme.var.*` and `theme.literal-warning` | verified |
| theme.lints.per-theme | The linter runs once per theme. Colour lints (`contrast`, `ntsc-red-bleed`) are reported for every row that fails, even when the message is identical. Geometry lints are deduplicated by rule against row 0. A lint from row `t` is prefixed `@theme <name>: ` | packages/layout/src/index.js:265-276, packages/layout/src/index.js:272, packages/layout/src/index.js:281-289 | a compile whose light theme kept a dark `:root` ink printed `warning: @theme light: contrast: "Library" contrast 1.29:1 < 3:1 — CRTs crush shadows harder than your monitor` with no unprefixed twin; layout.test.js:1002, :1021, :1078; the file passes 71/71 | verified |
| theme.structure.row0 | Whether a command exists is decided by theme 0 alone. A fill that is transparent in `:root` emits no rect even when a theme gives it a colour | packages/layout/src/paint.js:59-71 | compiled `:root { --ghost: rgba(1,2,3,0) }` with `@theme light { --ghost: #ff0000 }` and `background: var(--ghost)`: `1 paint commands`, and the IR holds a `text` op and no `rect`; layout.test.js:988 | verified |
| theme.seeing.bake | `ps2ui-bake --tints` prints the table as it writes it, one row per entry, one RGBA column per theme, the `var()` name or `(literal, unthemed)`, and `FIXED in every theme` for an entry no theme moves. The header is `# N tint entries over M theme(s): <names>` | packages/baker/ps2ui_bake/cli.py:289-303 | `ps2ui-bake landing.json library.json detail.json filters.json recent.json confirm.json -o opl.uib --tints` run from `<scratch>/opl` printed `# 28 tint entries over 2 theme(s): root, light` and 28 rows | verified |
| theme.seeing.check | `ps2ui-check --tints` prints what a loader finds: no names, a `where` column of `cmd`, `slot`, `UNREFERENCED`, and `fixed in every theme`. It exits 0 without running the check catalogue | packages/baker/ps2ui_bake/check.py:646-705, packages/baker/ps2ui_bake/check.py:736-738 | `ps2ui-check --tints examples/opl-env/build/ui.uib` printed `# 28 tint entries over 2 theme(s)`, the `where` column, exit 0 | verified |
| theme.seeing.preview | `preview.render(uib, screen=NAME, theme=N)` renders one row on the host. `ps2ui-bake --preview` renders theme 0 only; it calls `render(uib)` with no `theme`. `ps2ui serve` has a Theme control, disabled below two themes, and a `--theme` option | packages/baker/ps2ui_bake/preview.py:126-138; packages/baker/ps2ui_bake/cli.py:337; packages/baker/ps2ui_bake/serve_page.html:123, packages/baker/ps2ui_bake/serve_page.html:586-587; packages/baker/ps2ui_bake/serve.py:752 | rendered `docs/site/assets/authoring/theming/library-theme-0.png` and `library-theme-1.png`; `python3 tools/check-site-assets.py` printed `ok 6` and `ok 7`; `ps2ui serve --help` lists `--theme THEME  the theme row` | verified |
| theme.runtime | `int ps2ui_theme_set(ps2ui_ctx *ctx, unsigned theme)` writes `ctx->theme` and returns `PS2UI_OK`, `PS2UI_ERR_BOUNDS` for a null context, or `PS2UI_ERR_RANGE` when `theme >= ctx->hdr->n_theme`. A refused call leaves the live row where it was. No GSGLOBAL, no transfer, no invalidation: the next `ps2ui_render` reads the row. It is callable before `ps2ui_upload` and survives one | runtime/ps2ui.h:576-608; runtime/ps2ui.c:780-794 | `make -C runtime test`: ok 385-390, ok 393, ok 397, ok 398, ok 400; parent fact `api.functions.theme_set` | verified |
| theme.runtime.feature-bit | A blob with `n_theme > 1` must carry `PS2UI_FEAT_ROLE_TINTS` (bit 4, 0x10) or `ps2ui_load` returns `PS2UI_ERR_TINTS` (-14). The baker sets the bit from the row count, so every blob it writes with two or more themes carries it. A one-theme blob keeps the bit clear | runtime/ps2ui.h:62, runtime/ps2ui.h:429; runtime/ps2ui.c:292-294; packages/baker/ps2ui_bake/uib.py:372-378 | `make -C runtime test` ok 373, ok 374, ok 391; `ps2ui-check examples/opl-env/build/ui.uib` printed `ok 64 - 2 theme(s) with FEAT_ROLE_TINTS set: more than one row needs the bit, or ps2ui_load refuses the blob`; a header parse of that blob printed `feature_flags=0x1f` | verified |
| theme.runtime.cycle | The sample cycles rows by reading `ui.hdr->n_theme` and calling `ps2ui_theme_set` only when it is above 1 | runtime/sample/main.c:2095-2101 | read in the tree; the sample builds only under the ps2dev cross toolchain, which is not installed here | code-only |
| theme.defect.theme-without-root | A sheet that declares `@theme` while defining no `:root` custom property fails with an internal error instead of a diagnostic. `themeCount` reads the width from the first entry of `vars` and returns 1 for an empty map, while `themeNames` is already 2 long, so the first painted colour trips the width guard | packages/layout/src/css.js:631-635; packages/layout/src/paint.js:41-47 | compiled `@theme light { }` with a literal background and no `:root`: `error: layout: internal: background has 1 theme values, expected 2`, exit 1. Adding one `:root` name to the same sheet compiles clean | verified |
| theme.defect.stale-header-comment | **FIXED.** `runtime/ps2ui.h` said the live row is "Still 0 on every blob this toolchain can currently bake, since more than one row needs PS2UI_FEAT_ROLE_TINTS and no baker sets it yet". The baker does set it, and the comment now says so and carries the two blobs' measured flags | runtime/ps2ui.h:359-362 vs packages/baker/ps2ui_bake/uib.py:377-378 | the opl-env header parse printed `feature_flags=0x1f` with `n_theme` 2, and `ps2ui-check` ok 64 confirms the bit; not a drift row the brief listed | verified |
| theme.readme.absent | **Fixed in the README, and this row did not know.** It read "README.md's 'Supported CSS' list names no custom properties, no `var()` and no `@theme`, so the whole authoring mechanism is undocumented there". The list names all three: `- Custom properties on `:root`, `var()`, and `@theme` for a second palette`. Found when F41(a) made repo-root citations visible and the line this row cited turned out to be four sections above the list | README.md:228; packages/layout/src/css.js:377, packages/layout/src/css.js:259, packages/layout/src/css.js:494 | the line quoted above, read from README.md on this tree; every construct compiled in the audit session; drift row D7 | fixed |
| theme.readme.clut | **FIXED.** README.md:24-26 said "the palette is a CLUT row and switching it is a table swap". A theme is a row of the tint table, not a CLUT. `ps2ui_theme_set` touches no palette and schedules no transfer; `ps2ui_clut_set` is the separate CLUT mechanism and is refused before `ps2ui_upload` | README.md:25-30; runtime/ps2ui.c:780-794 vs runtime/ps2ui.c:739-777; runtime/ps2ui.h:564-575 | `make -C runtime test` ok 385-401 (theme_set, no GSGLOBAL argument) against ok 352-364 (clut_set, refused before upload); parent facts `api.functions.theme_set` and `api.scope.rules.clut-theme` | contradicts-readme |

## follow-up

Not in the drift table, found while verifying:

1. `@theme` with no `:root` custom property anywhere in the sheet produces
   `layout: internal: background has 1 theme values, expected 2` and exit 1
   (`themeCount` in packages/layout/src/css.js:546). The condition is
   diagnosable at parse time: `themeBlocks.length > 0 && vars.size === 0`.
   Row `theme.defect.theme-without-root`.
2. **FIXED.** The comment on `ps2ui_ctx.theme` (runtime/ps2ui.h:359-362)
   said no baker sets `PS2UI_FEAT_ROLE_TINTS`.
   packages/baker/ps2ui_bake/uib.py:377 sets it from `n_theme`, and
   examples/opl-env/build/ui.uib carries it. It now says so and carries
   both blobs' flags. Row `theme.defect.stale-header-comment`.
3. **FIXED.** README.md:24-26 described a theme as a CLUT row. The two
   mechanisms are distinct, have different signatures and different
   ordering rules, and the paragraph now says which is which.
   Row `theme.readme.clut`.
