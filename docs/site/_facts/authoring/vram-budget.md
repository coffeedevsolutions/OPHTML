# facts: authoring/vram-budget

Session notes. Every line number below was re-located by symbol in this
session. Scratch work lives under
`scratchpad/authoring/vram-budget/`: a bake of
`examples/memcard/build/*.json`, a scratch compile of
`fixtures/bench-stream/ui` (its `build.sh` was not run), a scratch copy
of the memcard sources carrying `"vramBudget": 262144`, and a scratch
screen compiled with `--canvas 796x448`. Nothing under `examples/*/build/`
or `fixtures/*/build/` was written.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| vram.total | `VRAM_TOTAL` is `4 * 1024 * 1024` = 4194304 B and `PAGE_BYTES` is 8192 | packages/baker/ps2ui_bake/vram.py:35-36 | the memcard bake in this session printed `framebuffers assumed: ... = 3440640 B` and `textures 163840 B of 753664 B budget (21%)`, and 3440640 + 753664 = 4194304 | verified |
| vram.default-budget | `default_budget(w, h)` returns `VRAM_TOTAL - 3 * framebuffer_size(w, h)`, where `framebuffer_size` is `page_rounded_size(w, h, PSMCT32)`. At the memcard canvas 640x448 that is 4194304 - 3*1146880 = 753664 B. The three reservations are two CT32 draw/display buffers plus one Z buffer; the tree's own sample runs `ZBuffering` off and holds two, and the default is deliberately sized for the heaviest host instead | packages/baker/ps2ui_bake/vram.py:128-131 (`framebuffer_size`), packages/baker/ps2ui_bake/vram.py:133-152 (`default_budget`) | `ps2ui-bake examples/memcard/build/library.json examples/memcard/build/saves.json -o <scratch>/memcard.uib` printed `framebuffers assumed: 2x draw/display + 1x Z @ 640x448 = 3440640 B` and `textures 163840 B of 753664 B budget (21%)`; packages/baker/tests/test_baker.py:1735 `test_default_budget_subtracts_framebuffers`. Agrees with parent `cli.bake.vram.budget-default` and `check.vram.default` | verified |
| vram.two-models | The file carries two cost models. `page_rounded_size` rounds to 8 KiB pages and is the BUDGET model, deliberately pessimistic. `alloc_size` is a port of `gsKit_texture_size`, charging 256-byte blocks rounded up to an alignment group of 1x1, 2x2, 4x4 or 8x4 blocks; 8x4 is one full page and is the largest case, not the unit. `ps2ui_upload`'s preflight uses the second | packages/baker/ps2ui_bake/vram.py:1-32 (module docstring), packages/baker/ps2ui_bake/vram.py:45-58 (`page_rounded_size`), packages/baker/ps2ui_bake/vram.py:64-98 (`alloc_size`, `BLOCK_BYTES = 256`) | `python3 tools/check-vram-model.py` printed `ok - alloc_size agrees with gsKit_texture_size on 45000 sizes` and `ok - and differs from the page model on 5120 of them, which is why both are reported`; test_baker.py:1722 `test_page_rounding_ct32`, :1730 `test_page_rounding_t8` | verified |
| vram.alloc-total | `alloc_total` mirrors the runtime loop: `alloc_size(w, h, fmt)` per texture plus `alloc_size(16, 16, PSMCT32)` = 1024 B for every PSMT8 texture. The CLUT charge is per PSMT8 texture, not per distinct CLUT | packages/baker/ps2ui_bake/vram.py:100-121; runtime/ps2ui.c:589-595 | memcard printed `allocator 143104 B`; 8 x (16384+1024) + 2 x (256+1024) + (256+1024) = 143104, with 11 CLUT charges for 1 CLUT | verified |
| vram.breakdown.columns | Each texture row is `tex[i] <fmt> <W>x<H> <kind> <N> B payload -> <M> B in pages`. `i` is the index in the blob's texture table. `<fmt>` is `PSMT8` or `PSMCT32`. `<W>x<H>` is texels. `<kind>` is `baked` or `streamed`. `payload` is `len(t.data)` for a baked texture and `t.reservation` for a streamed one. `in pages` is `page_rounded_size`. Each CLUT row is `clut[i] PSMCT32  256 entries        -> 8192 B in pages` and adds 1024 B to the payload total | packages/baker/ps2ui_bake/vram.py:213-242 | memcard bake: `tex[ 2] PSMT8      15x15   baked         225 B payload ->    8192 B in pages` and `clut[0] PSMCT32  256 entries        ->    8192 B in pages`; bench-stream bake: `tex[ 1] PSMCT32   128x128  streamed    65536 B payload ->   65536 B in pages` | verified |
| vram.breakdown.totals | Four trailing lines: `framebuffers assumed: 2x draw/display + 1x Z @ WxH = N B`; `payload P B -> allocator A B -> budget-charged T B`; `reclaimable A-P B (X% of committed) -- ...`; and either `textures T B of B B budget (P%)` or `textures T B, and no budget to charge them to`. The percentage is suppressed when the budget is not positive | packages/baker/ps2ui_bake/vram.py:243-290 | memcard: `payload 132667 B -> allocator 143104 B -> budget-charged 163840 B`, `reclaimable 10437 B (7% of committed)`, `textures 163840 B of 753664 B budget (21%)`; the 796x448 scratch bake printed `textures 24576 B, and no budget to charge them to`; test_baker.py:1871 `test_no_percentage_of_a_negative_budget` | verified |
| vram.payload-vs-pages | `payload` is the number `ps2ui_tex_set` wants for a streamed slot: `len` must equal the entry's reservation exactly, and the page-rounded figure is `PS2UI_ERR_SIZE`. The bench-stream 128x128 PSMCT32 slot reports `65536 B payload`; that is the `len` to pass | runtime/ps2ui.h:477-508 (`ps2ui_tex_set` comment naming `payload`, "the smaller of the two figures there"); packages/baker/ps2ui_bake/vram.py:222-236 | scratch bake of `fixtures/bench-stream/ui` printed four `streamed    65536 B payload ->   65536 B in pages` rows; `make -C runtime test` `ok 323-327` cover the size refusals. Agrees with parent `api.functions.tex_set` | verified |
| vram.payload-vs-pages.equal | The two figures coincide only when the texture already fills whole pages. A 128x128 PSMCT32 slot is 65536 B both ways; an 11x11 PSMT8 texture is 121 B payload against 8192 B in pages | packages/baker/ps2ui_bake/vram.py:45-58 | memcard bake `tex[ 7] PSMT8      11x11   baked         121 B payload ->    8192 B in pages`; bench-stream bake `tex[ 1]` row | verified |
| vram.flag.bake | `ps2ui-bake --vram-budget BYTES` (type int, default None) replaces the default budget; the help reads `texture VRAM budget (default: 4 MiB minus a double-buffered framebuffer pair + Z at canvas size)` | packages/baker/ps2ui_bake/cli.py:159-161, packages/baker/ps2ui_bake/cli.py:258-261 | `ps2ui-bake --help` in this session; `--vram-budget 100000` on memcard printed `textures 163840 B of 100000 B budget (163%)` and exited 1. Agrees with parent `cli.bake.options` | verified |
| vram.flag.check | `ps2ui-check --vram-budget BYTES` (default None) overrides the same budget; the help reads `override the default texture VRAM budget` | packages/baker/ps2ui_bake/check.py:595-609, packages/baker/ps2ui_bake/check.py:712-727 | `ps2ui-check --help` in this session. Agrees with parent `check.options` | verified |
| vram.key.project | New in 0.6.0: the project key `vramBudget` reaches `ps2ui-bake` and `ps2ui-check` alike, so the build and the check agree on one number. A scratch memcard copy with `"vramBudget": 262144` built `textures 163840 B of 262144 B budget (62%)` and `ps2ui check` on the same project printed `ok 60 - VRAM 160 KiB within budget 256 KiB` | packages/baker/ps2ui_bake/ps2ui.py:232-233 (`bake_argv`), packages/baker/ps2ui_bake/ps2ui.py:291-292 (`cmd_check`); CHANGELOG.md:823-854 | both commands run in this session on `<scratch>/proj`; `ps2ui-check` on the same blob with no flag printed `ok 60 - VRAM 160 KiB within budget 736 KiB`; test_baker.py:4313 `test_check_is_given_the_budget_the_build_was_given`. Agrees with parent `project.keys.check` | verified |
| vram.impossible-default | `budget_note(w, h)` returns two lines when `3 * framebuffer_size(w, h) >= VRAM_TOTAL`, and a different pair when `VRAM_TOTAL - 2 * framebuffer_size(w, h) <= 0`. At 448 lines the crossovers are 769 and 1153 columns. Below 769 the note is empty. From 769 the note names the three-framebuffer arithmetic and the budget two buffers leave. From 1153 no budget is offered and the only fix is a narrower canvas | packages/baker/ps2ui_bake/vram.py:158-200 | scratch screen compiled with `ps2ui-layout --canvas 796x448` and baked: the bake printed `the default budget does not exist at this canvas: three framebuffers at 796x448 need 4472832 B of 4194304 B total VRAM, ...` and `declare vramBudget (or --vram-budget) for the layout you actually run: with ZBuffering off the console holds two buffers, not three, which leaves 1212416 B`, then exit 1; `python3 -c "from ps2ui_bake import vram; ..."` in this session printed the first failing widths as 769 and 1153 and the 1153 note `two framebuffers at 1153x448 need 4358144 B of 4194304 B total VRAM, so this canvas cannot be displayed from GS VRAM under any Z setting` / `no budget can be declared for it; a narrower canvas is the only fix`; test_baker.py:1742 `test_an_impossible_default_says_so_instead_of_blaming_textures`, :1772 `test_the_advice_stops_when_two_buffers_stop_fitting` | verified |
| vram.impossible-default.only-default | The note prints only when the budget was inherited, never when a caller passed one. Both `report` and `check_vram` gate on the same condition and call the same function | packages/baker/ps2ui_bake/vram.py:208, packages/baker/ps2ui_bake/vram.py:250-251; packages/baker/ps2ui_bake/check.py:600, packages/baker/ps2ui_bake/check.py:624-626 | the 796x448 bake with `--vram-budget 1212416` printed `textures 24576 B of 1212416 B budget (2%)` and no note, exit 0; test_baker.py:1831 `test_a_checker_given_a_budget_gets_no_note`, :1861 `test_a_declared_budget_gets_no_lecture` | verified |
| vram.impossible-default.check | Under an impossible default `ps2ui-check` appends ` -- the default budget is unusable at this canvas, see notes` to the VRAM label and emits both note lines | packages/baker/ps2ui_bake/check.py:595-626 | `ps2ui-check <scratch>/wide.uib` printed the two `#` note lines and `not ok 40 - VRAM 24 KiB within budget -272 KiB -- the default budget is unusable at this canvas, see notes`, `FAIL: 43 checks, 1 error(s), 0 warning(s)`. This is the branch parent `check.vram.default` and `check.catalogue.50` record as `code-only` for want of a wide blob; a scratch blob at 796x448 reaches it. No parent fact is contradicted | verified |
| vram.bake.refusal | Over budget, the bake prints `error: texture VRAM footprint exceeds budget (see breakdown above; override with --vram-budget)` and returns 1 before any write | packages/baker/ps2ui_bake/cli.py:256-267 (the check sits above the first `write_uib`) | `ps2ui-bake ... --vram-budget 100000` on memcard: that message, exit 1, and the `-o` path did not exist afterwards. Agrees with parent `cli.bake.exit-conditions` and `cli.bake.exit-conditions.no-write` | verified |
| vram.runtime.preflight | `ps2ui_upload` sums `gsKit_texture_size(w, h, psm)` per texture plus `gsKit_texture_size(16, 16, GS_PSM_CT32)` per PSMT8 texture, and returns -1 when `gs->CurrentPointer + need` exceeds 4 MiB. The refusal is all-or-nothing: nothing is transferred and `ctx->uploaded` stays 0. The preflight exists because `gsKit_TexManager_bind` cannot report exhaustion; `_blockAlloc` evicts in a loop that never exits | runtime/ps2ui.c:567-598; runtime/ps2ui.h:468-475 | `make -C runtime test`: `ok 309 - upload reports failure when VRAM is exhausted...`, `ok 310 - and leaves the context not-uploaded...`, `ok 311 - upload refuses a budget that holds some textures but not all of them`, `ok 312 - and transfers nothing at all (0 transfers) -- all-or-nothing...`, `ok 313 - and still leaves it not-uploaded`; `PASS: 410 checks, 0 failure(s)`. Agrees with parent `api.functions.upload` and `api.return-conventions.upload` | verified |
| vram.runtime.streamed-counted | A streamed slot is preflighted like any other texture, so its reservation is charged from the start, but nothing is transferred for it until `ps2ui_tex_set` names texels. An unfilled slot is skipped at bind time | runtime/ps2ui.c:589-595, runtime/ps2ui.c:645-650; runtime/ps2ui.h:468-474 | `make -C runtime test` `ok 328-339`; the bench-stream bake charges the four unfilled slots 262144 B of budget. Agrees with parent `api.ordering.tex-any` | verified |
| vram.runtime.recheck | `ps2ui_render` re-tests `ctx->uploaded && gs->CurrentPointer + ctx->vram_need <= 4 MiB` before any textured draw; when it fails every textured draw is skipped and `stats.vram_lost` is set to 1 | runtime/ps2ui.c:964-989; runtime/ps2ui.h:325-331, runtime/ps2ui.h:390-395 | `make -C runtime test` `ok 36 - the blob costs a nonzero amount of VRAM to state a budget against`; the counter is in parent `api.structs.stats` | verified |
| vram.psmt8 | PSMT8 costs a quarter of PSMCT32 per texel, plus 1024 B of CLUT payload per palettized image; the page-rounded column charges whole 8 KiB pages either way | packages/baker/ps2ui_bake/vram.py:38-42 (`_PAGE_DIMS`), packages/baker/ps2ui_bake/vram.py:60-64 (`_BLOCK_DIMS`) | not re-run here; docs/site/authoring/images.md "What it costs" pastes the two bakes of one IR (22272 B against 5568 B of payload for one 96x58 cover). The page links there rather than restating it | code-only (the measurement belongs to authoring/images and is not repeated in this session) |
| vram.drift.d13 | The arena figure is per blob and per target, not the constant README.md hardcodes. The bakes in this session printed `arena 1662 bytes` for memcard, `arena 1875 bytes` for the bench-stream scratch blob and `arena 1066 bytes` for the 796x448 scratch blob | packages/baker/ps2ui_bake/cli.py:323-332 | three bakes in this session; README.md:182 shows `arena[1662]` as if fixed (drift D13). Agrees with parent `arena.line` and `check.arena-note.bake` | contradicts-readme |
