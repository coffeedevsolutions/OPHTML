# facts: authoring/video-modes

Session commands behind the rows below, all run from the repository root
unless the row says otherwise. `$S` is the scratch directory holding a copy
of `examples/channel6` and a minimal project `mini/`.

- `ps2ui-layout $S/channel6/ui/games.html $S/channel6/ui/channel6.css -o out/<mode>.json --mode <mode>` for each of the four modes, then `python3 -c "import json; print(json.load(open('out/<mode>.json'))['canvas'])"`.
- `ps2ui build $S/channel6/ps2ui.json --mode ntsc16x9 -o build/ui-16x9.uib --preview-display build/preview-16x9-display.png --preview none --montage none`.
- `PYTHONPATH=packages/baker python3 -m ps2ui_bake examples/channel6/build/games-16x9.json examples/channel6/build/probe-16x9.json -o $S/t.uib --preview-display $S/t-display.png`; `cmp` against `examples/channel6/build/preview-16x9-display.png` and `examples/channel6/build/ui-16x9.uib` reported both byte-identical.
- `make -C runtime test` printed `PASS: 5 checks, 0 failure(s)` and `PASS: 410 checks, 0 failure(s)`.
- `cc -std=c99 -Wall -Wextra -Werror -O2 -Istub -Ivendor/gsKit -Ivendor/host-shim -I. ps2ui.c stub/gskit_stub.c $S/par_check.c build/gsKit_texture_size.o -o $S/par_check` from `runtime/`; `par_check` loads each blob and prints `ps2ui_pixel_aspect_x1000`.
- `node --test test/layout.test.js` in `packages/layout` printed `# pass 71`, `# fail 0`.
- `python3 -m unittest test_baker.TestDisplayAspect -v` in `packages/baker/tests` printed `Ran 4 tests ... OK`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| modes.table | `MODES` holds four presets: ntsc 640x448 at 4:3, ntsc16x9 640x448 at 16:9, pal 640x512 at 4:3, pal16x9 640x512 at 16:9. Canvas is the framebuffer, aspect is the panel | packages/layout/src/aspect.js:47-52 | compiled channel6 `games.html` under each `--mode`; `ir.canvas` gave w/h and `displayAspect` exactly as listed | verified |
| aspect.par | `par = (num/den) / (canvasW/canvasH)`, rounded to four places in the IR. ntsc 0.9333, ntsc16x9 1.2444, pal 1.0667, pal16x9 1.4222. `display` is `{w: round(canvasH * num/den), h: canvasH}`: 597x448, 796x448, 683x512, 910x512 | packages/layout/src/aspect.js:33-35,42-44; packages/layout/src/index.js:159,279-285 | the four compiles above printed those `par` and `display` values; packages/layout/test/layout.test.js:784 "aspect: PAR derives from canvas and display ratio" passed in `node --test` | verified |
| aspect.flags | `--mode` sets canvas size and display aspect together. `--canvas WxH` overrides the size; `--display-aspect W:H` overrides the aspect. Both beat `--mode` whatever the argument order, because the bin applies `--mode` first and the two overrides after | packages/layout/bin/ps2ui-layout.js:43-45,66-72,81-86 | `--mode pal --canvas 704x448` gave `{'w': 704, 'h': 448, 'displayAspect': [4, 3], 'par': 0.8485}`; `--mode pal --display-aspect 16:9` gave `{'w': 640, 'h': 512, 'displayAspect': [16, 9], 'par': 1.4222}` | verified |
| aspect.flags.project | The project keys are `mode`, `canvas` and `displayAspect`; `compile_screens` forwards each to `ps2ui-layout` as `--mode`, `--canvas`, `--display-aspect`. `ps2ui build` adds `--mode` only | packages/baker/ps2ui_bake/project.py:39,47-49; packages/baker/ps2ui_bake/ps2ui.py:181-186,148-149,347 | built a scratch project whose only aspect key is `"mode": "ntsc16x9"`; the bake printed `display preview 796x448 at 16:9`; `ps2ui build --help` lists `--mode` and no other aspect flag | verified |
| aspect.flags.not-bake | `--mode`, `--display-aspect` and `--canvas` are compiler flags, not baker flags. `ps2ui-bake` has none of them; `ps2ui build` has `--mode` alone | packages/layout/bin/ps2ui-layout.js:43-45; packages/baker/ps2ui_bake/cli.py:138-160 | `ps2ui build --help` in this session; parents cli.layout.options.readme-flags and cli.bake.options.no-layout-flags record the README lines (drift D4) | contradicts-readme |
| aspect.anamorphic | Widescreen changes the panel ratio and nothing else. `--mode ntsc` and `--mode ntsc16x9` both compile to a 640x448 framebuffer; only `displayAspect`, `par` and `display` differ, and the framebuffer render is unchanged | packages/layout/src/aspect.js:47-52; packages/layout/src/index.js:155-159 | the two compiles printed the same `w`/`h` and 90 paint commands each; `par_check` reported 933 against 1244 at the same 640x448; `cmp docs/site/assets/authoring/video-modes/games-1x1.png examples/channel6/build/preview.png` exited 0, so the 1:1 render of the 16:9 blob is byte-identical to the 4:3 one | verified |
| aspect.mode.unknown | An unknown mode is refused by `ps2ui-layout`, not by the project loader. The usage line is printed and `ps2ui build` exits 1 | packages/layout/bin/ps2ui-layout.js:67; packages/baker/ps2ui_bake/ps2ui.py:181-182 | `ps2ui build mini/ps2ui.json --mode vga` and a project file carrying `"mode": "vga"` both printed the ps2ui-layout usage line then `ps2ui: ps2ui-layout failed on ui/title.html (exit 2)`, exit 1 | verified |
| aspect.preview.1x1 | `--preview` writes framebuffer pixels at canvas size. The channel6 16:9 blob renders 640x448 | packages/baker/ps2ui_bake/cli.py:336-338; packages/baker/ps2ui_bake/preview.py:126 | `preview.render(read_uib('examples/channel6/build/ui-16x9.uib'), screen='games-16x9')` saved a 640x448 PNG | verified |
| aspect.preview.display | `--preview-display` resamples that render to `display_size` with LANCZOS and prints `ps2ui-bake: display preview WxH at N:D -> path`. The channel6 16:9 build prints `796x448 at 16:9` | packages/baker/ps2ui_bake/cli.py:148,339-345; packages/baker/ps2ui_bake/preview.py:95-115 | the scratch 16:9 build printed `ps2ui-bake: display preview 796x448 at 16:9 -> build/preview-16x9-display.png`; the same two IRs baked with `ps2ui-bake --preview-display` produced a PNG byte-identical to the committed `examples/channel6/build/preview-16x9-display.png` | verified |
| aspect.preview.display.noop | `to_display_space` returns the image untouched when the panel size already equals the render size | packages/baker/ps2ui_bake/preview.py:111-115 | packages/baker/tests/test_baker.py:3286 `test_display_space_resamples_only_when_needed` (640x448 at 10:7 is left alone) passed in this session | verified |
| lint.aspect-distortion.threshold | The `aspect-distortion` rule fires once per document when `Math.abs(par - 1) > 0.08`. `maxShapeDistortion` defaults to 0.08 and has no opt-out attribute | packages/layout/src/lint.js:12-27,154 | channel6 `games.html`: silent at `--mode ntsc` (PAR 0.9333) and `--mode pal` (PAR 1.0667), silent at `--display-aspect 107:70` (PAR 1.0700), fires at `--mode ntsc16x9` (PAR 1.2444) and `--mode pal16x9` (PAR 1.4222); packages/layout/test/layout.test.js:802 passed | verified |
| lint.aspect-distortion.messages | Two messages, one for rounded corners and one for images, emitted only when that command kind is present. Corners: `N rounded corner(s) draw P% <wider\|narrower> than tall at PAR X.XXXX; divide the radius by X.XXX to look round`. Images: `N image(s) draw P% <wider\|narrower> than tall at PAR X.XXXX; pre-squash the art or set an explicit width` | packages/layout/src/lint.js:155-172 | `--mode ntsc16x9` on channel6 `games.html` printed `warning: aspect-distortion: 24 rounded corner(s) draw 24% wider than tall at PAR 1.2444; divide the radius by 1.244 to look round` and `warning: aspect-distortion: 6 image(s) draw 24% wider than tall at PAR 1.2444; pre-squash the art or set an explicit width` | verified |
| lint.safe-inset.canvas | The title-safe inset is 5% of the canvas per side, rounded, unless `safeInsetX`/`safeInsetY` are set. 640x448 gives 32 and 22; 640x512 gives 32 and 26, so PAL flags text the NTSC canvas allows | packages/layout/src/lint.js:15-20,139-147 | one scratch screen with 20px text at (40,24): `--canvas 640x448` printed no warning, `--canvas 640x512` printed `warning: overscan: text "CHANNEL 6" at (40,24) leaves the title-safe area; a CRT may crop it` | verified |
| header.display-aspect | The blob header carries `canvas_w`, `canvas_h` and `display_aspect_num`, `display_aspect_den` as an exact ratio. The default when a bake names no aspect is 4:3 | runtime/ps2ui.h:84,105-110 | `par_check` printed `display_aspect 16:9` for `examples/channel6/build/ui-16x9.uib` and `4:3` for `ui.uib`; packages/baker/tests/test_baker.py:3294 `test_default_is_four_three` passed | verified |
| runtime.pixel-aspect.values | `ps2ui_pixel_aspect_x1000` computes `num * canvas_h * 1000 / (den * canvas_w)` in integers. Measured over blobs baked in this session: ntsc 933, ntsc16x9 1244, pal 1066, pal16x9 1422 | runtime/ps2ui.c:1493-1500; runtime/ps2ui.h:685-689 | runtime/tests/test_runtime.c:1013 covers 933 only and passed in `make -C runtime test`; the scratch `par_check` binary loaded one blob per mode and printed 933, 1244, 1066, 1422 | verified |
| runtime.pixel-aspect.scope | The runtime never scales for the aspect. It draws in framebuffer pixels and reports the authored ratio so the app can compare it against the video mode it set | runtime/ps2ui.h:685-689 | read in source; no test drives a mismatch, because the host stub has no video mode to disagree with | code-only |
| previewer.aspect-modes | `ps2ui serve` offers four aspect modes: `framebuffer` (1:1), `authored` (the blob's ratio, the default), `force-4:3` and `force-16:9`. Forcing resizes the render to `round(height * w / h)` | packages/baker/ps2ui_bake/serve.py:66,119,203-206,233-247 | served `examples/channel6/build/ui-16x9.uib` with `--uib` on port 8237; the Aspect select listed the four modes and `force-4:3` produced a 597-pixel-wide frame from the 640x448 render | verified |
| previewer.inspector | The previewer's summary panel reports `aspect mode`, `canvas`, `display`, `pixel aspect` and `display aspect` | packages/baker/ps2ui_bake/serve_page.html:543-555 | read in source and confirmed in the served page's build panel during the Playwright capture; the values are computed in the browser from `/state`, so nothing byte-stable can assert them | code-only |
| limits.one-mode-per-blob | All IRs in one bake must share a canvas and a display aspect. A mismatch raises before any write and exits 1 | packages/baker/ps2ui_bake/quads.py:523-537 | `ps2ui-bake examples/channel6/build/games.json examples/channel6/build/probe-16x9.json -o ...` printed `ps2ui-bake: screen 'probe-16x9': canvas {... 'displayAspect': [16, 9] ...} differs from {... 'displayAspect': [4, 3] ...} — all screens share one video mode`, exit 1 | verified |
| limits.screen-names | `ps2ui build -o build/ui-16x9.uib` renames the intermediates by appending the part of the new stem past the old one, so the screens in the 16:9 blob are `games-16x9` and `probe-16x9`, not `games` and `probe` | packages/baker/ps2ui_bake/project.py:209-223; packages/baker/ps2ui_bake/cli.py:172,178 | `read_uib('examples/channel6/build/ui-16x9.uib')` lists `['games-16x9', 'probe-16x9']` against `['games', 'probe']` for `ui.uib`; `preview.render(..., screen='games')` on the 16:9 blob raised `ValueError: no screen named 'games'` | verified |
| limits.square-pixel-canvas | A square-pixel widescreen canvas is reachable with `--canvas 796x448 --display-aspect 16:9` (PAR 1.0006, no distortion warning), and does not fit the default VRAM budget: three framebuffers at 796x448 need 4472832 B of 4194304 B | packages/layout/src/aspect.js:33-35; packages/baker/ps2ui_bake/vram.py:133,203-209 | compiled channel6 `games.html` at that canvas, no `aspect-distortion` warning; the bake printed `the default budget does not exist at this canvas: three framebuffers at 796x448 need 4472832 B of 4194304 B total VRAM` and exited 1; `--vram-budget 1212416` then printed `textures 253952 B of 1212416 B budget (20%)`, exit 0 | verified |
| limits.display-aspect-parse | A malformed `--display-aspect` is not caught by the argument loop; node prints the `aspect:` error with a stack trace and exits 1 | packages/layout/bin/ps2ui-layout.js:72; packages/layout/src/aspect.js:19-30 | parent fact cli.layout.exit-codes.display-aspect, verified on cli/ps2ui-layout | verified |
