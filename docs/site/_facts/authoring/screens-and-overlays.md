# facts: authoring/screens-and-overlays

Session commands behind the rows below:

- `make -C runtime test` printed `PASS: 5 checks, 0 failure(s)` (test_narrow) and `PASS: 410 checks, 0 failure(s)` (test_runtime). "ok N" refers to the test_runtime numbering as printed.
- `ps2ui-bake games.json games-16x9.json -o out.uib` over copies of `examples/channel6/build/games.json` and `examples/channel6/build/games-16x9.json` in a scratch directory: exit 1, canvas-mismatch message, no `out.uib` written.
- `ps2ui-bake games.json dup/games.json -o out2.uib` over two copies of the same IR: exit 1, `error: duplicate screen name 'games' (file stems must be unique)`.
- `ps2ui-bake` over the six `examples/opl-env/build/*.json` IRs into a scratch blob printed `6 screen(s), 2158 records, 21 textures (161 KiB baked + 61 KiB reserved by slots), 2 CLUTs`.
- The same bake run three ways: `library.json` alone printed `694 records, 18 textures`; `confirm.json` alone printed `110 records, 8 textures`; the pair printed `804 records, 19 textures`.
- `cc -std=c99 -Wall -Wextra -Werror -Istub -Ivendor/gsKit -Ivendor/host-shim -I. -c <scratch>/x.c -o <scratch>/x.o` from `runtime/` exited 0; the scratch file is the page's minimal example.
- `ps2ui-layout a.html a.css -o a.json` over a scratch page using `position: absolute` printed three `not supported on this target; ignored` warnings and exited 0.
- `python3 -c "import inspect; from ps2ui_bake import preview; print(inspect.signature(preview.render))"` printed one `screen` parameter and a `background` tuple.
- `preview.render(..., screen='confirm', background=(0,0,0,0))` over `examples/opl-env/build/ui.uib`: alpha channel extrema `(157, 255)`.
- `PYTHONPATH=packages/baker python3 examples/channel6/preview_in_game.py examples/channel6/build/ui.uib <out>.png` exited 0; `cmp` against `examples/channel6/build/in-game.png` reported no difference.

Parent facts reused without restatement: `cli.bake.screens.stem`, `cli.bake.exit-conditions`, `cli.bake.exit-conditions.no-write`, `cli.bake.summary`, `api.functions.screen_set`, `api.functions.screen_name`, `api.functions.render`, `api.scope.rules.screen`, `api.scope.rules.focus`, `api.scope.rules.visible`, `api.scope.rules.slot`, `api.ordering.render-composites`, `api.ordering.stats-per-render`, `api.ordering.nextframe`, `api.return-conventions.one-zero`, `loop.never-clears`, `loop.two-renders`, `loop.stats.per-render`, `loop.nextframe.once`, `loop.clear.app-owns`, `loop.scissor.restored`, `loop.order`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| screens.rules | Each IR passed to one bake becomes a screen named by the IR file's stem; stems must be unique; textures, CLUTs, atlases and font tables are shared across screens; every IR must carry the same `canvas` object | packages/baker/ps2ui_bake/cli.py:172-176; quads.py:523-537 | parent `cli.bake.screens.stem`; the six-IR opl-env bake in this session printed `6 screen(s)`; the duplicate-stem and canvas-mismatch bakes both exited 1 | verified |
| screens.share.measured | Sharing is a dedupe, not a concatenation: `library.json` alone bakes 18 textures, `confirm.json` alone bakes 8, and the pair bakes 19 | packages/baker/ps2ui_bake/quads.py:523-537 | the three bakes listed above, in this session | verified |
| screens.records.concatenate | Commands, focus nodes and slots concatenate into contiguous per-screen ranges: 694 records plus 110 records bake to 804 | packages/baker/ps2ui_bake/quads.py:526-531, :554-555 | the same three bakes; 694 + 110 == 804 | verified |
| screens.canvas.refusal | A second IR whose `canvas` differs is a `ValueError` from the flattener, printed as `ps2ui-bake: screen '<stem>': canvas {...} differs from {...} — all screens share one video mode`, exit 1, no blob written | packages/baker/ps2ui_bake/quads.py:532-537; cli.py:229-237 | the channel6 4:3 plus 16:9 bake in this session; `out.uib` did not exist afterwards; parent `cli.bake.exit-conditions.no-write` | verified |
| screens.duplicate.refusal | Two IRs with the same file stem are refused before any font or flatten work: `error: duplicate screen name '<stem>' (file stems must be unique)`, exit 1 | packages/baker/ps2ui_bake/cli.py:172-176 | the duplicate bake in this session | verified |
| screens.boot | `ps2ui_load` opens on screen 0, which is the first IR on the bake command line, at that screen's baked initial focus | runtime/ps2ui.c:470, :510 | ok 18 "boots into the library screen"; ok 19, ok 20; memcard is baked `library.json saves.json` | verified |
| screens.focus.memory | `ps2ui_screen_set` stores the outgoing screen's focus index in `ctx->screen_focus[]` and loads the target's, which starts at the baked `initial_focus` for every screen | runtime/ps2ui.c:1477-1481, :510 | ok 130 "saves screen starts at its autofocus", ok 134 "library remembered its focus", ok 135 "saves remembered its focus" | verified |
| screens.set.same-screen | `ps2ui_screen_set` to the current screen returns 1 and skips the save and restore under `if (i != ctx->screen)` | runtime/ps2ui.c:1477 | code-only: the guard is unobservable through the API, because saving `ctx->focus` into `screen_focus[ctx->screen]` and reading it straight back yields the same focus, so no test can separate the two paths | code-only |
| screens.scope | Focus and visibility resolve inside the current screen; slot names resolve over the whole blob, so an overlay and its base need distinct slot names | runtime/ps2ui.c:1557, :1593, :1608, :1397; examples/opl-env/ui/confirm.html:24-32 | parents `api.scope.rules.focus`, `api.scope.rules.visible`, `api.scope.rules.slot`; ok 110, ok 279, ok 280, ok 287; the opl-env dialog names its telemetry slots `confirm-telem` and `confirm-telem2` for this reason | verified |
| compose.contract | The five composition rules: render never clears, so two `screen_set` and `render` pairs sum into one frame; input follows the last `screen_set`; dismissing is one `screen_set` back and restores the base's remembered focus; `ctx->stats` describes the last render only; `gsKit_TexManager_nextFrame` runs once after the flip, never between the two renders | runtime/ps2ui.h:610-649 | ok 271, ok 272, ok 273, ok 279, ok 280, ok 281, ok 276, ok 277, ok 283; parents `loop.never-clears`, `loop.two-renders`, `loop.stats.per-render`, `loop.nextframe.once`, `api.ordering.render-composites` | verified |
| compose.nextframe.drift | `gsKit_TexManager_nextFrame` between the two renders ages the base screen's textures, so an open dialog re-uploads the base's atlases every frame | runtime/ps2ui.h:635-638; runtime/tests/test_runtime.c:1921-1934 | ok 273, ok 283, ok 284; README.md:400 places the call before `gsKit_queue_exec` and `gsKit_sync_flip` in the overlay snippet (D14); parent `loop.nextframe.once` | contradicts-readme |
| compose.scissor | A composited frame leaves the scissor at the full canvas, so an app drawing its own geometry after the overlay inherits the whole screen | runtime/ps2ui.c:1164-1166; runtime/ps2ui.h:647-649 | ok 274; parent `loop.scissor.restored` | verified |
| compose.idempotent | Rendering a screen does not consume it: after a composite, the overlay alone still costs the primitives it cost before, and the base still costs its own | runtime/ps2ui.c:940-1173 | ok 275, ok 282 | verified |
| compose.sample | The sample ELF's covers arm runs the idiom live: `screen_set("covers")`, render, then `screen_set("dialog")`, render, then `screen_set("covers")` to hand input back | runtime/sample/main.c:2753-2762 | read in source; the arm is `#ifdef`-gated console bring-up with no host test, and ok 271 to ok 283 cover the same sequence on the stub | code-only |
| compose.no-api | There is no `ps2ui_overlay_push` and its absence is a decision, not a gap: the idiom covers everything except keeping the input screen distinct from the drawn screens | runtime/ps2ui.h:640-646 | `grep -rn overlay_push` matches only comments in runtime/ps2ui.h:628, README.md:422, CHANGELOG.md:523 and two design documents; no declaration exists | verified |
| compose.one-screen-no-overlap | Two boxes cannot overlap inside one screen: the CSS stage ignores `position`, `left` and `top` with a warning, so a scrim over a panel is a second screen | packages/layout/src/css.js:865; BACKLOG.md:532-541 | the scratch `ps2ui-layout` run in this session printed `warning: css: line 3: property "position" not supported on this target; ignored` and the same for `left` and `top`, exit 0 | verified |
| compose.previewer | `preview.render` takes one `screen` and a `background` colour tuple, not a frame, so the Python previewer cannot draw a two-screen composite | packages/baker/ps2ui_bake/preview.py:126-127; BACKLOG.md:567-572 | `inspect.signature(preview.render)` in this session printed `screen=0` and `background=(10, 14, 26, 255)` | verified |
| compose.previewer.workaround | A host can composite outside the previewer: `examples/channel6/preview_in_game.py` renders the screen on `background=(0, 0, 0, 0)` and `alpha_composite`s it over a synthetic frame | examples/channel6/preview_in_game.py:87-95 | run in this session against `examples/channel6/build/ui.uib`; the PNG is byte-identical to `examples/channel6/build/in-game.png` under `cmp` | verified |
| compose.scrim.alpha | An overlay's translucency is baked into its own quads, not left as frame alpha: opl-env's `--scrim` is `rgba(6, 9, 16, 0.62)` and the confirm screen rendered on full transparency has alpha extrema 157 to 255 | examples/opl-env/ui/opl.css:47; packages/baker/ps2ui_bake/preview.py:126 | the transparent-background render in this session; 0.62 * 255 rounds to 158, and the scrim covers the canvas, so the minimum is the scrim | verified |
| compose.minimal.compiles | The page's minimal example compiles against the shipped header under `-std=c99 -Wall -Wextra -Werror` | runtime/ps2ui.h; runtime/sample/main.c:2753-2762 | the `cc` command above, exit 0 | verified |
| screens.project-file | A project file lists its screens as HTML paths and `ps2ui build` compiles each one to an IR whose stem names the screen; opl-env lists six | packages/baker/ps2ui_bake/ps2ui.py:82-106; examples/opl-env/ps2ui.json:2-5 | read in source; the equivalent direct bake of the six IRs ran in this session, but `examples/*/build.sh` is not run here, so `ps2ui build` itself was not executed | code-only |
