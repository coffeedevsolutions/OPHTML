# facts: authoring/project-file

Session: `cd packages/baker/tests && python3 -m unittest test_baker.TestProjectFile -v` printed `Ran 17 tests in 0.440s` / `OK`. Scratch copies of `examples/memcard` and `examples/channel6` (sources and `ps2ui.json` only) were built under the scratchpad from the repository root; nothing under `examples/*/build/` was written.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| project.keys | A project takes exactly these keys: canvas, css, displayAspect, focusWrap, fonts, minFontSize, mode, montage, out, palettizeImages, preview, previewDisplay, screens, strict, vramBudget. The full table is under `## project.keys` below. | packages/baker/ps2ui_bake/project.py:39-55 (`DEFAULTS`) | `ps2ui build` on a project with `"colour": 1` printed the accepted list, pasted on the page; `TestProjectFile` (17 tests) OK | verified |
| project.keys.required | `screens` and `css` are the only keys without a default. `css` may be omitted at the top level when every screen sets its own. | project.py:40-41, 215-221 (`load`), 105-109 (`_screen`) | `no-css.json` refusal; `no-screens.json` refusal; `test_a_screen_may_be_an_object_and_its_keys_are_checked_too` (test_baker.py:4169) | verified |
| project.keys.unknown | An unknown top-level key is refused by name and the message lists every accepted key, sorted. | project.py:209-214 | `ps2ui build ps2ui.json` with `"colour": 1`, exit 1, message pasted on the page; `test_an_unknown_key_is_refused_by_name` (test_baker.py:4154) | verified |
| project.keys.check | `strict` and `vramBudget` are forwarded from the project to `ps2ui-check` (as `--strict`, `--vram-budget`). The forwarded set is derived from `DEFAULTS` against the checker's `--help`. | packages/baker/ps2ui_bake/ps2ui.py:336-351 (`cmd_check`); CHANGELOG.md:1538 (0.6.0, "`ps2ui check` is given the settings `ps2ui build` was given") | `test_every_project_key_the_checker_accepts_actually_reaches_it` (test_baker.py:4064) ... ok; `test_check_is_given_the_budget_the_build_was_given` (4313) ok; `test_no_budget_in_the_project_means_no_flag` (4338) ok | verified |
| project.keys.layout | `compile_screens` passes `--fonts`, `--mode`, `--canvas`, `--display-aspect`, `--strict`, `--min-font-size` and per-screen `--focus-wrap` to `ps2ui-layout`. | ps2ui.py:92-105 | `ps2ui-layout --help` lists each flag; the channel6 second build printed `build/games-16x9.json` with aspect warnings at PAR 1.2444, proving `--mode` reached the compiler | verified |
| project.keys.bake | `bake_argv` passes `-o`, `--fonts`, `--palettize-images`, `--vram-budget`; `cmd_build` adds `--preview`, `--montage`, `--preview-display` for each key that is a path. | ps2ui.py:135-141, 161-166 | `ps2ui-bake --help` lists each flag; memcard build printed `preview -> build/preview.png` and `montage -> build/states.png` | verified |
| project.keys.dev | `cmd_dev` forwards fonts, mode, canvas, displayAspect, strict, minFontSize, the chosen screen's focusWrap, and palettizeImages to `ps2ui-dev`, writing under `<build dir>/dev`. `strict` and `minFontSize` take effect there as they do under `ps2ui build` (fixed on this branch, D9). | ps2ui.py:289, 302-317; packages/layout/bin/ps2ui-dev.js (`options.lint`, `build()`); packages/layout/src/index.js:271 | `test_dev_builds_with_a_fonts_manifest_and_stays_out_of_build` (test_baker.py:4376) proves the forwarding; `TestDevAgreesWithBuild.test_dev_applies_strict_and_min_font_size_like_build` (test_serve.py) proves both flags land | verified |
| project.keys.mode-override | `ps2ui build --mode` replaces the project's `mode`; `-o`, `--preview`, `--montage`, `--preview-display` replace their keys; the value `none` means suppress. | ps2ui.py:148-156, 344-359 | channel6 second build line from the page: `--mode ntsc16x9 -o build/ui-16x9.uib --preview-display ... --preview none --montage none`, exit 0, `build/` listed on the page | verified |
| project.screen-keys | A screen is a string (the HTML path) or an object with `html` (required), `css` (default: the top-level `css`), `focusWrap` (default: the top-level `focusWrap`). Any other key in the object is refused by name. A non-string non-object entry is refused. | project.py:59 (`SCREEN_DEFAULTS`), 88-114 (`_screen`) | examples/channel6/ps2ui.json:4 `{ "html": "ui/probe.html", "focusWrap": true }` builds; `screen-unknown.json`, `screen-number.json`, `screen-no-html.json` refusals pasted on the page; `test_a_screen_may_be_an_object_and_its_keys_are_checked_too` (test_baker.py:4169) ok | verified |
| project.screen-name | A screen's name is the HTML file's basename without extension; it names the intermediate `<name><suffix>.json` and the screen inside the blob. | project.py:114, 158-160 | memcard build wrote `build/library.json`, `build/saves.json`; `ps2ui check examples/memcard` reported `2 screen(s)`; `test_the_examples_are_the_acceptance_test` (test_baker.py:4215) ok | verified |
| project.resolution | Every path in the file resolves against the directory holding the project file, never the cwd. The root is symlink-resolved, because `ps2ui build` also chdir's to it: a relative path counted from an unresolved spelling and walked from the resolved one lands elsewhere whenever the symlink changes the depth, which is macOS's `/var` → `/private/var`. `ps2ui build` runs from that directory, prints paths relative to it and restores the cwd. A directory argument means `<dir>/ps2ui.json`. Fonts: the `fonts` key if set; else `fonts/fonts.json` beside the project when that file exists; else no `--fonts` is passed and the baker applies its own default (the repository's `fonts/fonts.json`, a candidate that exists only in a checkout). | project.py:103 (`root`, realpath), 109-110 (`_abs`), 149-160 (`fonts_path`), 216-217 (`load` isdir); ps2ui.py:60-76 (`in_project`), 79-80 (`rel`); packages/baker/ps2ui_bake/cli.py:65-80 (`default_fonts_path`), 190-192 | `ps2ui build <scratch>/memcard/ps2ui.json` from `/home/user/OPHTML` wrote `<scratch>/memcard/build/{library.json,preview.png,saves.json,states.png,ui.uib}` and printed `-> build/ui.uib`; `ps2ui check examples/memcard` from the repo root printed `PASS: 63 checks`; `test_a_directory_means_the_ps2ui_json_inside_it` (test_baker.py:4156), `test_build_prints_paths_relative_to_the_project_not_the_cwd` (4439), `test_a_project_under_a_depth_changing_symlink_builds` (4486) ok. Fonts fallback: docs/tutorial-uc3.md:134-142 has no `fonts` key and its section 1 writes `fonts/fonts.json` with `ps2ui fontgen`; examples/memcard has neither a `fonts` key nor a `fonts/` directory and builds against the baker default | verified |
| project.out-override | `-o NEW` moves the blob and its intermediates. With `base` = stem of the project's `out` and `new` = stem of NEW: equal stems give no suffix; `new` starting with `base` gives suffix `new[len(base):]`; anything else gives suffix `-<new>`. The IR file is `<build dir of NEW>/<screen><suffix>.json`. Previews do not move; use `--preview`, `--montage`, `--preview-display`. | project.py:156-176 (`ir_suffix`, `ir_path`, `set_out_override`) | channel6 second build wrote `games-16x9.json`, `probe-16x9.json`, `ui-16x9.uib` beside `games.json`, `probe.json`, `ui.uib`; `ps2ui build ... -o build/other.uib` wrote `library-other.json`, `saves-other.json`, `other.uib`; `test_out_override_moves_the_intermediates_with_it` (test_baker.py:4441) covers `dist/ui.uib` -> `dist/games.json` | verified |
| project.preview-false | `false` for `preview`, `montage` or `previewDisplay` suppresses that output; `null` and `false` are treated alike. | project.py:138-140 (`preview_path`) | scratch memcard copy with `"preview": false` built `build/{library.json,saves.json,ui.uib}` and no PNG; `test_the_minimum_project_is_two_keys` (test_baker.py:4013) asserts `montage` default is None | verified |
| project.check-needs-blob | `ps2ui check` never builds; a missing blob at `out` is refused. | ps2ui.py:173-178 | `ps2ui check nobuild` printed `build/ui.uib: no blob to check. Run \`ps2ui build\` first ...`, exit 1 | verified |
| project.refusals | Loader refusals, each without a traceback, exit 1: missing file; invalid JSON; top level not an object; `screens` missing or empty; `screens` not a list; unknown key; screen entry neither string nor object; screen object with unknown key; screen object without `html`; screen with no stylesheet in either place. | project.py:91-109, 189-223; ps2ui.py:429-433 (`ProjectError` printed as `ps2ui: ...`) | nine scratch project files run through `ps2ui build`, every message pasted on the page; `test_missing_and_malformed_projects_are_not_tracebacks` (test_baker.py:4201), `test_a_screen_with_no_stylesheet_says_both_places_to_put_one` (4391) ok | verified |
| project.one-blob | The file describes one blob; there is no variants key. A second blob is a second `ps2ui build` with flags. | project.py:22-27 (module docstring); examples/channel6/build.sh:23-29 | channel6 second build line, exit 0 | verified |
| project.readme-agreement | README.md:140-143 shows the same two-key minimum and README.md:662-665 says serve reads everything else from `ps2ui.json`; neither contradicts the code. D4's stale bake flags are not restated on the page. | README.md:142, 663 | read | code-only |

## project.keys

Column `reaches` names the tool the key is forwarded to: layout = `ps2ui-layout`, bake = `ps2ui-bake`, check = `ps2ui-check`, dev = `ps2ui-dev`.

| key | type | default | reaches | source |
|---|---|---|---|---|
| screens | list of string or object | required | layout · bake · dev | project.py:40, 82-83; ps2ui.py:88-91, 135 |
| css | string | required unless every screen sets `css` | layout · dev | project.py:41, 105; ps2ui.py:90, 292 |
| fonts | string | `fonts/fonts.json` beside the project if present, else the baker default | layout · bake · dev | project.py:42, 126-136; ps2ui.py:92-93, 136-137, 302-303 |
| out | string | `build/ui.uib` | bake · check · dev (its directory) | project.py:43, 117-123; ps2ui.py:135, 198, 289 |
| preview | string or false | `build/preview.png` | bake | project.py:44, 138-140; ps2ui.py:161-166 |
| montage | string or false | none | bake | project.py:45; ps2ui.py:161-166 |
| previewDisplay | string or false | none | bake | project.py:46; ps2ui.py:161-166 |
| mode | string | none | layout · dev (and `ps2ui build --mode`) | project.py:47; ps2ui.py:94-95, 148-149, 304-305 |
| canvas | string `WxH` | none | layout · dev | project.py:48; ps2ui.py:96-97, 306-307 |
| displayAspect | string `W:H` | none | layout · dev | project.py:49; ps2ui.py:98-99, 308-309 |
| strict | boolean | false | layout · check · dev | project.py:50; ps2ui.py:100-101, 212-213, 310-311 |
| minFontSize | integer px | none | layout · dev | project.py:51; ps2ui.py:102-103, 312-313 |
| focusWrap | boolean | false | layout · dev (per screen) | project.py:52, 110-113; ps2ui.py:104-105, 314-315 |
| palettizeImages | boolean | false | bake · dev | project.py:53; ps2ui.py:138-139, 316-317 |
| vramBudget | integer bytes | none | bake · check | project.py:54; ps2ui.py:140-141, 199-200 |

## project.screen-keys

| key | type | default | reaches | source |
|---|---|---|---|---|
| html | string | required | layout · dev | project.py:102-104 |
| css | string | the top-level `css` | layout · dev | project.py:105-109 |
| focusWrap | boolean | the top-level `focusWrap` | layout · dev | project.py:110-113 |
