# facts: authoring/css

Every command was run from the repository root in this session.
`<scratch>` is
`/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/authoring/css`.
The compile command is
`ps2ui-layout <scratch>/<name>.html <scratch>/<name>.css --fonts fonts/fonts.json -o <scratch>/x.json`
unless a row says otherwise. The command blocks on the page were re-run from
`<scratch>` with relative paths and a `fonts/fonts.json` copied there, so the
paths they print are the ones the page shows. `node --test packages/layout/test/parse.test.js`
printed 39 pass, 0 fail. `node --test packages/layout/test/layout.test.js`
printed 71 pass, 0 fail.

Parent facts reused without restatement: `ir.geometry.focus-paint-only`,
`ir.colour.domain`, `ir.schema.rect.radius`, `ir.schema.rect.normalize`,
`ir.schema.scissor`, `ir.schema.themes`, `cli.layout.exit-codes`,
`cli.layout.output-lines`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| css.selectors | The grammar is type, `.class`, `#id`, `*`, a compound of those, the descendant combinator (whitespace), `:focus` on any compound, and comma-separated selector lists. Nothing else parses | packages/layout/src/css.js:143-170 | compiled `div > p { color: red }`: `error: css: line 1: unsupported selector syntax near ">" in ">"`, exit 1; parse.test.js:85 `css: descendant combinator matches ancestors, not siblings` | verified |
| css.selectors.specificity | Specificity is the `(id, class, type)` triple summed over every compound, with source order as the tiebreak; `*` contributes nothing | packages/layout/src/css.js:177-190, packages/layout/src/css.js:222-227 | a Node import of `parseSelector` printed `.card:focus [0,2,0]`, `div:focus [0,1,1]`, `.card [0,1,0]`, `.a .b [0,2,0]`, `#x [1,0,0]`, `.tile:focus .title [0,3,0]`, `* [0,0,0]`; parse.test.js:75 `css: specificity and source order decide the cascade` | verified |
| css.selectors.focus-specificity | `:focus` counts as a class in the triple. `.card:focus` beats `div:focus` although it is written first | packages/layout/src/css.js:182 | `<scratch>/spec.css` put `.card:focus { color: #ff0000 }` before `div:focus { color: #00ff00 }`; the `focused` text command printed `[255,0,0,255]` | verified |
| css.selectors.focus-match | A compound carrying `:focus` matches only an element with the `focusable` attribute. **New in 0.7.0:** when that attribute is the only reason the rule did not match, the compiler says so -- `"<selector>" matches <tag> line <n>, but no element in that selector has the focusable attribute, so the :focus delta can never show`. Keyed on the rule, so twenty `.panel` elements are one warning. A rule naming a class the element does not carry stays silent, which is an ordinary non-match | packages/layout/src/css.js `compoundMatches` (the `ignoreFocus` parameter) and the match loop in `computeStyle` | both selector shapes warn (`.panel:focus` fails at the rightmost compound, `.tile:focus .title` at the ancestor walk) and a `focusable` element warns about nothing; before 0.7.0 the same sheet compiled with no diagnostic at all. box.js carried this warning's text and could not reach it: it tested `focusDeclared && scope === null`, and the drop happens upstream of both | verified |
| css.selectors.focus-scope | `:focus` on a non-rightmost compound styles descendants of the focus scope. `.tile:focus .title` matches the `.title` child of a focusable `.tile` | packages/layout/src/css.js:146-152, packages/layout/src/css.js:186-188 | parse.test.js:131 asserts `selectorMatches` true for the focusable tree and false for the plain one; `<scratch>/demo.css` uses `.card:focus .name` and the compile emitted paired `unfocused`/`focused` text commands | verified |
| css.properties | Understood properties, each a `case` in `applyDeclaration`: `display`, `flex-direction`, `flex-wrap`, `justify-content`, `align-items`, `align-self`, `flex-grow`, `flex-shrink`, `flex-basis`, `flex`, `gap`, `row-gap`, `column-gap`, `width`, `height`, `min-width`, `min-height`, `max-width`, `max-height`, `padding`, `padding-top`, `padding-right`, `padding-bottom`, `padding-left`, `margin`, `margin-top`, `margin-right`, `margin-bottom`, `margin-left`, `border`, `border-width`, `border-color`, `border-radius`, `background`, `background-color`, `color`, `font-size`, `font-weight`, `line-height`, `text-align`, `white-space`, `text-overflow`, `letter-spacing`, `overflow`, `opacity` | packages/layout/src/css.js:760 (`applyDeclaration`), and one member per `case` line inside it, in the order the fact lists the properties: packages/layout/src/css.js:763, 768, 781, 783, 785, 787, 789, 791, 793, 795, 805, 811, 812, 813, 814, 815, 816, 817, 818, 819, 824, 831, 836, 841, 867, 868, 874, 875, 885, 891, 892, 895, 901, 903, 905, 907, 908, 913. **38 numbers for 45 properties** because three lines carry several cases each -- 824 the four `padding-*`, 836 the four `margin-*`, 875 both `background` and `background-color` | re-derived this session: `applyDeclaration` spans 760-936, and an `awk` over that span returns 38 `case '` lines carrying 45 properties, which are the numbers above and the properties in the fact. **The previous version of this row was 178 to 191 lines low on every one of them** -- 585 where 763 belongs, 722 where 913 does -- an ordered snapshot of a `css.js` that had since grown by about 180 lines, while its own verified-by cell said a `grep` had returned exactly those cases. Nothing had read it, because every member after the first named no path: the shape F42 removed | verified |
| css.properties.defaults | Defaults come from the frozen `INITIAL_STYLE`: `display: flex`, `flex-direction: column` (fallback only), `flex-wrap: nowrap`, `justify-content: flex-start`, `align-items: stretch`, `align-self: auto`, `flex-grow: 0`, `flex-shrink: 1`, `flex-basis: auto`, gaps 0, `width`/`height` auto, `min-*`/`max-*` null, padding and margin `[0,0,0,0]`, `border-width: 0`, `border-color: null`, `border-radius: 0`, `background: null`, `color: white`, `font-size: 16`, `font-weight: 400`, `line-height: 1.25`, `text-align: left`, `white-space: normal`, `text-overflow: clip`, `letter-spacing: 0`, `overflow: visible`, `opacity: 1` | packages/layout/src/css.js:72-126 | parse.test.js:93 asserts `INITIAL_STYLE.padding` is `[0,0,0,0]` and frozen; a compile with an empty sheet paints nothing, which is `background: null` | verified |
| css.flex-wrap.reverse | `flex-wrap` takes three values. `wrap-reverse` wraps like `wrap` and stacks its lines from the cross-end, so the first item lands on the last line. Both gates ask `flexWrap !== 'nowrap'`; the stacking reverses the line array rather than the offsets | packages/layout/src/flex.js `wraps`, `wrapReverse`, and the `stacked` line in the cross loop | compiled three 30px items into a 50px `row` box under each value: `wrap` gives three lines with the first at y=0, `wrap-reverse` gives three lines with the first at the `wrap` third line's y and the last at y=0; `packages/layout/test/layout.test.js` "flex: wrap-reverse wraps, and stacks its lines from the bottom". New in 0.7.0: before it, the value was stored unvalidated and compared only against `'wrap'`, so it did not wrap at all | verified |
| css.properties.unknown | An unrecognised property is a warning, not an error: `css: line <n>: property "<p>" not supported on this target; ignored`. The compile continues and exits 0 | packages/layout/src/css.js:917-925 | compiled `.panel { position: absolute; top: 10px }`: two `warning: css: line 1: property "..." not supported on this target; ignored` lines, exit 0; parse.test.js:139 `css: unknown properties warn instead of erroring` | verified |
| css.properties.position | `position` is unsupported. It sits in `GEOMETRY_PROPS` but has no `case`, so it warns like any unknown property while still being refused inside a `:focus` rule | packages/layout/src/css.js:48, packages/layout/src/css.js:726-734 | the `position: absolute` compile above warned and exited 0; no `case 'position'` appears in `grep -n "case '" packages/layout/src/css.js` | verified |
| css.properties.display | `display` accepts `flex` and `none` only. `display: none` drops the element and its subtree; a `display: none` root is `layout: root element is display: none` | packages/layout/src/css.js:763-767; packages/layout/src/box.js:176; packages/layout/src/index.js:196 | compiled `.panel { display: block }`: `error: css: line 1: display: only "flex" and "none" exist on this target (got "block")`, exit 1; a two-child column with `.gone { display: none }` emitted one text command, `["A"]` | verified |
| css.properties.overflow | `overflow` accepts `visible` and `hidden` only. `hidden` on a non-text box brackets its children with a `scissor_push`/`scissor_pop` pair | packages/layout/src/css.js:908-912; packages/layout/src/paint.js:315-326 | compiled `.panel { overflow: scroll }`: `error: css: line 1: overflow: only visible\|hidden (there is no scrolling on a memory card browser)`, exit 1; a compile with `overflow: hidden` printed the op sequence `scissor_push text scissor_pop text` | verified |
| css.units | `px` is the only unit accepted by `gap`, `row-gap`, `column-gap`, `padding`, `margin` and their per-side forms, `border-width`, `border-radius`, `font-size` and `letter-spacing`. `width`, `height`, `min-*`, `max-*` and `flex-basis` also take `%` and `auto`, and accept a bare number as pixels. `line-height` takes `px`, a bare number (a multiplier) or `%` (divided by 100 into a multiplier). Nothing accepts `em`, `rem`, `vw` or `vh` | packages/layout/src/css.js:639-653 (`pxOrThrow`), packages/layout/src/css.js:605, packages/layout/src/css.js:625-630, packages/layout/src/css.js:707-712; packages/layout/src/values.js:33-44; packages/layout/src/text.js:183-187 | compiled `padding: 1em` -> `error: css: line 1: padding: "1em" is not a length`; `padding: 8` -> `error: css: line 1: padding: unitless "8" — write "8px"`; `width: 50em` -> `error: css: line 1: width: "50em" is not a length`; a nested box with `width: 50` gave `w=50`, `width: 50%` gave `w=320`, `width: 200px` gave `w=200`, `width: auto` gave `w=640`; `line-height: 1.4`, `24px` and `140%` gave text `y` of 2, 0 and 2 at `font-size: 20px` | verified |
| css.units.zero | A bare `0` passes the px check; any other unitless value on a px-only property is refused by name | packages/layout/src/css.js:645-653 | `border-width: 2` printed `error: css: line 1: border-width: unitless "2" — write "2px"`; `padding: 0` compiles (memcard and the demo sheet both use px throughout, and the guard clause reads `l.unit === 'number' && l.value === 0`) | code-only |
| css.colors | Named colours are `black`, `white`, `red` (255,0,0), `green` (0,128,0), `blue`, `gray`, `grey` (both 128,128,128) and `transparent`. Hex is `#rgb`, `#rgba`, `#rrggbb`, `#rrggbbaa`. `rgb()` and `rgba()` take three or four comma-separated channels, each a number or a percentage, with alpha 0 to 1 | packages/layout/src/values.js:10-19, packages/layout/src/values.js:66-104 | compiled `background: <v>` for every form; the rects printed `black [0,0,0,255]`, `green [0,128,0,255]`, `gray`/`grey [128,128,128,255]`, `#abc [170,187,204,255]`, `#abcd [170,187,204,221]`, `#aabbcc [170,187,204,255]`, `#aabbccdd [170,187,204,221]`, `rgb(10,20,30) [10,20,30,255]`, `rgba(10,20,30,0.5) [10,20,30,128]`, `rgb(50%,0%,100%) [127,0,255,255]`, and `transparent` emitted no rect; parse.test.js:56 `values: color forms` | verified |
| css.colors.unknown | Any other colour name or a malformed hex is an error naming the property and the token | packages/layout/src/css.js:875-884, packages/layout/src/css.js:885-890; packages/layout/src/values.js:103 | `color: orange` -> `error: css: line 1: color: bad color "orange"`; `background: orange` -> `error: css: line 1: background: bad color "orange" (flat colors only — gradients are a texture you bake yourself)`; `background: #abcde` -> the same shape | verified |
| css.colors.readme | **Fixed in 0.7.0.** README.md's "Supported CSS" list said "Flat colors with real translucency" and never stated which names parse, so a sheet written from it could use `orange` and fail to compile. It now names all eight and the hex and `rgb()` forms | packages/layout/src/values.js:10-19; README.md:233-237 | `color: orange` still exits 1 with `bad color "orange"`; README.md:233 now reads "Colors are not open-ended" and lists the eight; drift row D7 | was-contradicts-readme, fixed |
| css.inherited | The inherited properties are `color`, `font-size`, `font-weight`, `line-height`, `text-align`, `white-space` and `letter-spacing`, plus `colorVar` and `colorThemes`, which travel with `color` so a theme moves a parent and its inheriting children together. Everything else resets to the initial value, `background` and `border` included | packages/layout/src/css.js:51-67, packages/layout/src/css.js:758-768 | a parent setting all seven plus `background`, `border` and `padding` produced one child text command `{x:478, y:16, size:24, weight:700, letterSpacing:3, color:[51,102,153,255]}` and exactly one rect, the parent's; parse.test.js:148 `css: inherited vs reset properties`; parse.test.js:219 `var: colorVar inherits with color` | verified |
| css.inherited.focus | The focus pass inherits from the parent's focus style, so a focused container recolours its text children even when they carry no `:focus` rule of their own | packages/layout/src/css.js:1029-1035 | parse.test.js:121 `css: :focus paint delta computes a separate focus style`; `<scratch>/demo.css` `.card:focus .name` produced paired `unfocused`/`focused` text commands in `<scratch>/demo.json` | verified |
| css.flex-direction.required | A container laying out two or more children must declare `flex-direction`. A container with one child or none is never asked. Declaring it is what counts, not the value: `flexDirectionDeclared` is set by the `case`, not inferred | packages/layout/src/box.js:393-402; packages/layout/src/css.js:645-652 | compiled `<scratch>/undirected.html` with only `.row` directed; the error named both containers and exited 1 | verified |
| css.flex-direction.message | Every offender is reported in one error, sorted by line: `layout: N container(s) lay out two or more children without stating flex-direction:` then one `  <tag> line N (M children)` line each, then the sentence `There is no default. CSS's initial value is row, ps2ui once used column, so either silent answer is wrong for half of all authors — add flex-direction: row or column to each.` | packages/layout/src/index.js:197-212 | the compile above printed the whole message with `2 container(s)`, `  <div> line 1 (2 children)`, `  <div> line 3 (2 children)` | verified |
| css.focus.geometry-props | `GEOMETRY_PROPS` holds 33 names: `display`, `flex-direction`, `flex-wrap`, `justify-content`, `align-items`, `align-self`, `flex-grow`, `flex-shrink`, `flex-basis`, `gap`, `row-gap`, `column-gap`, `width`, `height`, `min-width`, `min-height`, `max-width`, `max-height`, `padding` and its four sides, `margin` and its four sides, `border-width`, `font-size`, `line-height`, `white-space`, `overflow`, `position`. Any of them inside a `:focus` rule is a compile error: `css: line <n>: :focus may not change "<p>" — focus is a paint-only delta. Both states share one baked layout; move the geometry to the base rule.` | packages/layout/src/css.js:41-49, packages/layout/src/css.js:792-798 | compiled `.tile:focus { width: 120px }`: that exact message, exit 1; parse.test.js:112 `css: :focus changing geometry is a compile error` | verified |
| css.focus.paint-ok | A `:focus` rule that changes only paint compiles and emits a focused/unfocused command pair. `font-weight` is one such property and reaches the focused command | packages/layout/src/css.js:1052; packages/layout/src/paint.js:100-109, packages/layout/src/paint.js:138-158 | compiled `.tile:focus { font-weight: bold }`: exit 0, `1 focusables`; a `.tile:focus .t { font-weight: bold; ... }` compile printed `{"state":"unfocused","weight":400}` and `{"state":"focused","weight":700}` | verified |
| css.focus.guarded-fully | **Fixed in 0.7.0.** Three of the four text properties outside `GEOMETRY_PROPS` are now refused under `:focus` by a second set, `FOCUS_DROPPED_PROPS`: `letter-spacing`, `text-align` and `text-overflow` are read from the base style when the line is emitted, so a value there parsed, applied to `focusStyle` and vanished. The message differs from the geometry one deliberately -- these are not two-layout requests. `font-weight` stays allowed and is now MEASURED for: the anonymous text box carries `measureWeight = max(base, focus)`, so the box is sized for the heavier face and one baked layout holds both states. Sizing for the heavier face costs something on wrapping text: the unfocused state is wrapped at the bold face's break points, so it breaks differently and at some widths gains a line, making the box taller in both states. The compiler warns when the line count changes, naming both | packages/layout/src/css.js `FOCUS_DROPPED_PROPS` and the focus loop in `computeStyle`; packages/layout/src/box.js (`tbox.measureWeight`); packages/layout/src/flex.js `measureText` | each of the three exits 1 naming the property and the reason; `font-weight: bold` under `:focus` exits 0. Measured: a 20px `Shadow of the Colossus` row bolded on focus boxed at 236px before (regular width) and 270px after (bold width), so 34px drew outside the box; `layout.test.js` pins both the bold case and the no-delta case, and sabotaging the measure in either direction is caught; swept over one string at seven widths, the line count moves at 170px (3 to 4) and 250px (2 to 3) and holds at 150, 190, 210, 230 and 300, and the warning fires at exactly those two -- both a dropped diagnostic and one that fires on every weight delta are caught | verified |
| css.keywords.checked | **New in 0.7.0.** Eight keyword-valued properties -- `flex-direction`, `flex-wrap`, `justify-content`, `align-items`, `align-self`, `text-align`, `white-space`, `text-overflow` -- are checked against the set the solver implements, and refuse anything else. Real CSS with no branch behind it (`space-evenly`, `baseline`, `justify`, `pre`/`pre-wrap`/`pre-line`/`break-spaces`) gets a message naming the layout it would silently have produced; anything else reads as an unknown value. Before it, all eight were stored verbatim and every consumer ended in a default branch meaning the initial value, so `flex-direction: rows` laid out as a column at exit 0 | packages/layout/src/css.js:683-740 (`KEYWORDS`), packages/layout/src/css.js:741-752 (`keywordOrThrow`), packages/layout/src/css.js:709 (flex-direction, validated before `flexDirectionDeclared`); packages/layout/src/flex.js:511-525 (`justifyOffsets`), packages/layout/src/flex.js:527-533 (`alignOffset`), packages/layout/src/flex.js:100, 125, 552 (`whiteSpace`) -- the last three are where the accepted sets were copied from | one misspelling per property exits 1 on all eight, against 0 on all eight at base `6cc17f9`, where `flex-direction: rows` laid the two children out as a column (rects `0,0` and `0,20`, against `0,0` and `40,0` for `row`) and `text-align: centre` put the text at `x=0` against `x=92` for `center`; all eight `absent` values take the real-CSS branch; `parse.test.js` drives the table through `applyDeclaration` and counts 8 properties / 28 accepted values, and four sabotages are each caught by the test written for them | verified |
| css.keywords.direction-trap | **Closed in 0.7.0.** `flex-direction: rows` used to satisfy the required-direction check as well, because that check tests that a declaration exists and not that its value parses -- one typo defeating the only fence over this family. The value is validated before `flexDirectionDeclared` is set, so a refused value leaves the flag false | packages/layout/src/css.js:670-677; packages/layout/src/box.js:373 | the `flex-direction: rows` compile above exited 0 with no undirected-container error | verified |
| css.white-space | `white-space` is honoured for the single value `nowrap`, which suppresses wrapping. `normal` is the only other value accepted; `pre`, `pre-wrap`, `pre-line` and `break-spaces` are refused by name since 0.7.0, rather than wrapping like `normal` | packages/layout/src/css.js:722-734 (the accepted set and the four refused by name), packages/layout/src/css.js:903-904 (the `case`); packages/layout/src/flex.js:100, 125, 552 (`whiteSpace`) | `grep -n "whiteSpace" packages/layout/src/flex.js packages/layout/src/text.js` returns three lines, all in `flex.js` and none in `text.js`: 100 and 552 are `=== 'nowrap'`, 125 is the `!== 'nowrap'` measure-weight guard. **The previous version of this cell said the grep returned "only the two `=== 'nowrap'` comparisons"** and cited `flex.js:73-80` and `:408`, which are the margin helpers and a font resolve; review of #161 found it | verified |
| css.text-overflow | `text-overflow: ellipsis` ellipsizes a line only when `white-space: nowrap` is also set and the line is wider than the content box | packages/layout/src/flex.js:551-557; packages/layout/src/text.js:153-165 | `<scratch>/demo.css` `.title` sets both on a 380px box; `<scratch>/demo.json` carries `"Memory card browser…"` | verified |
| css.opacity | `opacity` is clamped to 0..1 and multiplies into the alpha of the background, the border colour and the text colour of the element it is set on. The fold runs over every theme row, not the default row alone | packages/layout/src/css.js:913-917; packages/layout/src/paint.js:30-33, packages/layout/src/paint.js:59-98, packages/layout/src/paint.js:100-109 | layout.test.js:951 `theme: the opacity fold runs over every row, not just the default` asserts `fillThemes` `[[51,102,153,128],[238,238,238,128]]` for a `var(--panel)` fill at `opacity: 0.5` in a two-theme sheet; the file passes 71/71 | verified |
| css.opacity.not-inherited | `opacity` does not inherit and does not compound down the tree. It is absent from `INHERITED_PROPS` and is read per box at paint time | packages/layout/src/css.js:51-67; packages/layout/src/paint.js:71, packages/layout/src/paint.js:102 | `opacity` does not appear in the `INHERITED_PROPS` set; parse.test.js:148 shows the inherit list applied to a child | code-only |
| css.border | `border` is a shorthand for `<width> solid <color>`; `none` is accepted as a token. Any other token is an error naming it. A `var()` token survives the split and sets the border colour | packages/layout/src/css.js:841-866 | `border: 2px dashed white` -> `error: css: line 1: border: unsupported token "dashed" (only solid borders exist)`; `<scratch>/demo.css` uses `border: 3px solid #222b40` and the compiled rect carries `borderWidth: 3`; parse.test.js:378 `@theme: the border shorthand finds a colour with spaces inside its parens` | verified |
| css.border-radius | `border-radius` takes exactly one px length. Two values are refused as a malformed length, not split | packages/layout/src/css.js:874; packages/layout/src/values.js:33-44 | `border-radius: 4px 8px` -> `error: css: line 1: border-radius: "4px 8px" is not a length`; `<scratch>/demo.css` `border-radius: 16px` produced `radius: 16` on the card rects | verified |
| css.border-radius.cost | A rounded box is emitted as a nine-cell patch, so `border-radius` adds **8 records per box**, flat, whatever the radius. A radius at half the shorter side (a pill) leaves the middle row of cells with zero height, those three are not emitted, and the cost is **5**. `slice_quads` skips a zero-size cell and `cell_empty` skips an uncovered one, and the row does not attribute the drop to one of them: weakening the `slice_quads` guard alone did not change the count, so something downstream covers the same case. The corner mask is **one texture per distinct radius**, shared across boxes and independent of colour, because the patch key is `(radius, border_w, part)` and the comment there says the colours are left out on purpose. Its image is `2*cell+1` square with `cell` one px larger than the radius, so `(2r+3)`: 19x19 for `8px`, 51x51 for `24px`, and every one charged 8 KiB of VRAM because that is the smallest page allocation. | packages/baker/ps2ui_bake/ninepatch.py:72-79 (`patch_key`), packages/baker/ps2ui_bake/ninepatch.py:82-89 (`NinePatch`), packages/baker/ps2ui_bake/ninepatch.py:155-176 (`slice_quads`) | through `Flattener`, one hand-built rect: 1 record square, 9 at any radius, 6 as a pill -- the only figures here that do not depend on a fixture. **End to end they do**, and review of #159 is why this row says so: a scratch project measured `3 records, 1 textures` square and `11 records, 2 textures` at `border-radius: 8px`, with 1/2/4/8 boxes at 3/5/9/17 and 11/21/41/81, while F38's own row recorded 3/6/12/24 and 11/22/44/88 for what reads like the same fixture. Both are right: **text is one record per glyph**, so the row's two-character label costs 3 a box and this one-character label costs 2. Measured directly: `x` 1, `hi` 2, `hello` 5, `Final Fantasy X` 13 -- fifteen characters and thirteen records, because a space emits nothing. Either fixture gives +8 a box. Radii 2, 4, 8, 16 and 24 each gave 11 records and a corner texture of 7x7, 11x11, 19x19, 35x35 and 51x51 PSMT8, every one `-> 8192 B in pages`. A 24px-tall box at `border-radius: 12px` gave 8 records rather than 11, and through `Flattener` a 60x40 rect at radius 20 gave 6 rather than 9, with a spy on `slice_quads` reporting 6 cells yielded. Two boxes at one radius and two colours gave 2 textures; two boxes at one colour and two radii gave 3 | verified |
| css.box-shorthand | `padding` and `margin` take 1 to 4 px values and expand in CSS order. 5 or more is `css: line <n>: padding: 1-4 values` | packages/layout/src/css.js:819-840; packages/layout/src/values.js:126-134 | `padding: 1px 2px 3px 4px 5px` -> `error: css: line 1: padding: 1-4 values`; parse.test.js:158 `css: shorthand box expansion` asserts `[1,2,3,4]` and `[5,6,5,6]` | verified |
| css.gap | `gap` takes one or two px values, row gap first, column gap second; one value sets both. `row-gap` and `column-gap` take one px value each | packages/layout/src/css.js:805-812 | `gap: 8px 12px` compiled with exit 0; `<scratch>/demo.css` uses `gap: 24px` and `gap: 20px`, and the three cards in `<scratch>/demo.json` sit 20px apart (`x` 48, 238, 437 at widths 170, 179, 155) | verified |
| css.font-weight | `font-weight` maps `bold` to 700 and `normal` to 400; anything else is `parseInt`. The font context resolves any weight at or above 600 to the bold face and everything below to the regular face | packages/layout/src/css.js:892-894; packages/layout/src/index.js:61-63 | `font-weight: 600` compiled with exit 0; `resolve(weight)` reads `weight >= 600 ? this.bold : this.regular` | verified |
| css.flex-shorthand | `flex` accepts `none` and `<grow> [<shrink>] [<basis>]`. `flex: 1` sets grow 1, shrink 1, basis 0px | packages/layout/src/css.js:795-804 | `flex: 1` compiled with exit 0; the shorthand's basis default is read from `style.flexBasis = basisTok ? ... : { unit: 'px', value: 0 }` | verified |
| css.images.no-stretch | An `<img>` is never stretched by `align-items: stretch`. The stretch branch is skipped for a box carrying an image, so a replaced element keeps its intrinsic aspect unless the cross size is explicit | packages/layout/src/flex.js:613-620 | layout.test.js:572 `image: <img> measures intrinsically and emits an image command` (8x6 in a `flex-direction: row` container under the default `align-items: stretch`) and layout.test.js:651 `image: one specified axis keeps the intrinsic aspect ratio` (32x24); the file passes 71/71 | verified |
| css.at-rules | `@theme <name>` is the only at-rule with meaning. Any other at-rule is skipped with `css: line <n>: at-rule "<name>" ignored` and its body is discarded | packages/layout/src/css.js:328-357 | parse.test.js:166 `css: at-rules are skipped with a warning, not a parse failure` asserts one surviving rule and a `@media` warning | verified |
| css.syntax.errors | Parse errors, all prefixed `css: line <n>:` with no exception since F37: `malformed declaration "<d>"`, `selector without a block`, `unterminated block`, `unterminated comment`, `unsupported selector syntax near "<c>" in "<s>"`, and for a pseudo-class `"<s>": :<name> does not exist on this target. :focus is the only pseudo-class -- a pad-driven UI has no pointer, so there is no hover, active or visited state.`. The selector errors are also the only ones a sheet can carry SEVERAL of in one pass: the rule is skipped and the parse reads on, since a selector that does not parse heads a rule that could not have applied | packages/layout/src/css.js:160-167, packages/layout/src/css.js:246, packages/layout/src/css.js:263, packages/layout/src/css.js:300, packages/layout/src/css.js:361, packages/layout/src/css.js:365, packages/layout/src/css.js:398-406 | compiled one sheet per case, all six re-measured for F37: `error: css: line 1: malformed declaration "color #fff"`, `error: css: line 1: selector without a block`, `error: css: line 1: unterminated block`, `error: css: line 1: unterminated comment`, `error: css: line 1: unsupported selector syntax near ">" in ">"`, `error: css: line 1: ".a:hover": :hover does not exist on this target. ...` | verified |
| css.readme.property-list | **Fixed in 0.7.0.** README.md's list omitted `opacity`, `min-width`, `min-height`, `max-width`, `max-height`, `align-self`, the `flex` shorthand, `row-gap` and `column-gap`, all of which `applyDeclaration` handles. All nine are now named, `border-radius` is stated as one px length, and `:root`/`var()`/`@theme` have a line | packages/layout/src/css.js:787, 795, 811, 812, 815, 816, 817, 818, 913 (the nine `case` lines, in the order the fact names them); README.md:217-227 | each of those properties compiles without a "not supported" warning; README.md:217-219 name them; drift row D7 | was-contradicts-readme, fixed |
| css.readme.focus-guard | **Fixed in 0.7.0, in the document rather than the code.** README.md said a `:focus` rule that changes geometry is a compile error and stopped there. The guard is a fixed list and `font-weight`, `letter-spacing`, `text-align` and `text-overflow` are outside it although they change text geometry. The defect is still open; the README now states the exception instead of overstating the guard | packages/layout/src/css.js:41-49; README.md:247-254 | measured on a `focusable` element, since a `:focus` compound on a non-focusable one is dropped before the guard runs and exits 0 either way: `width`, `padding`, `font-size`, `line-height`, `overflow` and `flex-grow` each exit 1 naming the property; `font-weight`, `letter-spacing`, `text-align` and `text-overflow` each exit 0; drift row D7 | was-contradicts-readme, fixed |
| css.demo | `docs/site/assets/authoring/css/demo/demo.html` and `demo.css` compile to 26 paint commands and 3 focusables, and bake to the two page screenshots | the committed demo sources | `node packages/layout/bin/ps2ui-layout.js docs/site/assets/authoring/css/demo/demo.html docs/site/assets/authoring/css/demo/demo.css --fonts fonts/fonts.json -o OUT.json && PYTHONPATH=packages/baker python3 -m ps2ui_bake OUT.json -o OUT.uib --fonts fonts/fonts.json --preview OUT` (and `--montage OUT`); `python3 tools/check-site-assets.py` printed `1..4` with `ok 3` and `ok 4` for both files | verified |

## follow-up

Not in the drift table, found while verifying:

1. `position` is listed in `GEOMETRY_PROPS` (packages/layout/src/css.js:26) but
   has no `case` in `applyDeclaration`, so it can never be applied. The entry
   only changes which diagnostic an author sees: inside a `:focus` rule
   `position` is refused as a geometry change, and outside one it warns as an
   unsupported property. Either drop it from the set or give it a `case` that
   errors by name.
2. ~~A `:focus` compound that matches no focusable element is dropped without
   a diagnostic.~~ **Closed, and this row was stale rather than open**: the
   warning the brief expected exists (packages/layout/src/css.js:985-992).
   `.panel:focus` over a `.panel` with no `focusable` attribute now prints
   `css: line 2: ".panel:focus" matches <div> line 1, but no element in that
   selector has the focusable attribute, so the :focus delta can never show.`
   Measured on this tree and on `main`, so it was already true when the F37
   change began; the item is kept struck through rather than deleted because
   what it records is that the page said the opposite of the compiler for as
   long as it did.
3. `letter-spacing`, `text-align` and `text-overflow` inside a `:focus` rule are
   accepted and then discarded at emission (packages/layout/src/paint.js:138-158
   reads `box.style`, never `box.focusStyle`). `font-weight` in the same position
   is honoured for the drawn face but not for the measurement. All four are
   candidates for `GEOMETRY_PROPS`, or for a warning of their own.
