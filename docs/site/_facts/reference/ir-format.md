# facts: reference/ir-format

Session commands were run from the repository root. `<scratch>` is `/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/reference/ir-format`. The memcard screen was compiled with `ps2ui-layout examples/memcard/ui/library.html examples/memcard/ui/library.css --fonts fonts/fonts.json -o <scratch>/library.json`; the themed screens with the same command over `examples/opl-env/ui/landing.html` and `examples/opl-env/ui/library.html` against `examples/opl-env/ui/opl.css`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| ir.version | The emitted `version` is the integer 1, from `IR_VERSION` | packages/layout/src/index.js:49, :239 | printed `library.json`: `"version": 1` | verified |
| ir.version.check | `ps2ui-bake` refuses an IR whose `version` is not 1 with `error: <path>: IR version <n>, expected 1` and exit 1, before reading anything else | packages/baker/ps2ui_bake/cli.py:168-171 | copied `library.json`, set `version` to 2, ran `ps2ui-bake <copy> -o <scratch>/v2.uib --fonts fonts/fonts.json`; stderr `error: .../library-v2.json: IR version 2, expected 1`, exit 1 | verified |
| ir.version.no-check-in-layout | Nothing in the layout package reads `version`; `IR_VERSION` is written once and never compared | packages/layout/src/index.js:49, :239 (only uses) | `grep -n IR_VERSION packages/layout/bin/*.js packages/layout/src/*.js` returns the two lines | verified |
| ir.schema | Top-level keys, in emission order: `version`, `canvas`, `fonts`, `themes`, `commands`, `focus`, `slots`, `warnings` | packages/layout/src/index.js:238-263 | Node key-path walk over `library.json`; the field tables on the page list every path the walk printed | verified |
| ir.schema.canvas | `canvas` is `{w, h, displayAspect: [num, den], par, display: {w, h}}`; `par` = (num/den)/(w/h) rounded to 4 decimals; `display.w` = round(h * num/den), `display.h` = h | packages/layout/src/index.js:240-248; packages/layout/src/aspect.js:33-35, :42-44 | printed memcard `canvas`: `{"w":640,"h":448,"displayAspect":[4,3],"par":0.9333,"display":{"w":597,"h":448}}` | verified |
| ir.schema.canvas.default | With no `--mode`, `--canvas` or `--display-aspect`, the canvas is 640x448 at 4:3 | packages/layout/src/index.js:137-140 | memcard compile used none of the flags and printed 640x448, `[4,3]` | verified |
| ir.schema.fonts | `fonts.regular` and `fonts.bold` are each `{family, weight}` copied from the loaded metrics | packages/layout/src/index.js:249-252 | printed `{"regular":{"family":"DejaVu Sans","weight":400},"bold":{"family":"DejaVu Sans","weight":700}}` | verified |
| ir.schema.themes | `themes` is an array of theme names, index-ordered, `root` first; it is `["root"]` when the sheet declares no `@theme`; its length is the width of every `*Themes` vector | packages/layout/src/index.js:179, :253-258 | memcard printed `["root"]`; opl-env landing printed `["root","light"]`; the invariant walk reported `vectorWidthOk: true` on all three screens | verified |
| ir.schema.themes.row0 | Element 0 of every `fillThemes`, `borderColorThemes`, `colorThemes`, `colorBaseThemes`, `colorFocusThemes` vector equals the scalar colour beside it | packages/layout/src/paint.js:75, :82, :105 | invariant walk `row0IsBase: true` on all three screens | verified |
| ir.schema.commands.ops | `op` is one of `rect`, `text`, `image`, `scissor_push`, `scissor_pop`; the baker raises `unknown IR command op` on anything else | packages/layout/src/paint.js:119, :143, :302, :318, :325; packages/baker/ps2ui_bake/quads.py:556-575 | ops set printed from the three screens: `rect`, `text`, `scissor_push`, `scissor_pop` (memcard) plus `image` (opl-env); each baked with exit 0 | verified |
| ir.schema.commands.state | Every command carries `state` in `always`, `unfocused`, `focused` and `focusId` (null or a focus node id); the baker maps unknown ids to `FOCUS_NONE` | packages/layout/src/paint.js:7-8; packages/baker/ps2ui_bake/quads.py:40, :143-147 | invariant walk: `states` = the three strings, `cmdFocusIdsResolve: true` | verified |
| ir.schema.commands.pairing | A box whose base and `:focus` paint differ emits one `unfocused` and one `focused` command with identical geometry; identical paints merge to one `always` command | packages/layout/src/paint.js:270-292, :112-114 | invariant walk `focusPairsShareGeometry: true`; memcard 18 unfocused/18 focused; layout.test.js:812 `integration: the memcard example compiles to the documented shape` | verified |
| ir.schema.rect | `rect` fields: `x y w h fill fillVar fillThemes borderWidth borderColor borderColorVar borderColorThemes radius state focusId` and optional `keep` | packages/layout/src/paint.js:116-136 | printed memcard `commands[0]` and opl-env rect with `fillVar: "--bg-page"` and a two-row `fillThemes` | verified |
| ir.schema.rect.radius | `radius` is clamped to floor(min(w, h) / 2) at emission | packages/layout/src/paint.js:128 | invariant walk `radiusOk: true` on all three screens | verified |
| ir.schema.rect.normalize | A fill with alpha 0 becomes `fill: null`; a border with zero width or transparent colour becomes `borderWidth: 0`, `borderColor: null`; a rect with neither is not emitted; the decision reads theme 0 only | packages/layout/src/paint.js:59-98, :117 | `node --test packages/layout/test/layout.test.js` 71 pass, including `theme: a fill that no theme paints does not exist in any row` (layout.test.js:988) | verified |
| ir.schema.rect.keep | `keep: true` appears only on rects whose element carries `data-keep`; the key is absent otherwise; the baker reads it to skip the dead-geometry trim | packages/layout/src/paint.js:134; packages/layout/src/box.js:177; packages/baker/ps2ui_bake/quads.py:212 | compiled `<scratch>/flags.html`; the `data-keep` rect printed `"keep":true`, no other command has the key; layout.test.js:341 | verified |
| ir.schema.text | `text` fields: `x y text size weight letterSpacing color colorVar colorThemes state focusId` and optional `nocontrast`; one command per laid-out line; `y` is the line top plus floor(leading / 2) | packages/layout/src/paint.js:138-159 | printed memcard text command `{"op":"text","x":28,"y":25,"text":"PS2",...}` | verified |
| ir.schema.text.nocontrast | `nocontrast: true` appears only for elements with `data-nocontrast`; the baker never reads it | packages/layout/src/paint.js:156; packages/layout/src/box.js:193 | `flags.html` text printed `"nocontrast":true`; `grep -rn nocontrast packages/baker/ps2ui_bake/` returns nothing; layout.test.js:253 | verified |
| ir.schema.image | `image` has two forms: baked `{src, palettize}` with `src` an absolute build-host path, or streamed `{streamed: true, name}`; both carry `x y w h state focusId`; `state` is always `always` | packages/layout/src/paint.js:295-313; packages/baker/ps2ui_bake/quads.py:421-445 | printed opl-env landing `src` image (`palettize: true`) and opl-env library `streamed` image (`name: "row-0-art"`) | verified |
| ir.schema.scissor | `scissor_push` carries `x y w h`; `scissor_pop` carries no geometry; both have `state: "always"`, `focusId: null`; a push/pop pair brackets the children of every non-text box with `overflow: hidden` | packages/layout/src/paint.js:315-326 | printed memcard `scissor_push` and `scissor_pop`; invariant walk `scissorAlways: true`, depth returns to 0; layout.test.js:505 | verified |
| ir.schema.focus | `focus` is `{nodes, initial}`; a node is `{id, name, rect: [x, y, w, h], up, down, left, right}` with neighbours as node ids or null; `name` is the element `id`, else `name`, else `box<id>`; `initial` is the `autofocus` node, else the first node, else null | packages/layout/src/focus.js:109-132 | printed memcard node `{"id":9,"name":"nav-games","rect":[28,92,128,39],"up":null,"down":11,"left":null,"right":28}`, `initial: 9`; `<scratch>/plain.html` with no focusables printed `{"nodes":[],"initial":null}`; layout.test.js:519, :537 | verified |
| ir.schema.focus.resolve | Every neighbour id and every command `focusId` names a node in the same file; nodes are in document order (ascending id) | packages/layout/src/focus.js:110-123; packages/baker/ps2ui_bake/quads.py:541-552 | invariant walk `neighboursResolve: true`, `docOrder: true` on all three screens | verified |
| ir.schema.slot | A slot is `{name placeholder x textY w size weight lineHeight align letterSpacing ellipsis capacity focusId colorBase colorFocus colorBaseVar colorFocusVar colorBaseThemes colorFocusThemes}`; `x`/`w` are the parent's content box, `textY` the glyph-box top | packages/layout/src/paint.js:208-238; packages/baker/ps2ui_bake/quads.py:661-688 | printed memcard slot `count` and opl-env slot `hero-sub` with `colorBaseVar: "--ink-muted"` and two-row vectors; `flags.html` slot inside a focusable printed `focusId: 5` and differing base/focus colours | verified |
| ir.schema.slot.capacity | `capacity` defaults to 63 when `data-slot-capacity` is absent; the baker does not clamp it | packages/layout/src/box.js:212; packages/baker/ps2ui_bake/quads.py:669-677 | `<scratch>/wrap.html` slot without the attribute printed `"capacity":63` | verified |
| ir.schema.slot.unique | Two `data-slot` elements with one name in one document are a compile error `layout: duplicate data-slot name "<n>"`; the baker repeats the check across screens | packages/layout/src/paint.js:329-335; packages/baker/ps2ui_bake/quads.py:601-611 | compiled `<scratch>/dup.html`: `error: layout: duplicate data-slot name "a"`, exit 1 | verified |
| ir.schema.slot.single-line | A slot placeholder that wraps to more than one line is a compile error | packages/layout/src/paint.js:196-202 | code-only: the scratch attempt sized the slot box to the canvas width and never wrapped, so the throw was not reached | code-only |
| ir.schema.warnings | `warnings` is an array of strings: repeat, css, box and focus warnings first, then lint lines as `<rule>: <message>`, then per-theme lint lines as `@theme <name>: <rule>: <message>`; geometry lints are deduplicated across themes, colour lints (`contrast`, `ntsc-red-bleed`) are not | packages/layout/src/index.js:146-153, :183, :219-236, :272 | `flags.html` printed six `css:` lines, then `ntsc-red-bleed: ...`, then `@theme light: ntsc-red-bleed: ...`; layout.test.js:1078 | verified |
| ir.geometry.integral | Every `x`, `y`, `w`, `h` on every command is an integer | packages/layout/src/text.js (integral advances), packages/layout/src/values.js `roundHalfUp` | invariant walk `integral: true` on all three screens; layout.test.js:32 `text: measure sums integral advances, never float widths` | verified |
| ir.geometry.focus-paint-only | A `:focus` rule that changes a geometry property is a compile error, so both states share one layout | packages/layout/src/css.js:739-796 | compiled `<scratch>/geo.css` (`.s:focus { width: 120px }`): `error: css: line 1: :focus may not change "width" — focus is a paint-only delta...`, exit 1 | verified |
| ir.colour.domain | Every colour is `[r, g, b, a]` with integer channels 0 to 255; `opacity` is folded into alpha in every theme row before emission | packages/layout/src/paint.js:30-33; docs/format-ir.md:9-11 | invariant walk `channels0to255: true`; layout.test.js:951 `theme: the opacity fold runs over every row` | verified |
| ir.baker.reads | The baker reads `canvas`, `canvas.displayAspect` (default `(4, 3)`), the length of `themes` (default 1), `commands`, `focus.nodes`, `focus.initial`, `slots`, `warnings`; it does not read `fonts`, `canvas.par` or `canvas.display` | packages/baker/ps2ui_bake/cli.py:177, :282-285; packages/baker/ps2ui_bake/quads.py:541-579 | `grep -n '"par"\|"display"\|ir\["fonts"\]' packages/baker/ps2ui_bake/*.py` finds no IR read of those keys; bakes of `library.json` and `landing.json` exit 0 | verified |
| ir.baker.var-names | `fillVar`, `borderColorVar`, `colorVar`, `colorBaseVar`, `colorFocusVar` are the names the baker's tint table keys on; a literal colour has `null` | packages/baker/ps2ui_bake/quads.py:61-67, :233-236, :263, :272, :454, :682-683 | `ps2ui-bake <scratch>/landing.json ... --tints` printed `# 12 tint entries over 2 theme(s): root, light` with `--bg-page`, `--ink-title` beside their rows | verified |
| ir.multi-screen | One `ui.json` is one screen; the screen name is the file stem; duplicate stems are a bake error | packages/baker/ps2ui_bake/cli.py:172-176 | `ps2ui-bake --help` positional `ir [ir ...]`: "several files become named screens in one blob (screen name = file stem)" | verified |
| ir.memcard.totals | The memcard library screen compiles to 89 commands (53 `always`, 18 `unfocused`, 18 `focused`), 9 focus nodes, 1 slot and 28 warnings | packages/layout/src/index.js:238-263 (emission); the compiled `library.json` | `ps2ui-layout examples/memcard/ui/library.html examples/memcard/ui/library.css --fonts fonts/fonts.json -o library.json` printed `89 paint commands, 9 focusables`; a Node count over the file printed `{"always":53,"unfocused":18,"focused":18}`, `slots: 1`, `warnings: 28` | verified |

## follow-up

`docs/format-ir.md` is stale in four places (drift row D8). A separate change should bring it in line with `packages/layout/src/index.js:238-263` and `packages/layout/src/paint.js`:

1. `docs/format-ir.md:16` shows `"canvas": { "w": 640, "h": 448 }`. The emitted `canvas` also carries `displayAspect`, `par` and `display` (index.js:240-248).
2. `docs/format-ir.md` never mentions the top-level `themes` array (index.js:258); the top-level listing at lines 14-33 omits it.
3. The `rect` block at `docs/format-ir.md:53-60` and the `text` block at `:81-86` omit `fillVar`, `fillThemes`, `borderColorVar`, `borderColorThemes`, `colorVar`, `colorThemes` (paint.js:121-127, :151-153).
4. The slot block at `docs/format-ir.md:172-182` omits `colorBaseVar`, `colorFocusVar`, `colorBaseThemes`, `colorFocusThemes` (paint.js:234-237).

Also observed, not in D8: the `image` block at `docs/format-ir.md` documents only the `src`/`palettize` form; the streamed form `{streamed: true, name}` (paint.js:307-309) is absent.
