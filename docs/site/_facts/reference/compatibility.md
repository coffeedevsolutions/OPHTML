# facts: reference/compatibility

Session commands behind the rows below, run from the repository root.

- `python3 tools/check-versions.py --except-tag` — 27 `ok -` lines, 1
  `skip -` line, exit 0.
- `ps2ui --version` — `ps2ui 0.6.0.dev0`.
- `node packages/layout/bin/ps2ui-layout.js --version` — `ps2ui-layout
  0.6.0-dev.0`.
- `python3 tools/check-format-frozen.py` — 19 `ok -` lines, exit 0.

Line numbers were re-located by symbol in this session.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| compat.versions | The tree carries five version numbers: `ophtml` (PyPI) 0.6.0.dev0, `@ophtml/layout` (npm) 0.6.0-dev.0, the ui.json IR 1, the `.uib` format 7, and the runtime macro `PS2UI_VERSION` 7. `check-versions.py` holds all five to each other. | packages/baker/ps2ui_bake/__init__.py:39; packages/layout/package.json:3; packages/layout/src/index.js:49; packages/baker/ps2ui_bake/uib.py `VERSION`; runtime/ps2ui.h:37 | `python3 tools/check-versions.py --except-tag` printed "ok - @ophtml/layout 0.6.0-dev.0 and ophtml 0.6.0.dev0 are the same version" and "ok - PS2UI_VERSION and uib.VERSION are both 7"; `ps2ui --version` printed `ps2ui 0.6.0.dev0`; `node packages/layout/bin/ps2ui-layout.js --version` printed `ps2ui-layout 0.6.0-dev.0`; the built `examples/memcard/build/library.json` carries `"version": 1`. Restates parent facts `cli.ps2ui.version`, `cli.ps2ui.checkout`, `uib.header.magic` | verified |
| compat.versions.macro-not-a-guard | `PS2UI_VERSION` is the frozen `.uib` format version, not a mechanism that keeps the baker and the runtime from drifting apart. They agree because `ps2ui vendor-runtime` ships both files from one package, one commit. README.md's Quick start comment claims the macro itself is the safeguard (D12). | runtime/ps2ui.h:37; packages/baker/ps2ui_bake/vendor.py:15-24 | restates parent facts `uib.pledge.version-hold` and `integrate.version`, both already `contradicts-readme` against the same README.md line | contradicts-readme |
| compat.format-matrix | Format v7 is the last incompatible `.uib` layout: 0.3.0 introduced it (the stability pledge) and 0.4.0, 0.5.0 and the open 0.6.0.dev0 section each record zero format moves since the release below them. A v7 runtime loads every v7 blob and a v7 baker's blob loads under every v7 runtime, whatever the two products' version numbers are. A blob baked before the pledge (format v1 through v6) is refused by a current runtime, and a current blob is refused by a pre-pledge runtime, both with `PS2UI_ERR_VERSION`. | CHANGELOG.md lines 183-187 ("Zero format moves have landed since 0.5.0 ... a blob baked here loads under a 0.5.0 runtime and the other way round"), lines 260-264 (the pledge, made at v7), line 337 ("v3 through v6 files are rejected; re-bake"); runtime/ps2ui.c:173, 267; runtime/ps2ui.h:427 (`PS2UI_ERR_VERSION`) | `python3 tools/check-format-frozen.py` printed "ok - the format is v7, the version the pledge froze" and 18 further `ok -` lines proving every struct and feature bit unchanged, exit 0. The two rejection directions are read from the header version compare at runtime/ps2ui.c:173 and 267; no pre-v7 blob or runtime binary exists in this tree to load against a v7 counterpart, so the rejection direction is code-only | verified (the v7-to-v7 load claim); code-only (the pre-v7 rejection, no old blob or binary in this tree to run) |
| compat.registries | npm resolves a plain `npm install @ophtml/layout` against the `latest` dist-tag; `publishConfig.tag` is `"next"` while the version is a prerelease, so a publish of this tree lands under `@next` and `latest` stays unset. PyPI has no dist-tag; pip excludes a prerelease from a plain `pip install ophtml` unless no stable version satisfies the request, and 0.3.0 through 0.5.0 already do. | packages/layout/package.json:4-7 (`publishConfig`); docs/releasing.md "What a prerelease actually protects" | `python3 tools/check-versions.py --except-tag` printed "ok - @ophtml/layout publishes to the 'next' dist-tag, so a publish of this prerelease would not take \`latest\`" and "ok - @ophtml is scoped and publishes with access: public" | verified |
| compat.platforms | Node.js needs 18 or newer; Python needs 3.9 or newer; the baker needs Pillow 9 or newer. | packages/layout/package.json:22; packages/baker/pyproject.toml:11-12 | read directly from both manifests in this session | verified |
| compat.platforms.macos-fribidi | Both macOS Pillow wheels compile Raqm into `_imagingft` but load `fribidi` at run time via `dlopen`; a plain `pip install ophtml` on a macOS runner with no fribidi installed makes `ps2ui-fontgen` refuse. `brew install fribidi` is the fix; a Pillow source rebuild is the fallback only when that alone does not clear it. | CHANGELOG.md "Unreleased" Fixed entry (`_raqm_remedy()`), lines 5-31; .github/workflows/registry.yml `macos-plain` job, lines 188-232 | code-only in this session: no macOS runner is available here, so the CHANGELOG entry and the CI job's own commentary are the evidence, not an execution. Restates parent scope `runtime/integrating`'s no-docker caveat for the equivalent reason | code-only |
| compat.platforms.host-compilers | The host test suite compiles with whatever `cc` resolves to by default and also with clang; both build the same 27 syntax-check lines. There is no separate `test-compat` target (drift D1). | runtime/Makefile:4 (`CC ?= cc`) | restates parent fact `integrate.test-targets`: `make -C runtime syntax-check CC=clang` printed the same 27 `ok -` lines as the default-`cc` run, exit 0 | verified |
| compat.platforms.gskit-pin | The host suite compiles against gsKit headers vendored verbatim from `ps2dev/gsKit` commit `43122eb96289167975b56caa45beb71eb8684fa2`. The console's PS2SDK toolchain is `ghcr.io/ps2dev/ps2dev:latest`, deliberately left unpinned so a struct-shape or TFX change upstream is caught by watching, not frozen out. | runtime/vendor/README.md:3-7; .github/workflows/hw.yml:47, 163 | restates parent fact `integrate.gskit-pin`, itself code-only: no container runtime or network fetch of upstream gsKit in this session | code-only |
| compat.names | `OPHTML` is the product; `ps2ui` names the format and its tooling (`.uib`, `ps2ui.h`, the `ps2ui`/`ps2ui-bake`/`ps2ui-check`/`ps2ui-fontgen`/`ps2ui-layout` commands, the `ps2ui_bake` Python module). The registries carry the product's names, `ophtml` on PyPI and `@ophtml/layout` on npm; nothing under the ps2ui spelling was renamed with them. | docs/releasing.md "Names, and what deliberately did not change" | read directly from docs/releasing.md in this session; the split is also visible in `python3 tools/check-versions.py --except-tag`'s two name rules (`ok - layout is named @ophtml/layout`, `ok - baker is named ophtml`) alongside the untouched `ps2ui` command and macro names elsewhere in the same run | verified |
