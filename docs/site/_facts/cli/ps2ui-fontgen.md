# facts: cli/ps2ui-fontgen

| id | fact | source | verified by | status |
|---|---|---|---|---|
| fontgen.argv | Bare form: `ps2ui-fontgen <font.ttf> <family> <weight> <out.metrics.json> [charset-file]`. Wrapper form: `ps2ui fontgen <regular> <bold> [-o DIR]`. | packages/baker/ps2ui_bake/fontgen.py:369-397 (`main`); packages/baker/ps2ui_bake/ps2ui.py:511-517 (`sub.add_parser("fontgen")`) | ran `ps2ui-fontgen` with 0, 3, 4 and 5 args; ran `ps2ui fontgen --help` and `ps2ui fontgen fonts/vendor/DejaVuSans.ttf fonts/vendor/DejaVuSans-Bold.ttf -o <scratch>` | verified |
| fontgen.argv.too-few | Fewer than four positional arguments prints the usage line to stderr and exits 2. The usage line spells the checkout form `python -m ps2ui_bake.fontgen` even when the installed `ps2ui-fontgen` is invoked. | packages/baker/ps2ui_bake/fontgen.py:387-392 | `ps2ui-fontgen` (no args) and `ps2ui-fontgen <ttf> DejaVu 400` both printed `usage: python -m ps2ui_bake.fontgen <font.ttf> <family> <weight> <out.metrics.json> [charset-file]` and exited 2 | verified |
| fontgen.version | `--version` or `-V` as the first argument prints `ps2ui-fontgen <__version__>` and exits 0, before the Raqm check. This tree is the 0.8.0 cut, so it prints `0.8.0`. | packages/baker/ps2ui_bake/fontgen.py:373-375; packages/baker/ps2ui_bake/__init__.py:39 | `ps2ui-fontgen --version` and `ps2ui-fontgen -V` both printed `ps2ui-fontgen 0.8.0`, exit 0 | verified |
| fontgen.entry-point | `ps2ui-fontgen` is the console script for `ps2ui_bake.fontgen:main`; `ps2ui` is `ps2ui_bake.ps2ui:main`. | packages/baker/pyproject.toml:21,24 | both commands resolve on PATH and behave as the modules do | verified |
| fontgen.outputs | The wrapper writes three files into `--out-dir`: `default.metrics.json` (weight 400 from `<regular>`), `default-bold.metrics.json` (weight 700 from `<bold>`), and `fonts.json`. It prints one `ps2ui-fontgen:` stderr line per file. | packages/baker/ps2ui_bake/ps2ui.py:345-370 (`cmd_fontgen`) | ran the wrapper into scratch; `ls` listed exactly those three files; stderr showed `115 glyphs, 284 kern pairs`, `115 glyphs, 163 kern pairs`, `manifest -> .../fonts.json` | verified |
| fontgen.outputs.family | The wrapper passes the family name `default` to both faces; it does not read the TTF's family name. `fonts/regen.sh` passes `DejaVu Sans` instead. | packages/baker/ps2ui_bake/ps2ui.py:358-359; fonts/regen.sh:49 | wrapper output `default.metrics.json` has `"family": "default"`; committed `fonts/default.metrics.json` has `"family": "DejaVu Sans"` | verified |
| fontgen.outputs.outdir | `-o` / `--out-dir` defaults to `fonts`. The directory is created with `os.makedirs(..., exist_ok=True)` before any face is generated. | packages/baker/ps2ui_bake/ps2ui.py:354-356,423-424 | `ps2ui fontgen --help` printed `(default: fonts/)`; on a mocked refusal the out dir existed and was empty | verified |
| fontgen.outputs.manifest-abs | The wrapper's `fonts.json` stores each TTF as a one-element list holding `os.path.abspath()` of the argument, and `metrics` as the bare file name. | packages/baker/ps2ui_bake/ps2ui.py:380-387 | scratch `fonts.json` read `"ttf": ["/home/user/OPHTML/fonts/vendor/DejaVuSans.ttf"]` after passing the relative path `fonts/vendor/DejaVuSans.ttf` | verified |
| fontgen.outputs.stop-on-error | The wrapper returns the bare tool's non-zero code from the first failing face and writes no manifest. | packages/baker/ps2ui_bake/ps2ui.py:358-362 | called `cmd_fontgen` with `features.check` mocked False: return 2, out dir empty | verified |
| fontgen.raqm | When `PIL.features.check("raqm")` is False, `main` prints `ps2ui-fontgen: this Pillow has no Raqm layout engine, so kerning cannot be extracted; refusing to write a metrics file without it.` plus a remedy to stderr and returns 2 before opening the TTF or writing anything. | packages/baker/ps2ui_bake/fontgen.py:382-386; packages/baker/tests/test_baker.py:157-181 (`TestFontgenRefusesWithoutRaqm.test_main_exits_nonzero_and_writes_nothing`) | `python3 -m unittest discover -s tests -p test_baker.py -k TestFontgenRefusesWithoutRaqm` in packages/baker: OK; also ran `fontgen.main` with `features.check` mocked False: exit 2, output file absent. This machine has Raqm (Pillow 12.3.0 reports raqm True, fribidi True), so the unmocked tool never refuses here. | verified |
| fontgen.raqm.remedy | The remedy reports Pillow version, platform and machine, asks `features.check("fribidi")` separately, and branches: fribidi False leads with `brew install fribidi` / `apt install libfribidi0` / `dnf install fribidi`, adds a Windows-only paragraph naming `fribidi-0.dll`, `libfribidi-0.dll` and `fribidi.dll` and the `PATH` requirement, and then `_escalation()`; fribidi True says it is not the usual cause and goes straight to `_escalation()`. Both end with the `features.check('raqm')` one-liner. | packages/baker/ps2ui_bake/fontgen.py:103-227 (`_raqm_remedy`), 229-286 (`_escalation`), 288-327 (`_windows_fribidi_hint`), 330-366 (`_rebuild_hint`) | ran `fontgen.main` with `features.check` mocked to False for both names, then mocked to False for raqm only; both messages pasted on the page. Tests `test_the_macos_remedy_is_prefix_relative_and_names_the_trap` and `test_it_reports_what_it_detected_and_asserts_no_platform_rule` pass. | verified |
| fontgen.raqm.rebuild-hint | On darwin the rebuild hint is `brew install libraqm`, a `PKG_CONFIG_PATH` built from `$(brew --prefix)`, then `pip install --no-binary pillow --force-reinstall pillow`. It has no win32 arm at all: `_escalation()` routes that platform away from the source build, because a rebuild on Windows wants MSVC and a native dependency chain first. Elsewhere it is the pip line, with the darwin arm adding `brew install libraqm` and a `PKG_CONFIG_PATH`. Both warn against `--no-binary :all:`. | packages/baker/ps2ui_bake/fontgen.py:330-366 | linux branch pasted from this session's mocked run; darwin branch asserted by `test_the_macos_remedy_is_prefix_relative_and_names_the_trap`, win32 by `test_windows_is_told_to_supply_the_dll_and_not_to_rebuild`, and the escalation's shape on all three platforms by `test_the_escalation_never_repeats_the_advice_it_escalates_from`; all passed this session | verified |
| fontgen.metrics-schema | Top-level keys, sorted: `advances`, `ascent`, `descent`, `family`, `kerning`, `missing`, `source`, `unitsPerEm`, `weight`. `advances` maps decimal codepoint strings to integer units. `kerning` maps `"prev,cur"` codepoint pairs to integer units. | packages/baker/ps2ui_bake/fontgen.py:90-100 (`build_metrics`) | printed `sorted(json.load(open('fonts/default.metrics.json')).keys())` and the same on the wrapper output | verified |
| fontgen.metrics.units | The face is loaded at a 1000px em, so `unitsPerEm` is always 1000 and advances are integer units per 1000/em. `ascent` and `descent` come from `font.getmetrics()` at that size. | packages/baker/ps2ui_bake/fontgen.py:80-82,93 | committed regular metrics: `unitsPerEm` 1000, `ascent` 929, `descent` 236; bold: same three values | verified |
| fontgen.metrics.missing | `missing` is the advance of `?` when `?` is in the charset, else 500. | packages/baker/ps2ui_bake/fontgen.py:98 | default charset gives `missing` 531 (regular) and 580 (bold), equal to `advances["63"]`; a charset file holding `ABC` gave `missing` 500 | verified |
| fontgen.metrics.source | `source` is the TTF path after the last `/`, the file name only. | packages/baker/ps2ui_bake/fontgen.py:99 | `fonts/vendor/DejaVuSans.ttf` produced `"source": "DejaVuSans.ttf"` | verified |
| fontgen.metrics.format | The file is written with `json.dump(indent=1, sort_keys=True)` and a trailing newline. Keys therefore sort as strings (`"100"` before `"32"`). | packages/baker/ps2ui_bake/fontgen.py:399-401 | `head` of fonts/default.metrics.json shows one-space indent and `"100"`, `"10003"` before `"101"` | verified |
| fontgen.metrics.advance-rounding | Each advance is `int(round(font.getlength(ch, features=feat)))`; codepoints below 32 are skipped. | packages/baker/ps2ui_bake/fontgen.py:85-89 | charset file `ABC\n` produced three advances and no entry for 10 | verified |
| fontgen.charset.default | `DEFAULT_CHARSET` is `chr(32)` + `string.printable.strip()` + a 20-character literal (U+00A0, `·–—‘’“”…×`, `△○□◇✕✓←→↑↓`). It has 115 distinct codepoints, lowest 32, highest 10005. | packages/baker/ps2ui_bake/fontgen.py:26-29 | printed `len(set(DEFAULT_CHARSET))` = 115 and `repr`; `fonts/default.metrics.json` has 115 advances, min key 32, max key 10005 | verified |
| fontgen.charset.nbsp | The literal still begins with U+00A0, so `advances["160"]` exists and equals `advances["32"]`. The comment above the literal describes this character as a past defect; the explicit `chr(32)` fixed the space, the U+00A0 was not removed. | packages/baker/ps2ui_bake/fontgen.py:22-29 | `'160' in advances` is True with value 318, `advances["32"]` is 318 | verified |
| fontgen.charset.file | An optional fifth argument names a UTF-8 file whose entire contents replace the default charset. Duplicates collapse and codepoints below 32 (newlines) are dropped. | packages/baker/ps2ui_bake/fontgen.py:394-397,85-88 | file holding `ABC\n` produced `3 glyphs, 3 kern pairs` and advances for 65, 66, 67 only | verified |
| fontgen.kerning.no-substitution | Kerning is measured, not read from a table: `getlength(a+b) - getlength(a) - getlength(b)` for every ordered pair, with features `-liga -clig -dlig -hlig -rlig -calt` so ligatures are not recorded as kerns. | packages/baker/ps2ui_bake/fontgen.py:42,51-76 (`NO_SUBSTITUTION`, `build_kerning`) | `TestKerningExtraction.test_ligatures_are_not_mistaken_for_kerns` passed; `fonts/default.metrics.json` has no `102,102` key and has `84,111` = -170, `65,86` = -64 | verified |
| fontgen.kerning.sparse | Only non-zero rounded pairs are stored; keys are `"<prev>,<cur>"` decimal codepoints; the table is directional. | packages/baker/ps2ui_bake/fontgen.py:72-75 | `test_only_nonzero_pairs_are_stored` and `test_keys_name_codepoints_in_order` passed; regular face has 284 pairs, bold 163 | verified |
| fontgen.kerning.no-raqm-empty | `build_kerning` returns `{}` with a stderr note when Raqm is absent, but `main` refuses earlier, so the empty table is reachable only from Python callers of `build_metrics`. | packages/baker/ps2ui_bake/fontgen.py:57,382-386 | read in source; `main` cannot reach it and no test calls `build_metrics` without Raqm | code-only |
| fontgen.regen | `fonts/regen.sh` regenerates `fonts/default.metrics.json` and `fonts/default-bold.metrics.json` with family `DejaVu Sans` and reproduces the committed files byte for byte. | fonts/regen.sh:38-53 | ran `./fonts/regen.sh` (exit 0), then `git diff --exit-code fonts/` returned 0 with no output; `git status --short fonts/` empty | verified |
| fontgen.regen.manifest | `regen.sh` resolves the TTFs through `load_font_manifest(fonts/fonts.json)`, which takes the first existing candidate path, expanding `~` first and resolving relative candidates against the manifest's directory. | fonts/regen.sh:40-47; packages/baker/ps2ui_bake/cli.py:82-112 (`load_font_manifest`) | regen printed `regen: regular = /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf`, the first candidate in `fonts/fonts.json` | verified |
| fontgen.regen.vendor-identical | The vendored `fonts/vendor/DejaVuSans.ttf` and the system DejaVu produce identical metrics. | fonts/vendor/DejaVuSans.ttf; fonts/fonts.json | `ps2ui-fontgen fonts/vendor/DejaVuSans.ttf "DejaVu Sans" 400 <scratch>` then `cmp` against `fonts/default.metrics.json`: identical | verified |
| fontgen.exit.uncaught | A non-integer `<weight>` raises `ValueError` and a missing or unreadable TTF raises `OSError: cannot open resource`; both are uncaught tracebacks with exit 1 and no output file. | packages/baker/ps2ui_bake/fontgen.py:393,81 | `ps2ui-fontgen <ttf> "DejaVu Sans" bold <out>` exit 1 with `ValueError`; `ps2ui-fontgen <absent> "DejaVu Sans" 400 <out>` exit 1 with `OSError`; neither output file created | verified |
| fontgen.readme | README.md lines 120, 129 and 241 name `ps2ui-fontgen` as a bare command, show `ps2ui fontgen <regular> <bold>`, and show `./fonts/regen.sh`; all agree with the code. | README.md:125,134,307 | compared with the runs above | verified |

## findings

- **Closed by the 0.7.0 cut.** This read "`__version__` is `0.6.0.dev0` while every page carries `version: 0.6.0`" -- a mismatch that is normal mid-cycle, because the page stamp names the published version and `__version__` names the tree. At a release the two meet: both are `0.7.0`. The page pastes the real `--version` output either way.
- The usage line printed by `ps2ui-fontgen` names `python -m ps2ui_bake.fontgen`, not the installed command (fontgen.py:389). Cosmetic; the page documents the argv shape, not the usage string.
- `DEFAULT_CHARSET` still contains U+00A0 as the first character of the literal (fontgen.py:28) after the comment on lines 23-25 describes it as the character that once impersonated the space. Harmless to layout because `&nbsp;` collapses to a space in the compiler (ARCHITECTURE.md D7), but the charset is 115 glyphs rather than the 114 the comment implies.
- `ps2ui fontgen` creates `--out-dir` before the Raqm check runs, so a refused run leaves an empty directory behind (ps2ui.py:228).
- `packages/layout/bin/ps2ui-dev.js` and `packages/layout/bin/ps2ui-layout.js` show as modified in `git status` at the start of this run. Not touched by this agent.
