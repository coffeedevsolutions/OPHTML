# facts: cli/ps2ui-layout

| id | fact | source | verified by | status |
|---|---|---|---|---|
| cli.layout.options | ps2ui-layout takes two positionals (html, css) and `-o` (required); flags: `--canvas WxH`, `--mode ntsc\|ntsc16x9\|pal\|pal16x9`, `--display-aspect W:H`, `--font-dir DIR`, `--fonts fonts.json`, `--focus-wrap`, `--strict`, `--min-font-size PX`, `-h`/`--help`, `-V`/`--version` | packages/layout/bin/ps2ui-layout.js:43-66 | `ps2ui-layout --help` printed the usage line with every flag; `npm test` in packages/layout: 121 tests, 121 pass, 0 fail | verified |
| cli.layout.options.version | `-V` and `--version` print `ps2ui-layout <package.json version>` on stdout and exit 0 | packages/layout/bin/ps2ui-layout.js:19,73-74 | `ps2ui-layout -V` and `--version` both printed `ps2ui-layout 0.8.0-dev.0`, exit 0; test/cli.test.js spawns both bins for both flags | verified |
| cli.layout.options.canvas-default | With no `--mode` and no `--canvas`, the canvas is 640x448 at 4:3 | packages/layout/src/index.js:156-159 | compiled memcard library with neither flag; `canvas` = `{'w': 640, 'h': 448, 'displayAspect': [4, 3], 'par': 0.9333, 'display': {'w': 597, 'h': 448}}` | verified |
| cli.layout.options.precedence | `--canvas` overrides the size `--mode` set; `--display-aspect` overrides the aspect `--mode` set | packages/layout/bin/ps2ui-layout.js:82-88,97-102 | `--mode pal --display-aspect 16:9` gave w 640 h 512 aspect [16,9]; `--mode pal --canvas 512x448` gave w 512 h 448 aspect [4,3] | verified |
| cli.layout.options.min-font-size.default | The min-font-size lint floor defaults to 14px; `--min-font-size` replaces it and reaches the lints through `options.lint` | packages/layout/src/lint.js:21; packages/layout/bin/ps2ui-layout.js:89-95; packages/layout/src/index.js:271 | 10px scratch screen: default run warned `below 14px`; `--min-font-size 8` removed the warning; memcard library: 20 min-font-size warnings at default, 40 with `--min-font-size 40` | verified |
| cli.layout.options.min-font-size.validation | `--min-font-size` with a value that is not a positive integer prints `ps2ui-layout: --min-font-size takes a positive integer` and exits 2 | packages/layout/bin/ps2ui-layout.js:89-93 | `--min-font-size 0` printed that line, exit 2 | verified |
| cli.layout.options.focus-wrap | `--focus-wrap` adds wrap-around edges to the focus graph; the IR has no wrap field, the edges change | packages/layout/bin/ps2ui-layout.js:54,80; packages/layout/src/index.js:234 | compiled memcard library with and without the flag; 7 of 9 focus nodes differ, e.g. `nav-games` gains `up: 13`, `left: 44` | verified |
| cli.layout.options.readme-flags | `--focus-wrap`, `--mode`, `--display-aspect` are ps2ui-layout flags (and ps2ui-dev flags); `ps2ui build` has `--mode` only; ps2ui-bake has none | packages/layout/bin/ps2ui-layout.js:46-54; packages/baker/ps2ui_bake/ps2ui.py `cmd_dev` | `ps2ui-layout --help` lists all three; `ps2ui build --help` lists `--mode` and no other; README.md:450 and README.md:455-456 present them under sections that do not name the tool (drift D4) | contradicts-readme |
| cli.layout.fonts.dir | `--font-dir` reads two fixed filenames, `default.metrics.json` and `default-bold.metrics.json`; a missing one names both files, ps2ui-fontgen, `--font-dir` and `--fonts` | packages/layout/src/index.js:65-75,118-127 | `--font-dir <empty dir>` printed `error: no font metrics at .../default.metrics.json.` plus the generate lines, exit 1; test/fonts.test.js "missing metrics say what to generate, not ENOENT" | verified |
| cli.layout.fonts.manifest | `--fonts` reads the same fonts.json ps2ui-bake reads; only the `metrics` paths of `regular` and `bold` are used, resolved relative to the manifest | packages/layout/src/index.js:94-114 | test/fonts.test.js "a fonts.json manifest loads the same faces as the directory" (bogus `ttf` paths, identical advance and kerning tables) and "a manifest missing a face names ps2ui-fontgen and the weight"; both pass in `npm test` | verified |
| cli.layout.fonts.default | With neither flag the compiler looks three directories above `src/`, which is `fonts/` in a checkout | packages/layout/src/index.js:48,65 | compiled memcard library with no font flag from the checkout; it succeeded and the IR names family `DejaVu...` | verified |
| cli.layout.exit-codes | Exit 2: wrong positional count, no `-o`, unknown `--mode`, malformed `--canvas`, non-positive `--min-font-size`. Exit 1: compile error (`error: ...`), or `--strict` with one or more warnings. Exit 0 otherwise, including warnings without `--strict` | packages/layout/bin/ps2ui-layout.js:78,83,90-93,99,122-129 | ran without `-o` (2), `--mode vga` (2), `--canvas 640` (2), `--min-font-size 0` (2), undirected container (1), missing input file (1), `--strict` on a screen with 2 warnings (1), same screen without `--strict` (0) | verified |
| cli.layout.exit-codes.display-aspect | A malformed `--display-aspect` (`16x9`) is not caught: node prints a stack trace from aspect.js and exits 1, not the usage line and 2 | packages/layout/bin/ps2ui-layout.js:88; packages/layout/src/aspect.js:19-23 | `--display-aspect 16x9` printed `Error: aspect: "16x9" is not a ratio like 4:3 or 16:9` with a stack trace, exit 1 | verified |
| cli.layout.output-lines | Everything except `--version` goes to stderr. Lines: `warning: <rule>: <message>` per warning, then `ps2ui-layout: N paint commands, N focusables -> <out>`, then with `--strict` and warnings `ps2ui-layout: --strict: N warning(s)`. Errors: one `error: <message>` line each, since a CSS pass reports every error it found rather than the first (F37) | packages/layout/bin/ps2ui-layout.js:106,121,123,130 | memcard library run printed 28 warning lines then `ps2ui-layout: 89 paint commands, 9 focusables -> .../library.json`; `2>/dev/null` left stdout empty; `--strict` run ended `ps2ui-layout: --strict: 2 warning(s)`; a sheet with a gradient, a `:hover` and a `display: grid` printed three `error: ` lines at lines 3, 5 and 9, and `cli.test.js` spawns both bins and counts them | verified |
| cli.layout.files | `-o` creates missing parent directories; the IR is written with `JSON.stringify(ir, null, 1)` (one-space indent) | packages/layout/bin/ps2ui-layout.js:113-114 | `-o $S/out/nested/library.json` into a missing tree wrote the file (31443 bytes); `head` shows one-space indentation; test/fonts.test.js "-o creates its output directory" | verified |
| modes.table | MODES: ntsc 640x448 4:3, ntsc16x9 640x448 16:9, pal 640x512 4:3, pal16x9 640x512 16:9; par = aspect / (w/h) | packages/layout/src/aspect.js:47-52,33-35 | compiled memcard library under each mode: ntsc par 0.9333 display 597x448; ntsc16x9 par 1.2444 display 796x448; pal par 1.0667 display 683x512; pal16x9 par 1.4222 display 910x512 | verified |
| cli.dev.options | ps2ui-dev takes two positionals and `-o <outdir>` (required); flags: `--mode`, `--display-aspect`, `--canvas`, `--font-dir`, `--fonts`, `--focus-wrap`, `--strict`, `--min-font-size`, `--montage`, `--palettize-images`, `--once`, `-h`/`--help`, `-V`/`--version` | packages/layout/bin/ps2ui-dev.js:55-87 | `ps2ui-dev --help` printed the usage line; `ps2ui-dev --version` printed `ps2ui-dev 0.8.0-dev.0`; `--mode pal16x9` produced canvas 640x512 [16,9] | verified |
| cli.dev.options.usage-drift | The ps2ui-dev usage line says `--mode ntsc\|pal` and omits `--display-aspect`; the switch accepts all four modes and `--display-aspect` | packages/layout/bin/ps2ui-dev.js:24-28,57-58,109-115 | `ps2ui-dev --mode pal16x9 --once` exited 0 with a pal16x9 canvas; `--help` output does not mention it | verified |
| cli.dev.files | ps2ui-dev creates `<outdir>` and writes `<html stem>.json`, `ui.uib`, `preview.png`, and `states.png` with `--montage`; the bake is `python3 -m ps2ui_bake <stem>.json -o ui.uib --preview preview.png [--montage states.png] [--palettize-images] [--fonts <manifest>]` with PYTHONPATH set to the sibling baker package | packages/layout/bin/ps2ui-dev.js:104,141-157 | `ps2ui-dev examples/memcard/ui/library.html ... -o $S/dev --once` created `library.json`, `preview.png`, `ui.uib`; with `--montage` also `states.png` | verified |
| cli.dev.exit-codes | `--once` exits 0 when the build succeeded and 1 when layout or bake failed; without `-o`, wrong positionals, unknown `--mode` or malformed `--canvas` exit 2 | packages/layout/bin/ps2ui-dev.js:102,110,118,172-173 | `--once` on memcard: exit 0; `--once` on an undirected screen: `layout error: ...`, exit 1, no files written; no `-o`: usage, exit 2; `--mode vga`: usage, exit 2 | verified |
| cli.dev.output-lines | Lines: `layout error: <message>` (red), `bake failed` (red), `built in <n>ms — N commands, N focusables, N warning(s) -> <outdir>/preview.png` (green), then `  warning: ...` per warning; in watch mode `watching <dir> — ctrl-c to stop` and `<file> changed` (dim) before each rebuild | packages/layout/bin/ps2ui-dev.js:146, 182, 186, 189, 213, 202 | `grep -n "console\.\(error\|log\)" packages/layout/bin/ps2ui-dev.js` puts the six printed lines at 127, 163, 167, 170, 194 and 183, in the order the fact lists them. **The previous members were 113, 142, 146-149, 162 and 173**: an argv assignment, a comment about `ps2ui_screen_set`, a `writeFileSync`/`bakeArgs` block, the `if (bake.status !== 0)` one line above `bake failed`, and `build()`. Not one was a print site, and F43 moved the last of them from 173 to 174 mechanically without reading it; review of #162 read it | verified |
| cli.dev.watch | ps2ui-dev watches the HTML, the CSS and the HTML's directory recursively; bursts are debounced by 120 ms | packages/layout/bin/ps2ui-dev.js:177-191 | a watch run rebuilt after an edit to the CSS; a first run with the log file inside the watched directory rebuilt on every log write, which shows the directory watch; the 120 ms figure is read from the setTimeout and not timed | verified |
| cli.dev.inert-flags | `--strict` and `--min-font-size` reach the linter in ps2ui-dev: the bin validates the floor and puts it in `options.lint`, and under `--strict` a compile with warnings prints `ps2ui-dev: --strict: N warning(s)` and fails before the bake, exit 1 with `--once`. Fixed after 0.6.0, shipping in 0.7.0; in 0.6.0 as released both flags are accepted and inert (D9) | packages/layout/bin/ps2ui-dev.js (`options.lint`, `build()`); packages/layout/src/index.js:271 | `node --test` in packages/layout, `ps2ui-dev --strict fails on warnings and --min-font-size moves the floor`; memcard library: `ps2ui-dev --once --min-font-size 40` printed 40 `min-font-size` warnings vs 20 with no flag, the same counts as `ps2ui-layout`; a scratch 8px page with `--once --strict` printed the strict line and exited 1 | verified |
| cli.dev.palettize | `--palettize-images` is forwarded to the baker as `--palettize-images`; ps2ui-dev itself does nothing else with it | packages/layout/bin/ps2ui-dev.js:150 | read in source; the effect belongs to the baker and is not observable from this tool's output without a PNG input | code-only |
| cli.dev.wrapper | `ps2ui dev` builds the ps2ui-dev command from the project file and writes to `<build dir>/dev`; its own flags are `--screen` and `--once` | packages/baker/ps2ui_bake/ps2ui.py `cmd_dev` | `ps2ui dev --help` listed `--screen NAME` and `--once` | verified |
