# facts: cli/ps2ui-bake

| id | fact | source | verified by | status |
|---|---|---|---|---|
| cli.bake.options | Options are `--version`, `ir` (one or more), `-o/--out` (required), `--fonts`, `--preview`, `--montage`, `--preview-display`, `--palettize-images`, `--tints`, `--vram-budget`; nothing else | packages/baker/ps2ui_bake/cli.py:139-161 | `ps2ui-bake --help` in this session; the usage line lists exactly these | verified |
| cli.bake.options.no-layout-flags | `--mode`, `--display-aspect` and `--focus-wrap` are not bake options | packages/baker/ps2ui_bake/cli.py:132-162 (no such add_argument) | `ps2ui-bake --help` shows none of them; README.md:456 and README.md:461-462 present them without naming the tool | contradicts-readme |
| cli.bake.version | `--version` prints `ps2ui-bake <__version__>`; the tree prints `ps2ui-bake 0.10.0.dev0` | packages/baker/ps2ui_bake/cli.py:139 | `ps2ui-bake --version`; packages/baker/tests/test_baker.py:3693 test_every_console_script_answers_version | verified |
| cli.bake.fonts.default | With no `--fonts`, the manifest is `fonts/fonts.json` three directories above the package; absent, the bake prints a manifest template and exits 1 | packages/baker/ps2ui_bake/cli.py:65-80, 233-249 | test_baker.py:3760 test_the_default_font_manifest_is_a_repo_path; the absent branch cannot fire from a checkout, where the file exists | code-only |
| cli.bake.fonts.resolution | Manifest maps face to `{ttf: [candidates], metrics: path}`; `~` expands; relative paths resolve against the manifest's directory; the first existing TTF wins; none existing raises | packages/baker/ps2ui_bake/cli.py:83-128 | test_baker.py:3857 test_a_home_relative_candidate_resolves_to_the_home_directory; test_baker.py:3774 test_a_named_manifest_that_is_absent_is_not_a_traceback | verified |
| cli.bake.fonts.agreement | The IR's `fonts` block (family, weight per face) is compared against the manifest's metrics; a face the manifest lacks or a family/weight mismatch exits 1 before any write | packages/baker/ps2ui_bake/cli.py:22-62, 217-226 | edited memcard library.json `fonts.regular.family` to `Not A Real Family`; bake printed "the IR and the font manifest describe different fonts" and returned 1; test_baker.py:3964 | verified |
| cli.bake.transcript | stderr order: `warning (layout <stem>): ...` per IR warning, `  runtime tables: ...`, optional `  trimmed N draw command(s) ...`, VRAM lines (`tex[i]`, `clut[i]`, `framebuffers assumed`, `payload -> allocator -> budget-charged`, `reclaimable`, `textures N B of M B budget (P%)`), optional tint table, `ps2ui-bake: N screen(s), ...`, `ps2ui-bake: arena ...`, then `preview ->`, `display preview ...`, `montage ->` | packages/baker/ps2ui_bake/cli.py:220, 289, 293, 262-263, 289-300, 372-377, 384-388, 394, 400, 404; caps.py:129-139; vram.py:203-290 | baked examples/memcard/build/library.json and saves.json with `--preview --montage`; stderr pasted on the page | verified |
| cli.bake.transcript.trim | The trim line appears only when the flattener dropped commands; memcard drops none, channel6 (games.json + probe.json) prints `trimmed 20 draw command(s)` | packages/baker/ps2ui_bake/cli.py:291-294; quads.py:576 | baked both example sets in this session | verified |
| cli.bake.transcript.tints | `--tints` prints `# N tint entries over M theme(s): names` then one row per entry with the RGBA vector per theme and the var() name, or `(literal, unthemed)`; a vector equal in every theme is marked `FIXED in every theme` | packages/baker/ps2ui_bake/cli.py:289-306 | memcard bake with `--tints` printed 23 entries over 1 theme, all literal | verified |
| cli.bake.transcript.display | `--preview-display` prints `display preview WxH at N:D -> path`; memcard gives `597x448 at 4:3` | packages/baker/ps2ui_bake/cli.py:396-402 | memcard bake with `--preview-display` in this session | verified |
| cli.bake.summary | The summary line counts screens, records, textures (KiB baked, plus `+ N KiB reserved by slots` when streamed slots exist), CLUTs, then the output path | packages/baker/ps2ui_bake/cli.py:360-377 | memcard: `2 screen(s), 1062 records, 11 textures (128 KiB baked), 1 CLUTs`; opl-env: `161 KiB baked + 61 KiB reserved by slots` | verified |
| cli.bake.screens.stem | Each IR becomes a screen named by its file stem; textures, CLUTs and atlases are shared across screens; the first IR's canvas is the reference | packages/baker/ps2ui_bake/cli.py:215, 221; quads.py:523-537 | read_uib on the memcard bake: screens `['library', 'saves']`, 11 textures for two screens | verified |
| cli.bake.exit-conditions | Exit 1 on: IR version != 1; duplicate stem; default manifest absent (template printed); manifest load error (FileNotFoundError, KeyError, ValueError); IR/manifest font disagreement; flattener ValueError (canvas mismatch, image errors, unknown op); cap error (scissor depth, uint16 counts); VRAM over budget. Everything else exits 0 | packages/baker/ps2ui_bake/cli.py:212-214, 217-219, 237-249, 212-214, 262-269, 275-281, 295-298, 320-323 | triggered in this session: IR version 2, duplicate stem, `--fonts` to a missing file, manifest lacking `ttf` (KeyError, printed `ps2ui-bake: 'ttf'`), font disagreement, `--vram-budget 100000`, channel6 4:3 + 16:9 IRs (canvas mismatch ValueError); tests for the rest: test_baker.py:1827 test_indexed_png_refuses_to_be_resized, 786 test_the_formats_own_uint16_count_still_bounds_the_tables, 1881 test_report_flags_over_budget | verified |
| cli.bake.exit-conditions.no-write | Every refusal happens before `write_uib`; no `.uib` is written on exit 1 | packages/baker/ps2ui_bake/cli.py:337 (first write) vs 169-267 (every return 1) | after each triggered refusal the `-o` path did not exist; test_baker.py:3990 asserts the same for font disagreement | verified |
| cli.bake.exit-conditions.caps | The only runtime cap is `PS2UI_MAX_SCISSOR_DEPTH` (8, parsed from runtime/ps2ui.h, FALLBACK otherwise); nesting at or past it is refused; table counts and slot capacity are bounded only by uint16 | packages/baker/ps2ui_bake/caps.py:27-35, 77-126 | test_baker.py:558 test_caps_match_the_runtime_header, 770 test_the_table_ceilings_are_gone, 786, 811; no test drives the scissor refusal through the CLI | verified |
| cli.bake.outdirs | Parent directories of `-o`, `--preview`, `--montage` and `--preview-display` are created before writing | packages/baker/ps2ui_bake/cli.py:275-279 | test_baker.py:3888 test_bake_creates_its_output_directories | verified |
| cli.bake.vram.budget-default | With no `--vram-budget` the budget is 4 MiB minus three framebuffers at canvas size; memcard at 640x448 reports `753664 B` | packages/baker/ps2ui_bake/vram.py:133, 231-237 | memcard bake printed `textures 163840 B of 753664 B budget (21%)`; test_baker.py:1534 test_default_budget_subtracts_framebuffers | verified |
| arena.line | Format: `ps2ui-bake: arena N bytes (static uint8_t arena[N] __attribute__((aligned(16))))`, printed unconditionally after the summary with the EE (4-byte pointer) figure; memcard in this session: N = 1662. `ps2ui-check` prints `arena: 1662 bytes on the EE (1750 on a 64-bit host; ...)` | packages/baker/ps2ui_bake/cli.py:383-388; arena.py:36-37, 52-63; check.py:151-153 | `ps2ui-bake examples/memcard/build/library.json examples/memcard/build/saves.json -o ui.uib` then `ps2ui-check ui.uib`; README.md:188 hardcodes `arena[1662]`, which matches memcard today and no other blob (opl-env 7319, channel6 10624 in this session) | verified |
| cli.bake.pngs.stable | `--preview` and `--montage` PNGs are byte-identical across two bakes and equal to `examples/memcard/build/preview.png` from `ps2ui build` | packages/baker/ps2ui_bake/cli.py:390-404 | `cmp` over two bakes and the example's build output in this session | verified |
