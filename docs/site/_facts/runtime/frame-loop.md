# facts: runtime/frame-loop

Session commands behind the rows below:

- `make -C runtime test` printed `PASS: 5 checks, 0 failure(s)` (test_narrow) and `PASS: 410 checks, 0 failure(s)` (test_runtime). "ok N" refers to the test_runtime numbering as printed.
- `ps2ui-bake examples/memcard/build/library.json examples/memcard/build/saves.json -o <scratch>/ui.uib` printed `ps2ui-bake: arena 1662 bytes (static uint8_t arena[1662] __attribute__((aligned(16))))`.
- `ps2ui-check examples/memcard/build/ui.uib` printed `# arena: 1662 bytes on the EE (1750 on a 64-bit host; GSTEXTURE holds pointers, so the two differ)`.
- `cc -std=c99 -Wall -Wextra -Werror -Istub -Ivendor/gsKit -Ivendor/host-shim -I. -c <scratch>/x.c -o <scratch>/x.o` from `runtime/` exited 0; the scratch file is the page's minimal example.
- `grep -cniE 'ps2ui_(unload|free|destroy|close|shutdown)' runtime/ps2ui.h runtime/ps2ui.c` printed `runtime/ps2ui.h:0` and `runtime/ps2ui.c:0`.

Parent facts reused without restatement: `api.functions.arena_size`, `api.functions.load`, `api.functions.upload`, `api.functions.render`, `api.ordering.load-first`, `api.ordering.arena-lifetime`, `api.ordering.stats-per-render`, `api.ordering.nextframe`, `api.ordering.render-composites`, `api.return-conventions.codes`, `api.return-conventions.upload`, `api.constants.arena-align`, `errors.arena`, `errors.align`, `errors.too_many`, `errors.upload.minus1`, `load.arena.untouched`, `constants.version.frozen`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| loop.order | The lifecycle in order: size the arena (`ps2ui_arena_size`, or the figure `ps2ui-bake` prints), declare a static `PS2UI_ARENA_ALIGN`-aligned arena, `ps2ui_load`, `ps2ui_upload` once, then per frame: app clear with `PrimAlphaEnable` OFF then ON, `ps2ui_render`, `gsKit_queue_exec`, `gsKit_sync_flip`, `gsKit_TexManager_nextFrame` | runtime/sample/main.c:1671, :1645, :1655, :2622, :2638-2640, :2737, :2739, :2740, :2747 | scratch x.c reproduces the sequence and compiles under `-Wall -Wextra -Werror`; ok 12, 39 | verified |
| loop.guarantees | Per render, `ps2ui_render` guarantees: writes the GS ALPHA register; sets `PrimAlphaEnable` ON; never clears; leaves the scissor at the full canvas; resets `ctx->stats` at entry; never calls `gsKit_TexManager_nextFrame` | runtime/ps2ui.c:988, :1027, :1055-1057, :1064, :1166; runtime/ps2ui.h:612-649 | ok 85, 86, 87, 89, 181, 272, 273, 274 | verified |
| loop.blend.equation | `ps2ui_render` writes `GS_SETREG_ALPHA(0, 1, 0, 1, 0)`, which is `Cv = (Cs - Cd) * As >> 7 + Cd`, every call rather than inheriting gsKit's default | runtime/ps2ui.c:1027 | ok 85 "render sets the GS blend mode rather than inheriting it"; ok 86 "and sets it to (Cs - Cd) * As >> 7 + Cd" | verified |
| loop.primalpha | `ps2ui_render` sets `gs->PrimAlphaEnable = GS_SETTING_ON` every call; gsKit feeds the field to PRIM.ABE and to TEX0.TCC, so OFF makes every glyph fill its quad | runtime/ps2ui.c:1029-1057 | ok 87; the test starts from `GS_SETTING_OFF` (test_runtime.c:790) so a missing assignment fails it | verified |
| loop.primalpha.build-switch | Building with `PS2UI_PRIMALPHA_OFF` forces the field OFF; it is bring-up step 4b's reference arm, not a fallback | runtime/ps2ui.c:1053-1055 | `make -C runtime test` syntax-check prints `ok - runtime compiles: -DPS2UI_PRIMALPHA_OFF` | verified |
| loop.never-clears | `ps2ui_render` issues no clear; two screens rendered in one frame sum their primitives | runtime/ps2ui.h:612-618; runtime/ps2ui.c:940-1173 | ok 271 "compositing two screens draws the sum"; ok 272 "and render issued no clear" | verified |
| loop.clear.app-owns | The app owns the clear. The sample clears before `ps2ui_render` with `PrimAlphaEnable` OFF and turns it back ON, because gsKit does not save and restore the field around `gsKit_clear` and frame 0 would otherwise composite the clear through gsKit's inverted default | runtime/sample/main.c:2651-2668, :215-216 | read in source; the host stub has no GS ALPHA semantics to replay a frame-0 clear against, so nothing executable covers the bracket | code-only |
| loop.scissor.restored | `ps2ui_render` reapplies the full-canvas scissor before returning, so a caller drawing after it inherits the whole screen | runtime/ps2ui.c:1164-1166; runtime/ps2ui.h:646-649 | ok 89 "scissor restored to full canvas after replay"; ok 274 (also after a composite) | verified |
| loop.geometry.on-canvas | Every primitive a render submits lies inside the canvas rectangle; the canvas scissor does not move with `ps2ui_offset_set`, so an offset pushes content off the display rather than outside the clip | runtime/ps2ui.c:1058-1064; runtime/tests/test_runtime.c:796-805 | ok 88 "all primitives inside the canvas"; ok 406 "the canvas scissor does NOT move" | verified |
| loop.stats.per-render | `ctx->stats` is memset at the top of every `ps2ui_render`, so it describes one render and a composited frame ends holding the last render's counters | runtime/ps2ui.c:988; runtime/ps2ui.h:631-634 | ok 181 "stats reset every frame"; ok 276 "stats describe one render, not the composited frame"; ok 277 | verified |
| loop.nextframe.once | `ps2ui_render` never ages residency; `gsKit_TexManager_nextFrame` belongs once per frame after the flip. Calling it between two renders ages the base screen's textures | runtime/ps2ui.h:635-638; runtime/sample/main.c:2769-2775 | ok 273 "and issued no residency ageing tick"; README.md:400 places the call before `gsKit_queue_exec`/`gsKit_sync_flip` (D14) | contradicts-readme |
| loop.two-renders | Two `screen_set`/`render` pairs composite; the last `screen_set` owns the D-pad; the sample's covers arm does exactly this and restores the base screen afterwards | runtime/ps2ui.h:618-629; runtime/sample/main.c:2753-2762 | ok 271, 279, 280 | verified |
| loop.no-unload | There is no unload, free, close or destroy entry point. The 29 public functions are the whole API; teardown is the caller dropping the blob and the arena | runtime/ps2ui.h:453-838 | the `grep -c` above printed 0 in both files; parent `api.functions.count` counted 29 prototypes | verified |
| loop.checked-form | The sample treats both failures as fatal: a non-`PS2UI_OK` load loops clearing solid red `RGBAQ(0x80, 0x00, 0x00, 0x80)`, a non-zero upload loops clearing solid yellow `RGBAQ(0x80, 0x80, 0x00, 0x80)` | runtime/sample/main.c:1673-1691 | read in source; the failure arms are console bring-up screens with no host test; README.md:184-185 calls both without checking the return (D6) | contradicts-readme |
| arena.contract | The arena is caller-provided, at least `ps2ui_arena_size()` bytes, `PS2UI_ARENA_ALIGN` (16) aligned, and must stay alive and unmoved for the context's lifetime, which covers every render and not just the load | runtime/ps2ui.h:443-460; runtime/ps2ui.c:485-500 | ok 68 (exact size accepted), ok 69 (one byte short refused), ok 70 (NULL refused), ok 71 (misaligned refused); parent `api.ordering.arena-lifetime` is code-only for the lifetime half | verified |
| arena.lifetime.why | The CLUT region at the arena's start is a DMA source that gsKit re-reads when the texture manager re-binds an evicted texture, which is render time | runtime/ps2ui.h:443-445, :369-375 | code-only: the host has no DMA and no eviction to observe; the rule is stated in the header | code-only |
| arena.size.zero | `ps2ui_arena_size` returns 0 when the header is unreadable or the tables do not fit in `size`, which is the answer for "do not call load" | runtime/ps2ui.h:448-453; runtime/ps2ui.c:238-243 | ok 66 "arena_size refuses a blob too short to hold a header"; ok 67 "arena_size refuses a bad magic rather than sizing from garbage" | verified |
| arena.validate-first | The arena is carved only after every blob check has passed, so a refused load never writes it and the buffer can be reused | runtime/ps2ui.c:474-500 | ok 73 "a refused blob leaves the arena untouched, so it can be reused"; ok 72 | verified |
| arena.number.per-blob | The arena figure is per blob and per target. For `examples/memcard`, the bake prints 1662 bytes and `ps2ui-check` prints 1662 on the EE and 1750 on a 64-bit host, because `GSTEXTURE` holds pointers | packages/baker/ps2ui_bake/cli.py:323-330; packages/baker/ps2ui_bake/check.py:151-154; runtime/ps2ui.c:163-236 | the bake and check commands listed above, run in this session; README.md:182 shows the literal with no blob or target named (D13) | verified |
| arena.sample.static | The sample declares `static uint8_t arena[16 * 1024] __attribute__((aligned(PS2UI_ARENA_ALIGN)))` and lets `ps2ui_load` report `PS2UI_ERR_ARENA` if it is ever short; static rather than stack so copying the pattern into a function that returns cannot dangle | runtime/sample/main.c:1657-1671 | read in source; a console-shaped choice with no host test, though ok 69 proves the short-arena refusal it relies on | code-only |
| upload.contract | `ps2ui_upload` runs once, after gsKit is initialised and before the first render. It preflights the whole texture sum against 4 MiB, returns -1 and transfers nothing when the sum does not fit, and returns 0 after binding every texture with a source | runtime/ps2ui.h:468-475; runtime/ps2ui.c:567-655 | ok 39, 309, 310, 311, 312, 313 | verified |
| upload.all-or-nothing | A refused upload transfers zero textures and leaves `ctx->uploaded` at 0; a half-uploaded context is deliberately not a reachable state | runtime/ps2ui.c:583-597, :653 | ok 312 "and transfers nothing at all (0 transfers) -- all-or-nothing"; ok 313 | verified |
| upload.preflight.why | gsKit_TexManager_bind cannot report exhaustion: `_blockAlloc` evicts until something fits and never exits when nothing can, so an over-budget blob hangs the console without this check | runtime/ps2ui.c:569-587 | code-only: the host stub has no eviction loop to hang, so the hazard itself is unobservable there; ok 309 covers the refusal | code-only |
| upload.second-call | `ps2ui_upload` sets `ctx->uploaded` and never reads it, so a second upload re-permutes every CLUT from the blob and silently reverts a `ps2ui_clut_set` swap. Nothing in the repository calls it twice | runtime/ps2ui.h:551-558; runtime/ps2ui.c:653 | code-only: no test calls upload twice, as parent `api.ordering.clut-after-upload` records | code-only |
| upload.streamed-reserved | A streamed slot's VRAM is counted in the preflight but nothing is transferred until `ps2ui_tex_set` names its texels; an unfilled slot draws nothing and increments `stats.tex_unfilled` | runtime/ps2ui.h:468-474; runtime/ps2ui.c:644-651, :1152-1154 | ok 328 "upload succeeds with a streamed slot still empty"; ok 329 | verified |
| render.vram-lost | `ps2ui_render` re-checks `ctx->uploaded` and the uploaded footprint every call; when either fails it skips every textured draw, still draws untextured quads, and sets `stats.vram_lost` to 1 | runtime/ps2ui.c:943-989 | ok 51-55 (footprint no longer fits), ok 56-59 (render after a refused upload) | verified |
| render.rebinds | `ps2ui_render` binds every texture it draws, so a texture manager reset heals by re-transfer rather than drawing from stale VRAM | runtime/ps2ui.c:1156 | ok 60 "a draw after invalidation re-transfers"; ok 61 | verified |
| loop.version.constant | `PS2UI_VERSION` is the frozen `.uib` format version, 7, checked by `ps2ui_load`; baker and runtime agree because `ps2ui vendor-runtime` ships both files from one package | runtime/ps2ui.h:37; runtime/ps2ui.c:267-268 | parent `constants.version.frozen`; `python3 tools/check-versions.py --except-tag` passes; README.md:172-173 casts the macro as the anti-drift mechanism (D12) | contradicts-readme |
| loop.minimal.compiles | The page's minimal example compiles against the shipped header under `-std=c99 -Wall -Wextra -Werror` | runtime/ps2ui.h; runtime/sample/main.c | the `cc` command above, exit 0 | verified |
