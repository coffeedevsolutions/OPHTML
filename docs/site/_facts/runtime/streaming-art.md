# facts: runtime/streaming-art

Session commands behind the rows below. All were run from the repository
root unless a row says otherwise. `<scratch>` is
`/tmp/claude-0/-home-user-OPHTML/6b0c72b8-d98f-5f58-b749-f9808bb620d6/scratchpad/runtime/streaming-art`.

- `make -C runtime test` printed `PASS: 5 checks, 0 failure(s)` (test_narrow)
  and `PASS: 410 checks, 0 failure(s)` (test_runtime). "ok N" below is the
  test_runtime numbering as printed.
- `python3 -m unittest discover -s tests -k TestStreamedAuthoring -v` in
  `packages/baker` printed `Ran 8 tests ... OK`.
- `python3 tools/make_cover_raw.py --self-test` printed six `ok -` lines,
  `1..6` and `PASS: make_cover_raw self-test`.
- The bench fixture was compiled and baked into `<scratch>/run/`, never
  through `fixtures/bench-stream/build.sh` and never into
  `fixtures/bench-stream/build/`:
  `ps2ui-layout fixtures/bench-stream/ui/covers.html fixtures/bench-stream/ui/bench.css -o covers.json`,
  the same for `dialog.html`, then
  `ps2ui-bake covers.json dialog.json -o bench.uib --preview covers-empty.png`.
  `<scratch>/run` holds a symlink named `fixtures` to the repository's
  `fixtures/`, so the printed paths are the ones a reader sees from the
  repository root. The blob it wrote is byte-identical to the committed
  `fixtures/bench-stream/build/bench.uib` (`cmp` exit 0), and the preview
  is byte-identical to `fixtures/bench-stream/build/preview.png`.
- `python3 tools/make_cover_raw.py examples/channel6/ui/assets/cover-kaiju.png --out-dir covers --size 128x128 --count 4`
  from `<scratch>/run`, which holds symlinks named `tools` and `examples`
  to the repository. `cover0.raw` is byte-identical to the same conversion
  run from the repository root.
- `cc -std=c99 -Wall -Wextra -Werror -Istub -Ivendor/gsKit -Ivendor/host-shim -I. -c <scratch>/stream_min.c`
  from `runtime/` exited 0; the scratch file is the page's minimal example.

Parent facts reused without restatement: `images.streamed`,
`images.alpha`, `images.texture.key`, `vram.payload-vs-pages`,
`vram.runtime.streamed-counted`, `vram.breakdown.columns`,
`api.functions.tex_set`, `api.functions.clut_set`, `api.functions.upload`,
`api.scope.rules.tex`, `api.scope.rules.clut-theme`,
`api.ordering.tex-any`, `api.ordering.clut-after-upload`,
`api.structs.stats`, `api.return-conventions.codes`,
`upload.streamed-reserved`, `loop.blend.equation`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| stream.authoring.form | A streamed slot is authored as `<img data-tex-slot="NAME">` with an explicit CSS `width` and `height` and no `src`. The blob carries geometry, the name and a PSMCT32 reservation of `w * h * 4` bytes, and no texels | packages/layout/src/box.js:227-262; packages/baker/ps2ui_bake/quads.py:390-418 | parent fact `images.streamed`, reused; test_baker.py:1021 `test_a_streamed_slot_reaches_the_blob` asserts `reservation == 64 * 64 * 4` and `data == b""`, and passes in the 8-test run | verified |
| stream.authoring.shared | Two elements naming one slot share one reservation and draw it twice. The name is the sharing key | packages/baker/ps2ui_bake/quads.py:400-408 (`self._streamed` lookup by name) | test_baker.py:1053 `test_two_elements_share_one_slot` asserts one streamed texture and two `OP_TEXQUAD` records; test_baker.py:1038 `test_the_slot_is_actually_drawn` asserts the single element draws once at 64x64 | verified |
| stream.authoring.one-size | One name at two laid-out sizes fails the bake: `image: streamed slot 'cover' is laid out at WxH in one place and WxH in another; a slot has one reservation, so give them the same size or different names` | packages/baker/ps2ui_bake/quads.py:404-412 | test_baker.py:1069 `test_one_name_at_two_sizes_is_refused` asserts the bake returns non-zero and the message contains `one reservation` | verified |
| stream.psmct32-only | A streamed reservation is PSMCT32 only. `_streamed_texture` appends `gs.PSMCT32` unconditionally and never assigns a CLUT, because quantizing needs art that does not exist at bake time | packages/baker/ps2ui_bake/quads.py:394-398 (docstring), packages/baker/ps2ui_bake/quads.py:415-417; packages/layout/src/box.js:254-260 | parent fact `images.streamed`, reused; the bench bake in this session printed `tex[ 1] PSMCT32   128x128  streamed` for all four slots and no `PSMT8` streamed row | verified |
| tex.contract | `ps2ui_tex_set(ctx, gs, name, texels, len)` points a streamed slot at the caller's texels. `len` must equal the entry's `data_len` exactly. `texels` must be 16-byte aligned. Nothing is copied: the pointer becomes the slot's DMA source and must stay alive and unmoved for as long as the slot can be drawn, because gsKit re-reads it at render time when the texture manager re-binds an evicted texture. The call writes the buffer back from the EE cache and invalidates residency. It is safe before `ps2ui_upload` and safe to call again to swap texels | runtime/ps2ui.h:477-509; runtime/ps2ui.c:675-733 (`runtime/ps2ui.c:692` kind, `runtime/ps2ui.c:697` len, `runtime/ps2ui.c:703` alignment, `runtime/ps2ui.c:710` `g->Mem`, `runtime/ps2ui.c:726` `SyncDCache`, `runtime/ps2ui.c:732` `gsKit_TexManager_invalidate`) | ok 323-327 (unknown name, baked texture, one byte short, one byte long, misaligned), ok 332 (exact reservation accepted), ok 333 (`gs_tex[ci].Mem` is the caller's pointer), ok 334 (the cache writeback covers exactly `texels..texels+len`), ok 337-339 (a second call swaps, repoints and invalidates). Agrees with parent `api.functions.tex_set` and `api.ordering.tex-any` | verified |
| tex.len-is-payload | The `len` to pass is the `payload` figure on the slot's VRAM row, not the `in pages` figure. `data_len` is set to `w * h * 4` at bake and the page-rounded number is `PS2UI_ERR_SIZE` | runtime/ps2ui.h:141-143, runtime/ps2ui.h:468-476; packages/baker/ps2ui_bake/quads.py:415-417 | parent fact `vram.payload-vs-pages`, reused; test_baker.py:1092 `test_the_breakdown_prints_the_number_tex_set_demands` uses a 100x70 slot where payload 28000 and pages 49152 differ, and asserts both appear on the row; the bench bake in this session printed `65536 B payload ->   65536 B in pages` for a 128x128 slot | verified |
| tex.before-upload | `ps2ui_tex_set` has no upload-state guard, unlike `ps2ui_clut_set`. It returns its ordinary codes before `ps2ui_upload` and never `PS2UI_ERR_STATE` | runtime/ps2ui.c:675-733 (no `ctx->uploaded` test anywhere in the function); runtime/ps2ui.h:506-508 | ok 323-327 all call `tex_set` before the `ps2ui_upload` at ok 328 and get NOT_STREAMED, SIZE and ALIGN. The accepting call is exercised after upload (ok 332); no test accepts one before it, so the success path before upload rests on the header | verified |
| tex.errors | `ps2ui_tex_set` returns `PS2UI_ERR_NOT_STREAMED` (-10) for a NULL argument, an unknown name or a baked texture, `PS2UI_ERR_SIZE` (-11) when `len` differs from `data_len`, and `PS2UI_ERR_ALIGN` (-8) for a texel address not 16-byte aligned. The return is a bare code: nothing reports the number that was expected | runtime/ps2ui.h:424-441, runtime/ps2ui.h:494-496; runtime/ps2ui.c:682-706 | ok 323-327; the header at :471-476 records the missing accessor as a gap and names the bake output as where the number lives | verified |
| stream.unfilled | An unfilled streamed slot draws nothing and increments `stats.tex_unfilled`. `ps2ui_upload` skips binding it because `g->Mem` is NULL, and `ps2ui_render` skips the textured draw on the same test. Other draws in the frame are unaffected | runtime/ps2ui.c:644-651, runtime/ps2ui.c:1147-1155; runtime/ps2ui.h:323-326 | ok 328 (upload succeeds with the slot empty), ok 329 (`Mem` left NULL), ok 330 (`stats.tex_unfilled == 1`), ok 331 (the baked quad still draws), ok 336 (the counter returns to 0 once filled). Agrees with parent `upload.streamed-reserved` | verified |
| stream.reservation-charged | A streamed slot's VRAM is charged from the moment the blob loads, filled or not. The bench blob's four unfilled 128x128 slots are 256 KiB of the 368640 B charged | packages/baker/ps2ui_bake/vram.py:213-242; runtime/ps2ui.c:589-595 | parent facts `vram.runtime.streamed-counted` and `vram.payload-vs-pages`, reused; the bench bake in this session printed `9 textures (96 KiB baked + 256 KiB reserved by slots)` and `textures 368640 B of 753664 B budget (48%)` | verified |
| clut.contract | `ps2ui_clut_set(ctx, gs, clut_index, colors, ncolors)` repoints one CLUT without moving a texel. `colors` is linear, as the blob stores palettes; the CSM1 permutation is applied on the way into the pool. The call must follow `ps2ui_upload` and is refused before it with `PS2UI_ERR_STATE`. Every texture sharing the index changes together. The swap takes effect on the next bind, which `ps2ui_render` performs for every texture it draws; `clut_set` does not bind | runtime/ps2ui.h:511-574; runtime/ps2ui.c:739-777 (`runtime/ps2ui.c:752` state, `runtime/ps2ui.c:754` range, `runtime/ps2ui.c:761` width, `runtime/ps2ui.c:765` permute, `runtime/ps2ui.c:770-775` `VramClut = 0` per sharing texture) | ok 354-356 (a palette no wider than the baked one is accepted, moves no texels and does move palette bytes), ok 359 (one palette per drawn texture, exactly), ok 360-361 (written in CSM1 order, not linear), ok 362-364 (index past `n_clut`, a wider palette, and a call before upload refused). Agrees with parent `api.functions.clut_set` and `api.scope.rules.clut-theme` | verified |
| clut.short-palette | A palette shorter than the baked width blanks the tail rather than leaving it alone. `permute_clut` opens with `memset(out, 0, 256 * 4)`, so a 16-entry palette handed to a 256-entry CLUT erases 240 entries to transparent black | runtime/ps2ui.c:543-556 (`runtime/ps2ui.c:546` the memset); runtime/ps2ui.h:560-566 | code-only: the suite covers the width refusal (ok 363) and the CSM1 ordering (ok 361), and nothing asserts the erased tail. The blanking is `permute_clut`'s opening memset, which ok 361 exercises for the entries it does write | code-only |
| clut.no-survive-upload | A CLUT swap does not survive an upload in either direction. Before upload it is refused; a second `ps2ui_upload` re-permutes every CLUT from the blob and reverts a swap with no error, because `upload` sets `ctx->uploaded` and never reads it | runtime/ps2ui.h:538-558; runtime/ps2ui.c:653 (`ctx->uploaded = 1`), runtime/ps2ui.c:615-631 (the re-permute loop) | ok 364 covers the refusal before upload. Nothing in the repository calls `ps2ui_upload` twice, so the reversion is the header's statement and is unexercised; parent `api.ordering.clut-after-upload` and `upload.second-call` record the same split | code-only |
| clut.vs-theme | `ps2ui_clut_set` schedules a palette transfer and takes a `GSGLOBAL`; `ps2ui_theme_set` moves a pointer, takes no `GSGLOBAL`, works before upload and survives one. An app using both does the CLUT swap last | runtime/ps2ui.h:576-608 | ok 389 (`theme_set` works before upload, where `clut_set` is refused), ok 364 (the refusal). Agrees with parent `api.functions.theme_set` | verified |
| stream.host-format | `tools/make_cover_raw.py` writes a bare `.raw` of exactly `width * height * 4` bytes of PSMCT32, row-major, with no header. Every alpha goes through `css_alpha_to_gs`, so an opaque texel reaches the GS as 0x80 and not 0xFF. A source whose size differs from the reservation is cover-fitted and centre-cropped, so the picture bends and the geometry does not | tools/make_cover_raw.py:1-30 (module docstring), tools/make_cover_raw.py:41-45 (`encode`), tools/make_cover_raw.py:49-66 (`convert`) | `python3 tools/make_cover_raw.py --self-test` printed `ok - a cover is exactly w * h * 4 bytes`, `ok - opaque alpha lands in the GS domain (0x80), not 255 ...` and `ok - 128x128 is 65536 bytes, which is what the blob reserves`, then `PASS`. Converting `examples/channel6/ui/assets/cover-kaiju.png` (160x96) at `--size 128x128` wrote four 65536 B files, and `sorted(set(d[3::4]))` is `[128]` for each. Agrees with parent `images.alpha` | verified |
| stream.host-format.why-bare | The file carries no header because the ELF reads it straight into a 16-aligned buffer and hands that buffer to `ps2ui_tex_set`. Anything in front of the texels would have to be stripped by a copy, which is the cost the zero-copy contract exists to avoid. The ELF checks the file's size instead | tools/make_cover_raw.py:11-18; runtime/sample/main.c:1398-1412 (`read` into `cover_buf[i]`, then a length comparison against `COVER_BYTES`) | read in source; the read path is `PS2UI_SAMPLE_USB`-only and needs a console with a drive, so no host test covers it | code-only |
| stream.host-format.deterministic | `synthetic(index, w, h)` generates a coarse two-hue checker with a one-texel border and a corner block, deterministically, so a photograph can answer both "which cover is this" and "did it arrive intact". The same generator exists in C at `runtime/sample/cover_pattern.h` and the two are pinned to one CRC | tools/make_cover_raw.py:68-104; runtime/sample/cover_pattern.h | the self-test's `ok - two synthetic covers differ`, `ok - and carries more than two distinct texels` and `ok - generation is deterministic`; test_baker.py:1198 `TestCoverPattern.test_python_matches_the_crc_the_header_pins` | verified |
| stream.deploy.usb | A blob is compiled into the ELF by `bin2c` and needs no file beside it. Streamed texels are the exception: the sample reads them from `mass:/ps2ui/cover0.raw` onward, so a console filling slots needs USB attached | docs/deploying.md:23-34; runtime/sample/main.c:1346, runtime/sample/main.c:1362-1366 | read in source and in the repository document; the path is `PS2UI_SAMPLE_USB`-only and needs hardware. `tools/make_cover_raw.py` prints the same instruction as its closing line, which this session saw: `make_cover_raw: copy covers/ to the drive as ps2ui/ (so the ELF finds mass:/ps2ui/cover0.raw)` | code-only |
| stream.bench.fixture | `fixtures/bench-stream/ui` bakes to two screens and nine textures, four of them 128x128 PSMCT32 streamed slots named `cover0` to `cover3`. The blob is 74 records, 1 CLUT, arena 1875 bytes | fixtures/bench-stream/ui/covers.html:6-11; fixtures/bench-stream/ui/bench.css:9-11 | the layout and bake commands listed above, run in this session; the bake printed the four `streamed    65536 B payload ->   65536 B in pages` rows, `2 screen(s), 74 records, 9 textures (96 KiB baked + 256 KiB reserved by slots), 1 CLUTs` and `arena 1875 bytes`. The blob matches the committed `fixtures/bench-stream/build/bench.uib` byte for byte | verified |
| stream.bench.sample-path | The bench ELF holds one static 16-aligned `cover_buf[4][65536]`, reads four files off USB into it, and calls `ps2ui_tex_set` once per slot. Phase 0 sets nothing, phase 1 fills all four, phase 2 repoints `cover0` at `cover3`'s texels to make the residency invalidation visible | runtime/sample/main.c:1269-1275, runtime/sample/main.c:1357-1387, runtime/sample/main.c:2681-2708 | read in source; the arm is `PS2UI_SAMPLE_USB`-only and is a console reading. ok 337-339 cover the repoint-and-invalidate behaviour the phase-2 step is built to show | code-only |
| stream.minimal.compiles | The page's minimal example compiles against the shipped header under `-std=c99 -Wall -Wextra -Werror` | runtime/ps2ui.h | the `cc` command above, exit 0 | verified |

## follow-up

| where | finding |
|---|---|
| tools/make_cover_raw.py:64 | `Image.getdata()` is deprecated in current Pillow and warns on every real conversion: `DeprecationWarning: Image.Image.getdata is deprecated and will be removed in Pillow 14 (2027-10-15). Use get_flattened_data instead.` The parent facts file `authoring/images` records the same call site in `packages/baker/ps2ui_bake/quads.py`; this is a second one, in `convert()`, that no drift row or follow-up names. `--self-test` does not reach it, so CI is silent about it |
