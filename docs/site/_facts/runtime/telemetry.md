# facts: runtime/telemetry

Session commands behind the rows below:

- `make -C runtime test` printed `PASS: 5 checks, 0 failure(s)` (test_narrow) and `PASS: 418 checks, 0 failure(s)` (test_runtime). "ok N" below is the test_runtime numbering as printed in this session.
- `make -C runtime syntax-check` printed `ok - sample compiles: -DPS2UI_SAMPLE_TELEMETRY` among its lines, exit 0.
- `grep -n "TELEMETRY" runtime/sample/Makefile` and `grep -n "EE_BIN" runtime/sample/Makefile` read in this session.

Parent facts reused without restatement: `api.structs.stats`, `api.functions.render`, `api.ordering.stats-per-render`, `loop.stats.per-render`, `loop.two-renders`, `render.vram-lost`.

| id | fact | source | verified by | status |
|---|---|---|---|---|
| stats.fields | `ps2ui_stats` has eight `uint32_t` counters: `cmds`, `prims`, `skipped_hidden`, `slot_glyphs`, `slots_hidden`, `scissor_overflow`, `tex_unfilled`, `vram_lost` | runtime/ps2ui.h:310-331 | ok 174-181, 330, 51 (parent `api.structs.stats` reused) | verified |
| stats.comment.no-timing | The struct comment states the runtime does no timing and no I/O; a host has no EE cycle counter and no log sink, both are the app's decision | runtime/ps2ui.h:303-309 | read in source; a repository comment, nothing to run | code-only |
| stats.comment.cost | The comment states the counting cost is one increment per command record walked plus one per primitive drawn, proportional to the frame and not to the API | runtime/ps2ui.h:306-308 | read in source | code-only |
| stats.reset.render-entry | `ps2ui_render` calls `memset(&ctx->stats, 0, sizeof ctx->stats)` as its first act on `ctx->stats`, before walking any command | runtime/ps2ui.c:1028 | ok 181 "stats reset every frame"; test_runtime.c:1257 | verified |
| stats.reset.vram_lost-set-next | The line after the memset sets `stats.vram_lost` from the upload/footprint check computed earlier in the function, so `vram_lost` is never left at the memset's zero when the check fails | runtime/ps2ui.c:1029 | ok 51 "render reports vram_lost when the uploaded footprint no longer fits" | verified |
| stats.increment.cmds | `cmds` increments once per command record the loop visits, including SCISSOR_PUSH and SCISSOR_POP records | runtime/ps2ui.c:1111 | ok 175 "stats.cmds is the current screen's record count" | verified |
| stats.increment.scissor_overflow | `scissor_overflow` increments when a SCISSOR_PUSH is refused for want of stack depth (`PS2UI_MAX_SCISSOR_DEPTH`, 8); the subtree then draws under the enclosing clip instead | runtime/ps2ui.c:1120; runtime/ps2ui.h:297 | ok 176 "a baked blob never overflows the scissor stack" (a baked blob never trips it; no test forces an overflow) | code-only |
| stats.increment.skipped_hidden | `skipped_hidden` increments once per command record skipped because its focus node is hidden; a hidden row's own suppressed slot text is not counted here | runtime/ps2ui.c:1158 | ok 177 "nothing hidden, nothing skipped"; ok 179-180 (hiding one node moves records from prims to skipped_hidden) | verified |
| stats.increment.prims.quad | `prims` increments once for an untextured `PS2UI_OP_QUAD` submitted to `gsKit_prim_sprite` | runtime/ps2ui.c:1171 | ok 174 "stats.prims agrees with the stub's independent count" | verified |
| stats.increment.prims.texquad | `prims` increments again for a textured `PS2UI_OP_TEXQUAD` submitted, but only when `tex_ok` (the render-time VRAM check passed) | runtime/ps2ui.c:1178 | ok 174; ok 56-59 in `render.vram-lost` (parent) cover the `tex_ok` false path drawing no textured prims | verified |
| stats.increment.tex_unfilled | `tex_unfilled` increments when a textured draw's streamed slot has no texels yet (`gs_tex[c->tex].Mem == NULL`); the draw is skipped, not an error | runtime/ps2ui.c:1193 | ok 328-329 (parent `upload.streamed-reserved`: upload succeeds with a streamed slot still empty) | verified |
| stats.increment.slots_hidden | `slots_hidden` increments once per slot suppressed by runtime visibility | runtime/ps2ui.c:1360 | code-only: no test_runtime CHECK isolates this counter by name; covered only as part of the general visibility suite | code-only |
| stats.increment.slot_glyphs | `slot_glyphs` increments once per glyph quad the slot pen composes, alongside a matching `prims` increment, at two call sites (one per text-run class) | runtime/ps2ui.c:1407-1408, 1425-1426 | ok 178 "slot glyphs are a subset of the stub's textured prims" | verified |
| stats.increment.vram_lost.only-site | `vram_lost` has one write site, the post-memset assignment; nothing in the walk loop touches it again | runtime/ps2ui.c:1029 | `grep -n 'stats.vram_lost' runtime/ps2ui.c` in this session shows one write at :989 and the read at :954 comment | verified |
| stats.readout-format | The sample's telemetry build (`PS2UI_SAMPLE_TELEMETRY`) prints one line per elapsed second: `ps2ui-telemetry frame=%u fps=%u.%u miss=%u ee_us(min/avg/max)=%u/%u/%u prims=%u hidden=%u slotg=%u slothid=%u sciov=%u vramlost=%u` | runtime/sample/main.c:2806-2814 | read in source; `make -C runtime syntax-check` compiles this variant, no host stdout to capture on a host build | code-only |
| stats.readout.per-frame-sample | `ee_us` times only the `ps2ui_render` call with `cop0_count()` before and after, not the app's clear or the flip; min, max and sum accumulate across every frame in the second, and the printed avg divides sum by frame count | runtime/sample/main.c:2671-2677, 2807-2810 | read in source; no host COP0 counter exists to execute this, and since the macOS fix `cop0_count` says so in code -- the asm is guarded on `__mips__` and the host arm returns 0. Both ranges are this row's originals shifted by the +28 lines that guard added: 2643-2649 brackets the render sample and its min/max/sum, 2779-2782 the `ee_us(min/avg/max)` print | code-only |
| stats.readout.peaks | `prims`, `hidden` (`skipped_hidden`), `slotg` (`slot_glyphs`) and `slothid` (`slots_hidden`) are the interval's peak single-frame value, tracked with a running max, not a sum | runtime/sample/main.c:2646-2647, 2789-2792 | read in source | code-only |
| stats.readout.sciov-sum | `sciov` (`scissor_overflow`) is the interval sum, added every frame rather than maxed, because a baked blob overflowing the stack at all is the alarm | runtime/sample/main.c:2647, 2765 | read in source | code-only |
| stats.readout.vramlost-or | `vramlost` is the interval's logical OR of every frame's `vram_lost`, not the last frame's value, so one bad frame in an otherwise-fine second still reads `vramlost=1` | runtime/sample/main.c:2798 (`a_vramlost |=`) | read in source | code-only |
| stats.readout.miss | `miss` counts frames in the interval whose wall-clock loop time (vsync included) exceeded one 60 Hz field plus 10 percent tolerance (`EE_HZ/60u + EE_HZ/600u`) | runtime/sample/main.c:2787-2789 | read in source | code-only |
| stats.readout.reset-per-second | Every aggregate (`t_min`, `t_max`, `t_sum`, `sec_frames`, `missed`, `elapsed`, the five peak/sum counters) is reset to its start value at the end of the printed line, so each line describes exactly one elapsed second | runtime/sample/main.c:2803-2819 | read in source | code-only |
| stats.build.telemetry-flag | `make -C runtime/sample TELEMETRY=1` adds `-DPS2UI_SAMPLE_TELEMETRY` to `EE_CFLAGS`; `EE_BIN` is an ordinary Makefile variable overridable on the command line (default `ps2ui_sample.elf`), so `make -C runtime/sample TELEMETRY=1 EE_BIN=telemetry.elf` builds the telemetry variant under that name | runtime/sample/Makefile:22-27, 8 | `grep -n TELEMETRY runtime/sample/Makefile` and `grep -n EE_BIN runtime/sample/Makefile` in this session | verified |
| stats.build.syntax-check-covers | `make -C runtime syntax-check` compiles the `-DPS2UI_SAMPLE_TELEMETRY` variant of `sample/main.c` as part of its loop over `SAMPLE_VARIANTS`, on the host, without linking an EE toolchain | runtime/Makefile:40, 81-86 | `make -C runtime syntax-check` in this session printed `ok - sample compiles: -DPS2UI_SAMPLE_TELEMETRY` | verified |
